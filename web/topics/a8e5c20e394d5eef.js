window.QUESTION_TOPICS = window.QUESTION_TOPICS || {};
window.QUESTION_TOPICS["content/01_cpp/14_macro_and_build.md"] = [{"id":"content/01_cpp/14_macro_and_build.md:3e54730f7c7c5d10:1","question":"<p>Что такое препроцессор в C++?</p>\n","answer":"<p><strong>Ответ:</strong> Препроцессор — это предварительный инструмент конвейера трансляции, выполняющий текстовые преобразования исходного кода (включение файлов, макроподстановку, условную компиляцию) до начала синтаксического и семантического анализа компилятором.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define BUFFER_SIZE 512\nchar buffer[BUFFER_SIZE]; // Препроцессор заменит BUFFER_SIZE на 512\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor?utm_source=gemini\">Cppreference: Preprocessor</a></p>\n","group":"Лёгкий уровень","line":7},{"id":"content/01_cpp/14_macro_and_build.md:3e86fae40581250f:1","question":"<p>На каком этапе обработки кода работает препроцессор?</p>\n","answer":"<p><strong>Ответ:</strong> Препроцессор работает на самых первых фазах трансляции C++ (фазы 1–4 из 9 стандартизированных), выполняя нормализацию символов, объединение строк, удаление комментариев и выполнение директив до передачи результата компилятору.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Получение вывода препроцессора (фазы 1-4) в GCC/Clang:\ng++ -E main.cpp -o main.i\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/translation_phases?utm_source=gemini\">Cppreference: Translation phases</a></p>\n","group":"Лёгкий уровень","line":21},{"id":"content/01_cpp/14_macro_and_build.md:f167148dcc57c8b2:1","question":"<p>Чем препроцессор отличается от компилятора?</p>\n","answer":"<p><strong>Ответ:</strong> Препроцессор оперирует сырым текстом и лексемами препроцессинга без понимания грамматики, типов данных, пространств имён и областей видимости; компилятор выполняет синтаксический анализ, проверку системы типов и генерацию машинного или ассемблерного кода.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define FOO int x;\n// Препроцессор лишь копирует текст &quot;int x;&quot;, а компилятор проверяет синтаксис и аллоцирует переменную\nFOO\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.stroustrup.com/4th.html?utm_source=gemini\">The C++ Programming Language (Bjarne Stroustrup)</a></p>\n","group":"Лёгкий уровень","line":34},{"id":"content/01_cpp/14_macro_and_build.md:dae51935d9a91a8e:1","question":"<p>Что такое директива препроцессора?</p>\n","answer":"<p><strong>Ответ:</strong> Директива препроцессора — это специальная строка в исходном тексте программы, начинающаяся с символа <code>#</code> (без учёта пробелов перед ним), которая даёт препроцессору команду выполнить определенное текстовое преобразование.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#line 100 &quot;generated_source.cpp&quot; // Директива управления информацией о строках\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor?utm_source=gemini\">Cppreference: Preprocessor</a></p>\n","group":"Лёгкий уровень","line":49},{"id":"content/01_cpp/14_macro_and_build.md:311355a781e52741:1","question":"<p>Почему директивы препроцессора начинаются с символа <code>#</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Символ решётки <code>#</code> был исторически выбран создателями языка C (Деннисом Ритчи и Майком Леском) в качестве уникального управляющего префикса в начале строки, не конфликтующего с базовыми лексемами, идентификаторами и операторами языка.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-c\">/* Исторический C78: строки с '#' обрабатывались отдельной утилитой /lib/cpp */\n#include &lt;stdio.h&gt;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.bell-labs.com/usr/dmr/www/chist.html?utm_source=gemini\">The Development of the C Language (Dennis M. Ritchie)</a></p>\n","group":"Лёгкий уровень","line":62},{"id":"content/01_cpp/14_macro_and_build.md:f346d369cbe1aa5e:1","question":"<p>Что делает директива <code>#include</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#include</code> предписывает препроцессору найти указанный заголовочный или исходный файл, полностью прочитать его содержимое и вставить его «как есть» в текущую точку единицы трансляции вместо самой строки директивы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#include &lt;iostream&gt; // Содержимое заголовка iostream вставляется в эту строку\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/include?utm_source=gemini\">Cppreference: Source file inclusion</a></p>\n","group":"Лёгкий уровень","line":76},{"id":"content/01_cpp/14_macro_and_build.md:5c5d8ae9e2b399a0:1","question":"<p>Что делает директива <code>#define</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#define</code> определяет макрос препроцессора, ассоциируя идентификатор либо со списком параметров и телом подстановки (функциональный макрос), либо просто с последовательностью токенов (объектный макрос).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define PLATFORM_ID 1\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini\">Cppreference: Replacing text macros</a></p>\n","group":"Лёгкий уровень","line":89},{"id":"content/01_cpp/14_macro_and_build.md:3e4f09f7bb4b0acc:1","question":"<p>Что делает директива <code>#undef</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#undef</code> отменяет текущее определение макроса с указанным именем, после чего данный идентификатор больше не заменяется препроцессором и считается неопределённым для директив <code>#ifdef</code>/<code>#ifndef</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define DEBUG_MODE\n#undef DEBUG_MODE\n// Теперь DEBUG_MODE не определён\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.google.com/search?q=https://en.cppreference.com/w/cpp/preprocessor/replace%2523.23undef&amp;utm_source=gemini\">Cppreference: Replacing text macros: #undef</a></p>\n","group":"Лёгкий уровень","line":102},{"id":"content/01_cpp/14_macro_and_build.md:15a9f47d7c60d14c:1","question":"<p>Что делает директива <code>#if</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#if</code> проверяет константное целочисленное выражение времени препроцессинга; если выражение истинно (не равно нулю), последующий блок кода включается в компиляцию, иначе он отбрасывается до парной директивы <code>#elif</code>, <code>#else</code> или <code>#endif</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define VERSION 2\n#if VERSION &gt; 1\n    void new_feature();\n#endif\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/conditional?utm_source=gemini\">Cppreference: Conditional inclusion</a></p>\n","group":"Лёгкий уровень","line":117},{"id":"content/01_cpp/14_macro_and_build.md:1b6e762d1460d32a:1","question":"<p>Что делает директива <code>#ifdef</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#ifdef IDENTIFIER</code> проверяет факт определения макроса с заданным именем (эквивалентно <code>#if defined(IDENTIFIER)</code>); код внутри ветки компилируется только в том случае, если макрос был предварительно объявлен через <code>#define</code> или флаг компилятора <code>-D</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifdef __linux__\n    #include &lt;unistd.h&gt;\n#endif\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/conditional?utm_source=gemini\">Cppreference: Conditional inclusion</a></p>\n","group":"Лёгкий уровень","line":133},{"id":"content/01_cpp/14_macro_and_build.md:767e89376a439e30:1","question":"<p>Что делает директива <code>#ifndef</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#ifndef IDENTIFIER</code> проверяет, что макрос с указанным именем <em>не</em> был определён (эквивалентно <code>#if !defined(IDENTIFIER)</code>); используется преимущественно для предотвращения повторного включения заголовочных файлов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifndef MY_HEADER_H\n#define MY_HEADER_H\n// Содержимое заголовочного файла\n#endif\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/conditional?utm_source=gemini\">Cppreference: Conditional inclusion</a></p>\n","group":"Лёгкий уровень","line":148},{"id":"content/01_cpp/14_macro_and_build.md:0376d70e2cdd85f3:1","question":"<p>Что делает директива <code>#elif</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#elif</code> (сокращение от «else if») предоставляет альтернативную ветку условной компиляции с дополнительным логическим выражением, если условие предшествующего <code>#if</code> или <code>#elif</code> оказалось ложным.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#if defined(_WIN32)\n    const char* os = &quot;Windows&quot;;\n#elif defined(__APPLE__)\n    const char* os = &quot;macOS&quot;;\n#endif\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/conditional?utm_source=gemini\">Cppreference: Conditional inclusion</a></p>\n","group":"Лёгкий уровень","line":164},{"id":"content/01_cpp/14_macro_and_build.md:3b97237bfe3ab45a:1","question":"<p>Что делает директива <code>#else</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#else</code> открывает блок кода, который будет включён в единицу трансляции в том случае, если все предшествующие условия в цепочке <code>#if</code>/<code>#elif</code>/<code>#ifdef</code>/<code>#ifndef</code> вернули ложь.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifdef TEST_MODE\n    int port = 8080;\n#else\n    int port = 80;\n#endif\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/conditional?utm_source=gemini\">Cppreference: Conditional inclusion</a></p>\n","group":"Лёгкий уровень","line":181},{"id":"content/01_cpp/14_macro_and_build.md:9f019acf86508bcf:1","question":"<p>Что делает директива <code>#endif</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#endif</code> обозначает окончание области действия блока условной компиляции, открытого директивами <code>#if</code>, <code>#ifdef</code> или <code>#ifndef</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#if 0\n    // Этот код полностью вырезается препроцессором\n#endif\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/conditional?utm_source=gemini\">Cppreference: Conditional inclusion</a></p>\n","group":"Лёгкий уровень","line":198},{"id":"content/01_cpp/14_macro_and_build.md:7de85e570fa21921:1","question":"<p>Что делает директива <code>#error</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#error message</code> принудительно прерывает процесс компиляции на этапе препроцессинга и выводит указанное пользователем диагностическое сообщение в консоль сборщика.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#if __cplusplus &lt; 202002L\n    #error &quot;This library requires at least a C++20 compliant compiler!&quot;\n#endif\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.google.com/search?q=https://en.cppreference.com/w/cpp/preprocessor/error&amp;utm_source=gemini\">Cppreference: Error directive</a></p>\n","group":"Лёгкий уровень","line":213},{"id":"content/01_cpp/14_macro_and_build.md:ed23d9233bea8367:1","question":"<p>Для чего нужна директива <code>#pragma</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#pragma</code> передаёт платформозависимые указания и специальные команды конкретному компилятору (управление предупреждениями, выравниванием структур, оптимизациями или подавлением повторных включений), сохраняя переносимость (неизвестные прагмы игнорируются).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#pragma once\n#pragma pack(push, 1)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/impl?utm_source=gemini\">Cppreference: Diagnostic directives: #pragma</a></p>\n","group":"Лёгкий уровень","line":228},{"id":"content/01_cpp/14_macro_and_build.md:fc203775420b0dfb:1","question":"<p>Что такое макрос?</p>\n","answer":"<p><strong>Ответ:</strong> Макрос — это именованный фрагмент текста или шаблон замены, зарегистрированный в таблице препроцессора, который автоматически подставляется и раскрывается в коде везде, где встречается его идентификатор.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define PI 3.14159\ndouble area = PI * r * r;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini\">Cppreference: Replacing text macros</a></p>\n","group":"Лёгкий уровень","line":242},{"id":"content/01_cpp/14_macro_and_build.md:bba1c72f992b3da1:1","question":"<p>Чем объектоподобный макрос отличается от функциональноподобного?</p>\n","answer":"<p><strong>Ответ:</strong> Объектный макрос не принимает аргументов и замещает идентификатор фиксированной последовательностью токенов; функциональноподобный макрос имеет список формальных параметров в круглых скобках сразу за именем (без пробела) и подставляет аргументы в шаблон.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define MAX_ITEMS 100               // Объектный\n#define SQUARE(x) ((x) * (x))       // Функциональноподобный\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini\">Cppreference: Replacing text macros</a></p>\n","group":"Лёгкий уровень","line":256},{"id":"content/01_cpp/14_macro_and_build.md:95b2d904a9cb752e:1","question":"<p>Как объявить простой макрос-константу?</p>\n","answer":"<p><strong>Ответ:</strong> Макрос-константа объявляется с помощью директивы <code>#define</code>, за которой следует имя идентификатора в верхнем регистре (согласно общепринятой конвенции) и значение подстановки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define TIMEOUT_MS 5000\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines</a></p>\n","group":"Лёгкий уровень","line":270},{"id":"content/01_cpp/14_macro_and_build.md:097a4f99ecdad072:1","question":"<p>Как объявить макрос с параметрами?</p>\n","answer":"<p><strong>Ответ:</strong> Макрос с параметрами объявляется директивой <code>#define NAME(arg1, arg2) ...</code>, где открывающая круглая скобка следует вплотную за именем макроса без пробельного символа, а параметры в теле оборачиваются в скобки для сохранения приоритета операций.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define MIN(a, b) (((a) &lt; (b)) ? (a) : (b))\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini\">Cppreference: Replacing text macros</a></p>\n","group":"Лёгкий уровень","line":283},{"id":"content/01_cpp/14_macro_and_build.md:6edcad1dbcfc6159:1","question":"<p>Что такое подстановка макроса?</p>\n","answer":"<p><strong>Ответ:</strong> Подстановка (раскрытие) макроса — это процесс текстовой замены идентификатора макроса на связанную с ним последовательность токенов препроцессором, сопровождающийся подстановкой фактических аргументов вместо формальных параметров (если макрос с параметрами).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define ADD(a, b) ((a) + (b))\nint x = ADD(2, 3); // Превращается в: int x = ((2) + (3));\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini\">Cppreference: Replacing text macros</a></p>\n","group":"Лёгкий уровень","line":296},{"id":"content/01_cpp/14_macro_and_build.md:8f7e0d2b8f01b5b8:1","question":"<p>Чем макрос отличается от переменной <code>const</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Переменная <code>const</code> подчиняется правилам областей видимости (scope), имеет строгий тип данных, адрес в памяти и проверяется компилятором; макрос глобален для всего последующего текста файла, не имеет типа и подменяется до компиляции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">const int GlobalLimit = 100; // Типобезопасно, имеет область видимости\n#define MACRO_LIMIT 100      // Игнорирует namespaces и классы, засоряя глобальный скоуп\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.google.com/search?q=https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines%2523Res-macros&amp;utm_source=gemini\">C++ Core Guidelines: ES.31: Don’t use macros for constants or functions</a></p>\n","group":"Лёгкий уровень","line":310},{"id":"content/01_cpp/14_macro_and_build.md:2ddfe71349b43c36:1","question":"<p>Чем макрос отличается от <code>constexpr</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>constexpr</code> функции и переменные являются полноправными конструкциями C++, типизированы, могут вычисляться как в compile-time, так и в runtime, доступны для отладки в IDE и подчиняются правилам пространств имён, в отличие от слепой замены текста макросом.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">constexpr int square(int x) { return x * x; } // Типобезопасно, без побочных эффектов двойного вычисления\n#define SQUARE(x) ((x) * (x))                 // SQUARE(x++) вызовет x++ дважды!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/constexpr?utm_source=gemini\">Cppreference: constexpr specifier</a></p>\n","group":"Лёгкий уровень","line":324},{"id":"content/01_cpp/14_macro_and_build.md:f18651f857b08bb3:1","question":"<p>Почему макросы не знают о типах?</p>\n","answer":"<p><strong>Ответ:</strong> Препроцессор работает на фазах трансляции, предшествующих синтаксическому разбору, построению AST и семантическому анализу; для него любой исходный код — это лишь набор символов и токенов без концепции типов языка C++.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define SET_VAL(target, val) target = val;\n// Макрос подставит аргументы даже если типы несовместимы, ошибка возникнет только в компиляторе\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.stroustrup.com/dne.html?utm_source=gemini\">The Design and Evolution of C++ (Bjarne Stroustrup)</a></p>\n","group":"Лёгкий уровень","line":338},{"id":"content/01_cpp/14_macro_and_build.md:fc4547e9fca523fa:1","question":"<p>Почему макросы считаются текстовой подстановкой?</p>\n","answer":"<p><strong>Ответ:</strong> Потому что алгоритм их работы сводится исключительно к манипуляциям над лексемами и строками: замене одного фрагмента текста на другой с подстановкой строковых фрагментов аргументов без проверки валидности синтаксиса результирующего C++ кода.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define NUMBER 1 + 2\nint result = NUMBER * 3; // Раскрывается в 1 + 2 * 3 = 7, а не (1 + 2) * 3 = 9\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini\">Cppreference: Replacing text macros</a></p>\n","group":"Лёгкий уровень","line":352},{"id":"content/01_cpp/14_macro_and_build.md:98517692d3338e3a:1","question":"<p>Что такое заголовочный файл?</p>\n","answer":"<p><strong>Ответ:</strong> Заголовочный файл (header, <code>.h</code>/<code>.hpp</code>) — это текстовый файл, содержащий интерфейсные объявления типов данных, структур, классов, сигнатур функций, шаблонов и констант, предназначенный для совместного включения через <code>#include</code> в различные единицы трансляции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// math_utils.hpp\n#pragma once\nint add(int a, int b); // Объявление интерфейса\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language?utm_source=gemini\">Cppreference: Headers and source files</a></p>\n","group":"Лёгкий уровень","line":366},{"id":"content/01_cpp/14_macro_and_build.md:7231200cdfc355d7:1","question":"<p>Что такое исходный файл <code>.cpp</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Исходный файл (<code>.cpp</code>, <code>.cc</code>, <code>.cxx</code>) — это файл с исходным кодом программы, содержащий конкретную реализацию объявленных функций и методов, который напрямую передается компилятору в качестве самостоятельной единицы трансляции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// math_utils.cpp\n#include &quot;math_utils.hpp&quot;\nint add(int a, int b) { return a + b; } // Реализация\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/translation_phases?utm_source=gemini\">Cppreference: Translation units</a></p>\n","group":"Лёгкий уровень","line":381},{"id":"content/01_cpp/14_macro_and_build.md:122062e6448b2da3:1","question":"<p>Чем заголовочный файл отличается от файла реализации?</p>\n","answer":"<p><strong>Ответ:</strong> Заголовочный файл декларирует контракты (<em>что</em> доступно для вызова) и обычно не компилируется отдельно в объектный файл; файл реализации определяет исполняемый код (<em>как</em> это работает) и компилируется в <code>.o</code>/<code>.obj</code> файл.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// .hpp -&gt; объявление: void print_message();\n// .cpp -&gt; определение: void print_message() { std::cout &lt;&lt; &quot;Hello\\n&quot;; }\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.google.com/search?q=https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines%2523S-source&amp;utm_source=gemini\">C++ Core Guidelines: Source files</a></p>\n","group":"Лёгкий уровень","line":396},{"id":"content/01_cpp/14_macro_and_build.md:b2d1323ec9e166ce:1","question":"<p>Для чего вообще разделяют <code>.h/.hpp</code> и <code>.cpp</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Разделение ускоряет компиляцию за счёт параллельной сборки независимых единиц трансляции, скрывает детали реализации за стабильными границами интерфейса и предотвращает нарушения One Definition Rule (ODR) при линковке неинлайновых сущностей.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Изменение тела функции в .cpp перекомпилирует только один файл, не затрагивая зависимые модули\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.oreilly.com/?utm_source=gemini\">Large-Scale C++ Software Design (John Lakos)</a></p>\n","group":"Лёгкий уровень","line":410},{"id":"content/01_cpp/14_macro_and_build.md:02fea6c77b3f4568:1","question":"<p>Что такое единица трансляции?</p>\n","answer":"<p><strong>Ответ:</strong> Единица трансляции (Translation Unit, TU) — это исходный файл (<code>.cpp</code>) вместе со всеми включенными в него заголовочными файлами и раскрытыми макросами после завершения работы препроцессора, передаваемый на вход компилятору.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// main.cpp + все заголовочные файлы из #include = единый поток кода для одной единицы трансляции\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/translation_phases?utm_source=gemini\">Cppreference: Translation phases: Translation unit</a></p>\n","group":"Лёгкий уровень","line":423},{"id":"content/01_cpp/14_macro_and_build.md:aaf5b03350caec61:1","question":"<p>Что обычно входит в одну единицу трансляции после препроцессинга?</p>\n","answer":"<p><strong>Ответ:</strong> В развернутую единицу трансляции входят тысячи строк объявлений стандартной библиотеки и сторонних заголовков, типы данных, инлайн-функторы, шаблоны и непосредственно код самого исходного файла <code>.cpp</code> (без комментариев и директив препроцессора).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Простой файл с '#include &lt;vector&gt;' после препроцессора разворачивается в 20 000+ строк кода\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/include?utm_source=gemini\">Cppreference: Source file inclusion</a></p>\n","group":"Лёгкий уровень","line":436},{"id":"content/01_cpp/14_macro_and_build.md:b9e0f433713d3519:1","question":"<p>Что значит “подключить заголовок”?</p>\n","answer":"<p><strong>Ответ:</strong> Это значит прописать директиву <code>#include &lt;header&gt;</code> или <code>#include &quot;header&quot;</code>, предписывая препроцессору найти данный заголовок в файловой системе и физически вставить всё его содержимое в текущую точку файла.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#include &quot;config.h&quot; // Подключение пользовательского заголовка\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/include?utm_source=gemini\">Cppreference: Source file inclusion</a></p>\n","group":"Лёгкий уровень","line":449},{"id":"content/01_cpp/14_macro_and_build.md:62919fa924fb309a:1","question":"<p>Чем <code>#include &lt;...&gt;</code> отличается от <code>#include &quot;...&quot;</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Порядком и алгоритмом поиска файла в каталогах: форма со скобками <code>&lt;...&gt;</code> ищет заголовок строго по системным стандартным путям компилятора (<code>-I</code>); форма с кавычками <code>&quot;...&quot;</code> сначала проверяет текущую директорию с исходным файлом, и только затем переходит к системным путям.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#include &lt;string&gt;     // Системный каталог стандартной библиотеки\n#include &quot;my_class.h&quot; // Текущая папка проекта\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/include?utm_source=gemini\">Cppreference: Source file inclusion</a></p>\n","group":"Лёгкий уровень","line":462},{"id":"content/01_cpp/14_macro_and_build.md:e829c7fe30af816b:1","question":"<p>Когда используют угловые скобки в <code>#include</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Угловые скобки <code>&lt;...&gt;</code> используют при подключении компонентов стандартной библиотеки C++, системных API заголовков ОС (POSIX, Windows SDK) и внешних сторонних библиотек, установленных в системные или внешние include-директории.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#include &lt;vector&gt;\n#include &lt;fcntl.h&gt;\n#include &lt;boost/asio.hpp&gt;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.google.com/search?q=https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines%2523SF-include-syntax&amp;utm_source=gemini\">C++ Core Guidelines: SF.12: Prefer the quoted form of #include for files relative to the including file and the angle bracket form everywhere else</a></p>\n","group":"Лёгкий уровень","line":476},{"id":"content/01_cpp/14_macro_and_build.md:dcd704d51c3a4067:1","question":"<p>Когда используют кавычки в <code>#include</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Двойные кавычки <code>&quot;...&quot;</code> используют для подключения собственных заголовочных файлов проекта, расположенных относительно текущего исходного файла или в локальных директориях исходного кода приложения.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#include &quot;network/socket.hpp&quot;\n#include &quot;../common/types.hpp&quot;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines</a></p>\n","group":"Лёгкий уровень","line":491},{"id":"content/01_cpp/14_macro_and_build.md:80377606f10d6a9f:1","question":"<p>Что происходит, если один и тот же заголовок подключить несколько раз?</p>\n","answer":"<p><strong>Ответ:</strong> Без использования защитных механизмов содержимое файла вставится повторно, что приведёт к ошибкам компилятора о повторном переопределении типов, структур, классов или неинлайновых переменных (<code>redefinition of 'struct Foo'</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// a.h\nstruct Bar {};\n// main.cpp\n#include &quot;a.h&quot;\n#include &quot;a.h&quot; // Ошибка компиляции: redefinition of 'struct Bar'\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.google.com/search?q=https://en.cppreference.com/w/cpp/language/definition%2523One_Definition_Rule&amp;utm_source=gemini\">Cppreference: One Definition Rule</a></p>\n","group":"Лёгкий уровень","line":505},{"id":"content/01_cpp/14_macro_and_build.md:b7da46cf9cfc8f15:1","question":"<p>Что такое include guard?</p>\n","answer":"<p><strong>Ответ:</strong> Include guard (страж включения) — это идиома препроцессора, оборачивающая всё содержимое заголовочного файла в связку <code>#ifndef</code>, <code>#define</code> и <code>#endif</code>, гарантирующая, что тело файла будет включено в единицу трансляции ровно один раз.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifndef MY_PROJECT_LOGGER_HPP\n#define MY_PROJECT_LOGGER_HPP\n\nclass Logger {};\n\n#endif // MY_PROJECT_LOGGER_HPP\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.wikipedia.org/wiki/Include_guard?utm_source=gemini\">Wikipedia: Include guard</a></p>\n","group":"Лёгкий уровень","line":522},{"id":"content/01_cpp/14_macro_and_build.md:27ce5eceeb0aa8a4:1","question":"<p>Зачем нужны include guards?</p>\n","answer":"<p><strong>Ответ:</strong> Они защищают проект от ошибок нарушения One Definition Rule (ODR) и рекурсивных циклических включений, когда заголовок <code>A.h</code> косвенно включает <code>B.h</code>, а тот в свою очередь снова включает <code>A.h</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Без include guards включение headers приведёт к бесконечной вложенности препроцессора:\n// fatal error: #include nested too deeply\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.google.com/search?q=https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines%2523Rs-guards&amp;utm_source=gemini\">C++ Core Guidelines: SF.8: Use #include guards for all .h files</a></p>\n","group":"Лёгкий уровень","line":540},{"id":"content/01_cpp/14_macro_and_build.md:e5ebcd20b83cab7a:1","question":"<p>Как выглядит классический include guard?</p>\n","answer":"<p><strong>Ответ:</strong> Он состоит из трёх директив: проверки отсутствия уникального макроса <code>#ifndef</code>, немедленного его определения <code>#define</code> и закрытия блока <code>#endif</code> в самом конце файла.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifndef UNIQUE_NAME_HPP\n#define UNIQUE_NAME_HPP\n\n// Объявления\n\n#endif // UNIQUE_NAME_HPP\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/include?utm_source=gemini\">Cppreference: Source file inclusion</a></p>\n","group":"Лёгкий уровень","line":554},{"id":"content/01_cpp/14_macro_and_build.md:a662eade00b78a8b:1","question":"<p>Что такое <code>#pragma once</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>#pragma once</code> — это директива препроцессора, поддерживаемая всеми современными компиляторами, сообщающая, что текущий файл должен быть физически открыт и прочитан препроцессором только один раз в рамках сборки данной единицы трансляции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#pragma once\n\nclass Engine {};\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.wikipedia.org/wiki/Pragma_once?utm_source=gemini\">Wikipedia: Pragma once</a></p>\n","group":"Лёгкий уровень","line":572},{"id":"content/01_cpp/14_macro_and_build.md:614f06fe22668893:1","question":"<p>Чем <code>#pragma once</code> похоже на include guard?</p>\n","answer":"<p><strong>Ответ:</strong> Она решает абсолютно идентичную практическую задачу: предотвращает дублирование кода заголовков, исключает ошибки многократного переопределения типов и разрывает циклы взаимозависимых включений.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// И #pragma once, и include guard гарантируют однократное включение тела заголовка в TU\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/cpp/Pragmas.html?utm_source=gemini\">GCC Online Docs: Pragmas</a></p>\n","group":"Лёгкий уровень","line":587},{"id":"content/01_cpp/14_macro_and_build.md:1dccefc7f2bcfed4:1","question":"<p>Чем <code>#pragma once</code> отличается от include guard?</p>\n","answer":"<p><strong>Ответ:</strong> <code>#pragma once</code> не стандартизирована (хотя де-факто повсеместно поддержана), избавляет от необходимости придумывать уникальные имена макросов и ускоряет сборку (компилятор даже не открывает файл повторно), но может сбоить при работе со сложными симлинками или распределёнными файловыми системами.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// #pragma once: 1 строка, нет риска опечатки в имени макроса, не входит в стандарт ISO C++\n// include guard: 3 строки, гарантированная переносимость стандартом C++\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/?utm_source=gemini\">Clang Documentation: #pragma once</a></p>\n","group":"Лёгкий уровень","line":600},{"id":"content/01_cpp/14_macro_and_build.md:6f61a8ccc8d95482:1","question":"<p>Что такое процесс сборки программы?</p>\n","answer":"<p><strong>Ответ:</strong> Сборка программы — это многоэтапный автоматизированный процесс трансформации набора текстовых файлов с исходным кодом C++ в готовый исполнимый бинарный файл, динамическую (<code>.so</code>/<code>.dll</code>) или статическую (<code>.a</code>/<code>.lib</code>) библиотеку.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\">g++ main.cpp utils.cpp -o my_app\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"http://csapp.cs.cmu.edu/?utm_source=gemini\">Computer Systems: A Programmer's Perspective (CS:APP)</a></p>\n","group":"Лёгкий уровень","line":614},{"id":"content/01_cpp/14_macro_and_build.md:51884ddd1c7802f9:1","question":"<p>Из каких основных этапов состоит сборка C++-программы?</p>\n","answer":"<p><strong>Ответ:</strong> Классический конвейер сборки состоит из 4 последовательных этапов:</p>\n<ol>\n<li>\n<p>Препроцессинг (Preprocessing)</p>\n</li>\n<li>\n<p>Компиляция (Compilation)</p>\n</li>\n<li>\n<p>Ассемблирование (Assembly)</p>\n</li>\n<li>\n<p>Компоновка / Линковка (Linking)</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// .cpp -&gt; [Препроцессор] -&gt; .i -&gt; [Компилятор] -&gt; .s -&gt; [Ассемблер] -&gt; .o -&gt; [Линкер] -&gt; executable\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/gcc/Overall-Options.html?utm_source=gemini\">GCC Documentation: Overall Options</a></p>\n</li>\n</ol>\n","group":"Лёгкий уровень","line":627},{"id":"content/01_cpp/14_macro_and_build.md:93afc8ea861126f6:1","question":"<p>Что такое препроцессинг?</p>\n","answer":"<p><strong>Ответ:</strong> Препроцессинг — это первый этап сборки, на котором обрабатываются директивы <code>#</code>, разворачиваются заголовочные файлы <code>#include</code>, подставляются макросы <code>#define</code>, отсекаются неактивные ветки условий <code>#if</code> и формируется чистый монолитный текст единицы трансляции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Выполнение только этапа препроцессинга:\nclang++ -E main.cpp -o main.ii\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor?utm_source=gemini\">Cppreference: Preprocessor</a></p>\n","group":"Лёгкий уровень","line":645},{"id":"content/01_cpp/14_macro_and_build.md:9c6af481630bb763:1","question":"<p>Что такое компиляция?</p>\n","answer":"<p><strong>Ответ:</strong> Компиляция (в узком смысле) — это этап, на котором синтаксический анализатор разбирает текст единицы трансляции, строит абстрактное синтаксическое дерево (AST), проверяет семантику и систему типов, выполняет оптимизации и генерирует текстовый ассемблерный код целевой процессорной архитектуры.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Остановка после генерации ассемблерного кода:\ng++ -S main.cpp -o main.s\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language?utm_source=gemini\">Cppreference: Compilation</a></p>\n","group":"Лёгкий уровень","line":659},{"id":"content/01_cpp/14_macro_and_build.md:5046fe62920c77c5:1","question":"<p>Что такое ассемблирование?</p>\n","answer":"<p><strong>Ответ:</strong> Ассемблирование — это этап, на котором ассемблер транслирует человекочитаемый ассемблерный листинг целевой архитектуры в двоичный машинный код процессора и упаковывает его в промежуточный объектный файл (<code>.o</code> или <code>.obj</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Генерация объектного файла без линковки:\ng++ -c main.cpp -o main.o\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://sourceware.org/binutils/docs/as/?utm_source=gemini\">GNU Assembler (as) Manual</a></p>\n","group":"Лёгкий уровень","line":673},{"id":"content/01_cpp/14_macro_and_build.md:9f0e0e93cbabf6ee:1","question":"<p>Что такое компоновка?</p>\n","answer":"<p><strong>Ответ:</strong> Компоновка (линковка) — это финальный этап сборки, на котором линкер объединяет множество объектных файлов (<code>.o</code>) и системных библиотек, сопоставляет неразрешенные символы (адреса внешних функций и глобальных переменных) и формирует результирующий исполняемый файл.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Ручной запуск компоновщика через g++:\ng++ main.o utils.o -o application\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.iecc.com/linker/?utm_source=gemini\">Linkers and Loaders (John R. Levine)</a></p>\n","group":"Лёгкий уровень","line":687},{"id":"content/01_cpp/14_macro_and_build.md:29e4d669035bcb17:1","question":"<p>Что делает компилятор?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор преобразует высокоуровневые конструкции C++ в низкоуровневые инструкции: производит лексический, синтаксический и семантический анализ, генерирует промежуточное представление (IR), выполняет оптимизации (инлайнинг, векторизацию, разворот циклов) и порождает объектный код.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Компилятор превращает C++ выражение:\n// int z = x + y;\n// в машинную инструкцию: add eax, edx\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.aosabook.org/en/llvm.html?utm_source=gemini\">LLVM Project: Compiler Architecture</a></p>\n","group":"Лёгкий уровень","line":701},{"id":"content/01_cpp/14_macro_and_build.md:cbd7571e32dbafb5:1","question":"<p>Что делает линкер?</p>\n","answer":"<p><strong>Ответ:</strong> Линкер связывает вызовы функций с их фактическими определениями в других объектных файлах (resolution), выполняет перемещение адресов секций кода и данных (relocation) и генерирует единый бинарный файл формата ELF (Linux) или PE (Windows).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Если линкер не найдет тело объявленной функции print(), он выбросит ошибку:\n// undefined reference to 'print()'\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://refspecs.linuxfoundation.org/elf/gabi4+/contents.html?utm_source=gemini\">System V Application Binary Interface</a></p>\n","group":"Лёгкий уровень","line":716},{"id":"content/01_cpp/14_macro_and_build.md:75b4687c7da2f9f6:1","question":"<p>Что получается после работы препроцессора?</p>\n","answer":"<p><strong>Ответ:</strong> Результатом работы препроцессора является так называемая единица трансляции (Translation Unit) — текстовый поток, в котором раскрыты все директивы <code>#include</code> (их содержимое физически вставлено в файл), подставлены значения и тела макросов <code>#define</code>, отсечены неактивные ветки условной компиляции (<code>#if</code>, <code>#ifdef</code>), а также удалены все комментарии.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Команда для генерации результата препроцессинга:\n// g++ -E main.cpp -o main.i\n// В main.i будет чистый C++ код без макросов и комментариев\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/translation_phases\">cppreference: Phases of translation</a></p>\n","group":"Ниже среднего уровня","line":733},{"id":"content/01_cpp/14_macro_and_build.md:fa2dd245fbb111f3:1","question":"<p>Что получает компилятор на вход после препроцессинга?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор (frontend) получает на вход единицу трансляции — один непрерывный плоский поток C++-кода, полностью очищенный от директив <code>#</code> (за исключением служебных маркеров строк <code>#line</code>) и готовый к лексическому, синтаксическому и семантическому анализу.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Исходный файл main.cpp содержал:\n// #define MAX_VAL 100\n// int limit = MAX_VAL;\n//\n// Компилятор на вход получает только:\n// int limit = 100;\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/gcc/Preprocessor-Output.html\">GCC Online Docs: Preprocessor Output</a></p>\n","group":"Ниже среднего уровня","line":747},{"id":"content/01_cpp/14_macro_and_build.md:09ff714e41dcf77c:1","question":"<p>Что такое объектный файл?</p>\n","answer":"<p><strong>Ответ:</strong> Объектный файл — это промежуточный бинарный файл, порождаемый ассемблером/компилятором из одной единицы трансляции. Он содержит сгенерированный машинный код, сегменты инициализированных и неинициализированных данных, таблицу символов (определенных и требуемых внешних), а также таблицы перемещений (relocation records), необходимые линкеру для финальной сборки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Получение объектного файла из исходника без линковки:\ng++ -c math.cpp -o math.o\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://refspecs.linuxfoundation.org/elf/gabi4+/ch4.intro.html\">System V Application Binary Interface (ELF Specification)</a></p>\n","group":"Ниже среднего уровня","line":764},{"id":"content/01_cpp/14_macro_and_build.md:6b0eacce357142d6:1","question":"<p>Что такое исполняемый файл?</p>\n","answer":"<p><strong>Ответ:</strong> Исполняемый файл — это законченный бинарный файл (форматов ELF, PE или Mach-O), содержащий машинные инструкции, метаданные и структуру заголовков, достаточную для того, чтобы операционная система (загрузчик OS loader) могла отобразить его сегменты в виртуальную память процесса, настроить стек/кучу и передать управление на точку входа (например, <code>_start</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Запуск исполняемого файла в POSIX-системе:\n./my_application\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.osdev.org/ELF\">OSDev Wiki: Executable and Linkable Format</a></p>\n","group":"Ниже среднего уровня","line":777},{"id":"content/01_cpp/14_macro_and_build.md:46e4c227e50f681c:1","question":"<p>Что такое библиотека?</p>\n","answer":"<p><strong>Ответ:</strong> Библиотека — это структурированный набор скомпилированного кода (функций, классов, глобальных данных) и служебных таблиц символов, упакованный в отдельный файл для повторного использования другими программами без необходимости повторной компиляции исходников.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Использование сторонней библиотеки через заголовок и флаг линковки:\n#include &lt;zlib.h&gt;\n// Сборка с флагом: g++ main.o -lz\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://tldp.org/HOWTO/Program-Library-HOWTO/index.html\">TLDP: Program Library HOWTO</a></p>\n","group":"Ниже среднего уровня","line":790},{"id":"content/01_cpp/14_macro_and_build.md:0e2384fb5b12dbc5:1","question":"<p>Чем статическая библиотека отличается от динамической?</p>\n","answer":"<p><strong>Ответ:</strong> Статическая библиотека внедряется линкером непосредственно в тело итогового бинарника на этапе сборки (копируются нужные объектные модули). Динамическая библиотека связывается поверхностно на этапе сборки и загружается в память динамическим загрузчиком либо при старте процесса, либо во время его выполнения (<code>dlopen</code>), что позволяет разделять один и тот же код в физической памяти между несколькими процессами.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Статическая линковка:\ng++ main.o libmath.a -o app_static\n# Динамическая линковка:\ng++ main.o -L. -lmath -o app_shared\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.iecc.com/linker/\">Levine, J. R. &quot;Linkers and Loaders&quot;</a></p>\n","group":"Ниже среднего уровня","line":804},{"id":"content/01_cpp/14_macro_and_build.md:3d0994db60ea95c2:1","question":"<p>Что такое <code>.o</code> или <code>.obj</code> файл?</p>\n","answer":"<p><strong>Ответ:</strong> Это стандартные расширения для объектных файлов. Расширение <code>.o</code> принято в POSIX-совместимых ОС (Linux, macOS) для формата ELF/Mach-O, а <code>.obj</code> используется преимущественно в Windows и компиляторах MSVC для формата COFF.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Сборка под MSVC порождает .obj:\ncl /c utility.cpp # Создает utility.obj\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/cpp/build/reference/c-compile-without-linking\">Microsoft Docs: MSVC Compiler Reference /c</a></p>\n","group":"Ниже среднего уровня","line":819},{"id":"content/01_cpp/14_macro_and_build.md:2780ae5a0e74d62c:1","question":"<p>Что такое <code>.a</code> или <code>.lib</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Это статические библиотеки (архивы). Файл <code>.a</code> (Linux/macOS) создается утилитой <code>ar</code> и представляет собой обычный архив сжатых <code>.o</code> файлов с таблицей символов. Файл <code>.lib</code> в Windows может быть как статической библиотекой (архив <code>.obj</code> файлов), так и «библиотекой импорта» (import library), содержащей заглушки для связывания с <code>.dll</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Создание статического архива в Linux:\nar rcs libutils.a file1.o file2.o\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://sourceware.org/binutils/docs/binutils/ar.html\">GNU Binutils: ar documentation</a></p>\n","group":"Ниже среднего уровня","line":832},{"id":"content/01_cpp/14_macro_and_build.md:d400c2197240db49:1","question":"<p>Что такое <code>.so</code>, <code>.dll</code>, <code>.dylib</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Это расширения динамических (разделяемых) библиотек в различных операционных системах: <code>.so</code> (Shared Object) — Linux и Unix-подобные системы; <code>.dll</code> (Dynamic Link Library) — Microsoft Windows; <code>.dylib</code> (Dynamic Library) — Apple macOS.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Просмотр зависимостей от динамических библиотек в Linux:\nldd my_program\n# Вывод: libc.so.6 =&gt; /lib/x86_64-linux-gnu/libc.so.6\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://man7.org/linux/man-pages/man8/ld.so.8.html\">Linux man-pages: ld.so(8)</a></p>\n","group":"Ниже среднего уровня","line":845},{"id":"content/01_cpp/14_macro_and_build.md:880cfdc55dfa64aa:1","question":"<p>Что такое символ в контексте компоновки?</p>\n","answer":"<p><strong>Ответ:</strong> Символ — это поименованная запись в таблице символов объектного файла, которая связывает идентификатор (имя функции, переменной, mangled name) со смещением относительно секции кода (<code>.text</code>) или данных (<code>.data</code>, <code>.bss</code>), либо указывает, что идентификатор должен быть найден во внешнем файле.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Просмотр таблицы символов через утилиту nm:\nnm -C my_file.o\n# Вывод: 0000000000000000 T calculate_sum(int, int)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://refspecs.linuxfoundation.org/elf/gabi4+/ch4.symtab.html\">System V ABI: Symbol Table</a></p>\n","group":"Ниже среднего уровня","line":859},{"id":"content/01_cpp/14_macro_and_build.md:9fc09ab3dfcb5dc7:1","question":"<p>Что такое определение символа?</p>\n","answer":"<p><strong>Ответ:</strong> Определение символа — это создание самого объекта или функции, при котором компилятор резервирует конкретное место в секции данных или генерирует машинный код в секции <code>.text</code>. Такое действие создает запись в таблице символов с установленным адресом и ненулевым размером.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">int global_counter = 0; // Определение глобального символа (резервирует 4 байта в .bss/.data)\n\nvoid execute_task() {}  // Определение функции (генерирует инструкции в секции .text)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/definition\">cppreference: Definitions and ODR</a></p>\n","group":"Ниже среднего уровня","line":873},{"id":"content/01_cpp/14_macro_and_build.md:45d3f3315df8ff83:1","question":"<p>Что такое объявление символа?</p>\n","answer":"<p><strong>Ответ:</strong> Объявление символа — это инструкция компилятору о существовании идентификатора, его типе и сигнатуре, без выделения под него памяти или генерации машинного кода. Объявление сообщает компилятору: «данный символ существует, проверяй соответствие типов, а реальный адрес подставит линкер».</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">extern int global_counter; // Объявление переменной (память не выделяется)\nvoid execute_task();       // Объявление (сигнатура) функции\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/declarations\">cppreference: Declarations</a></p>\n","group":"Ниже среднего уровня","line":887},{"id":"content/01_cpp/14_macro_and_build.md:83ffbe6741b80e25:1","question":"<p>Чем объявление функции отличается от её определения в контексте сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Объявление нужно только компилятору для проверки корректности вызова (аргументы, возвращаемый тип) на этапе генерации единичного <code>.o</code> файла. Определение порождает реальный машинный код в секции <code>.text</code> объектного файла, адрес которого линкер обязан подставить на место вызовов во время этапа компоновки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">void run(); // Объявление: компилятор сгенерирует call run [смещение 0000 и метку для релокации]\n\nvoid run() { /*...*/ } // Определение: генерирует тело функции и экспортирует символ run\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/\">GCC: Function Declarations vs Definitions</a></p>\n","group":"Ниже среднего уровня","line":900},{"id":"content/01_cpp/14_macro_and_build.md:326638aebf17d130:1","question":"<p>Почему функция может быть объявлена в заголовке, но определена в <code>.cpp</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Такое разделение позволяет включать заголовок во множество разных единиц трансляции для использования интерфейса без дублирования кода. Определение при этом компилируется всего один раз в один объектный файл, что предотвращает множественное появление одного и того же тела функции и ускоряет компиляцию.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// math_utils.h\nint add(int a, int b); // Видно всем, кто подключит header\n\n// math_utils.cpp\n#include &quot;math_utils.h&quot;\nint add(int a, int b) { return a + b; } // Скомпилируется ровно один раз в math_utils.o\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines#sf1-keep-declarations-in-header-files\">C++ Core Guidelines: SF.1 Keep declarations in header files</a></p>\n","group":"Ниже среднего уровня","line":914},{"id":"content/01_cpp/14_macro_and_build.md:d5cf7a7e3c25bea4:1","question":"<p>Почему линкеру нужны определения, а не только объявления?</p>\n","answer":"<p><strong>Ответ:</strong> Инструкция процессорного вызова (например, <code>call</code> в архитектуре x86) требует реального абсолютного или относительного адреса перехода. Объявление не порождает адреса в памяти. Если определение отсутствует, линкер не сможет выполнить операцию релокации (relocation) и подставить валидный адрес в команду перехода.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Объявление есть:\nvoid fetch_data();\n\nint main() {\n    fetch_data(); // Компилятор оставил дыру под адрес для линкера.\n                  // Без тела fetch_data() подставить нечего.\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eli.thegreenplace.net/2011/08/25/load-time-relocation-of-shared-libraries\">Eli Bendersky: How linkers work</a></p>\n","group":"Ниже среднего уровня","line":931},{"id":"content/01_cpp/14_macro_and_build.md:f68731dd9c942406:1","question":"<p>Что такое unresolved external symbol?</p>\n","answer":"<p><strong>Ответ:</strong> Это ошибка этапа компоновки (в MSVC: <code>LNK2019</code>, в GNU/Clang: <code>undefined reference</code>), означающая, что компилятор на основе объявления оставил в объектном файле запрос на связывание внешнего символа, но ни в одном из переданных линкеру объектных файлов и библиотек этот символ так и не был найден в списке экспортируемых определений.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Ошибка линкера:\n# undefined reference to `Database::connect()'\n# error: ld returned 1 exit status\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/cpp/error-messages/tool-errors/linker-tools-error-lnk2019\">Microsoft Docs: Linker Tools Error LNK2019</a></p>\n","group":"Ниже среднего уровня","line":949},{"id":"content/01_cpp/14_macro_and_build.md:54654eb6d57e0171:1","question":"<p>Когда возникает ошибка линковки из-за отсутствующего определения?</p>\n","answer":"<p><strong>Ответ:</strong> Ошибка возникает, если: 1) не был скомпилирован или передан линкеру соответствующий <code>.cpp</code> файл; 2) забыли подключить нужную библиотеку (<code>-lmylib</code>); 3) не совпали сигнатуры в объявлении и определении (из-за чего сгенерировались разные mangled-имена); 4) метод класса объявлен, но его тело не написано; 5) шаблонный метод определен в <code>.cpp</code> без явной инстанциации.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">struct Logger {\n    static void log(); // Объявлено\n};\n// Забыли написать void Logger::log() { ... }\nint main() { Logger::log(); } // Вызовет unresolved external symbol\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/wiki/VerboseDiagnostics#undefined_reference\">GCC FAQs: Undefined references</a></p>\n","group":"Ниже среднего уровня","line":963},{"id":"content/01_cpp/14_macro_and_build.md:d578c98fe47d365c:1","question":"<p>Что такое multiple definition error?</p>\n","answer":"<p><strong>Ответ:</strong> Это ошибка компоновки (в GNU/Clang: <code>multiple definition of...</code>, в MSVC: <code>LNK2005</code>), означающая, что один и тот же не-inline символ со внешней компоновкой (external linkage) был скомпилирован и определен с одинаковым именем в двух или более различных объектных файлах, переданных линкеру.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Ошибка линкера:\n# main.o: multiple definition of `global_config'\n# config.o: first defined here\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://sourceware.org/binutils/docs/ld/Options.html\">GNU ld: Duplicate symbols</a></p>\n","group":"Ниже среднего уровня","line":979},{"id":"content/01_cpp/14_macro_and_build.md:72a4ad711045ee94:1","question":"<p>Почему multiple definition error часто связан с заголовками?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#include</code> выполняет копирование текста. Если в заголовочном файле находится определение обычной функции или переменной, и этот заголовок включен в два разных файла (<code>a.cpp</code> и <code>b.cpp</code>), компилятор продублирует тело функции в оба объектных файла (<code>a.o</code> и <code>b.o</code>). При сведении этих файлов вместе линкер обнаружит два одинаковых сильных символа и выдаст ошибку.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// common.h\nint counter = 0; // Определение! Включение common.h в два .cpp приведет к дублированию символа.\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines#sf2-do-not-put-data-definitions-in-header-files\">C++ Core Guidelines: SF.2 Do not put data definitions in header files</a></p>\n","group":"Ниже среднего уровня","line":993},{"id":"content/01_cpp/14_macro_and_build.md:14fb52bd6999ca09:1","question":"<p>Почему определение обычной функции в заголовке может быть проблемой?</p>\n","answer":"<p><strong>Ответ:</strong> По умолчанию функции имеют внешнюю компоновку (external linkage). Определение обычной (не <code>inline</code>, не <code>static</code>) функции в заголовке порождает «сильный» (strong) символ в каждом объектном файле, куда этот заголовок включен. На этапе линковки это нарушает правило ODR и приводит к конфликту имен между объектными модулями.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// math.h\nint square(int x) { return x * x; } // Ошибка при включении в &gt;= 2 файлов: duplicate symbol\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/storage_duration\">cppreference: Storage duration and linkage</a></p>\n","group":"Ниже среднего уровня","line":1006},{"id":"content/01_cpp/14_macro_and_build.md:a8f141747ce35d72:1","question":"<p>Когда функцию можно определять в заголовке?</p>\n","answer":"<p><strong>Ответ:</strong> Функцию можно безопасно определять в заголовке, если она: 1) помечена спецификатором <code>inline</code>; 2) определена прямо внутри объявления класса или структуры (неявно считается <code>inline</code>); 3) является шаблонной функцией (templates); 4) имеет внутреннюю компоновку (<code>static</code> или помещена в анонимное пространство имен, хотя последнее в заголовочных файлах не рекомендуется).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// header.h\ninline int max(int a, int b) { return a &gt; b ? a : b; } // Корректно\n\nstruct Calculator {\n    int get_zero() { return 0; } // Корректно (неявно inline)\n};\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/inline\">cppreference: inline specifier</a></p>\n","group":"Ниже среднего уровня","line":1019},{"id":"content/01_cpp/14_macro_and_build.md:3f3ede653d941a08:1","question":"<p>Что означает <code>inline</code> с точки зрения ODR и заголовков?</p>\n","answer":"<p><strong>Ответ:</strong> С точки зрения ODR и компоновщика, ключевое слово <code>inline</code> — это указание линкеру разрешить множественные идентичные определения этого символа в разных единицах трансляции. Компилятор помечает такой символ как «слабый» (weak symbol), а линкер при сборке просто отбрасывает дубликаты, оставляя ровно одну копию для всей программы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// utils.h\ninline void debug_print() { /*...*/ } // Разрешено включать в N файлов проекта, дубли сольются линкером\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/inline\">cppreference: Inline function linkage</a></p>\n","group":"Ниже среднего уровня","line":1036},{"id":"content/01_cpp/14_macro_and_build.md:75a050b758a61dac:1","question":"<p>Что такое ODR — One Definition Rule?</p>\n","answer":"<p><strong>Ответ:</strong> One Definition Rule (правило одного определения) — фундаментальный стандарт C++, гласящий: 1) в любой единице трансляции переменная, функция, класс, перечисление или шаблон не могут иметь более одного определения; 2) в рамках всей программы сущности со внешней компоновкой (не <code>inline</code>) должны иметь ровно одно определение; 3) <code>inline</code>-функции и типы могут определяться в разных единицах трансляции, но эти определения обязаны быть посимвольно/семантически идентичны.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Нарушение части 1 ODR (в пределах одной единицы трансляции):\nint val = 5;\nint val = 10; // Ошибка компиляции: redefinition of 'int val'\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/basic.def.odr\">ISO C++ Standard: One Definition Rule (§ 6.3 [basic.def.odr])</a></p>\n","group":"Ниже среднего уровня","line":1049},{"id":"content/01_cpp/14_macro_and_build.md:8be95a9ac5c33ca4:1","question":"<p>Почему ODR важен для больших C++-проектов?</p>\n","answer":"<p><strong>Ответ:</strong> В больших проектах без строгого соблюдения ODR возникают трудноуловимые ошибки неопределенного поведения (Undefined Behavior). Если структура данных или класс определены по-разному в разных единицах трансляции (например, из-за разных дефайнов сборки), то разные части бинарного файла будут работать с несовпадающими смещениями полей в памяти, что приводит к разрушению памяти (memory corruption) и крашам в рантайме.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// В файле A.cpp: struct Data { int x; int y; };\n// В файле B.cpp: struct Data { double x; };\n// Передача Data между ними приведет к UB из-за нарушения ODR!\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.sei.cmu.edu/confluence/display/cplusplus/DCL60-CPP.+Obey+the+one-definition+rule\">SEI CERT C++ Coding Standard: DCL60-CPP</a></p>\n","group":"Ниже среднего уровня","line":1063},{"id":"content/01_cpp/14_macro_and_build.md:044e444ecd13568e:1","question":"<p>Что считается нарушением ODR?</p>\n","answer":"<p><strong>Ответ:</strong> Нарушением ODR считается: дублирование не-<code>inline</code> определений функций или переменных в разных файлах; расхождение определений одного и того же класса/структуры в разных единицах трансляции (разный порядок членов, разные размеры, разные флаги компиляции типа <code>-DDEBUG</code>); наличие нескольких определений шаблона, если их токены или поиск имен различаются.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// file1.cpp\nstruct Config { int timeout; };\n\n// file2.cpp\nstruct Config { double timeout; }; // ODR violation! Линкер часто не сообщает об этом.\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/definition#One_Definition_Rule\">cppreference: ODR Violations</a></p>\n","group":"Ниже среднего уровня","line":1077},{"id":"content/01_cpp/14_macro_and_build.md:54a2df4198a34760:1","question":"<p>Почему include guard не решает все проблемы ODR?</p>\n","answer":"<p><strong>Ответ:</strong> Include guard (<code>#pragma once</code> или <code>#ifndef GUARDFLAG</code>) защищает только от повторного включения заголовочного файла внутри <strong>одной конкретной</strong> единицы трансляции (одного <code>.cpp</code>). Если заголовок с обычным определением функции будет подключен в два независимых файла (<code>file1.cpp</code> и <code>file2.cpp</code>), guard отработает в каждом из них изолированно, определение попадет в оба <code>.o</code> файла, и линкер завершится ошибкой множественного определения.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifndef FOO_H\n#define FOO_H\nvoid print() {} // Guard спасет от дублей внутри main.cpp,\n#endif          // но при включении в other.cpp возникнет ошибка линковки\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.learncpp.com/cpp-tutorial/header-guards/\">LearnCpp: Header guards</a></p>\n","group":"Ниже среднего уровня","line":1093},{"id":"content/01_cpp/14_macro_and_build.md:1a575b399a5edcbb:1","question":"<p>Что такое forward declaration?</p>\n","answer":"<p><strong>Ответ:</strong> Forward declaration (предварительное объявление) — это объявление идентификатора (класса, структуры, функции) без предоставления его полного описания и внутренней структуры. Для компилятора это делает тип так называемым «неполным» (incomplete type), позволяя оперировать указателями и ссылками на него без включения тяжелых файлов определений.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">class Engine; // Forward declaration класса\n\nclass Car {\n    Engine* engine_; // Указателю не требуется знание размера Engine\n};\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/type#Incomplete_type\">cppreference: Incomplete types</a></p>\n","group":"Ниже среднего уровня","line":1108},{"id":"content/01_cpp/14_macro_and_build.md:436b1a2b93b5699a:1","question":"<p>Когда достаточно forward declaration вместо <code>#include</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Forward declaration достаточно, когда объявляются: 1) указатели или ссылки на тип (<code>T*</code>, <code>T&amp;</code>); 2) аргументы или возвращаемые типы функций в их сигнатурах; 3) статические методы и поля, если компилятору в данной точке не требуется вычислять размер <code>sizeof(T)</code> или обращаться к внутренним полям и методам типа <code>T</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Header.h\nclass Worker; // Достаточно, #include &quot;Worker.h&quot; не нужен!\n\nvoid process(const Worker&amp; w);\nWorker* create_worker();\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://google.github.io/styleguide/cppguide.html#Forward_Declarations\">Google C++ Style Guide: Forward Declarations</a></p>\n","group":"Ниже среднего уровня","line":1124},{"id":"content/01_cpp/14_macro_and_build.md:6589a2a1079c4ed8:1","question":"<p>Почему forward declaration помогает уменьшать зависимости?</p>\n","answer":"<p><strong>Ответ:</strong> Замена <code>#include</code> на forward declaration предотвращает каскадное втягивание транзитивных заголовков. В результате размер препроцессированного файла уменьшается, компилятор парсит меньше кода, а изменение внутренней реализации класса не провоцирует перекомпиляцию тех файлов проекта, которые используют только интерфейсные указатели на этот класс.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Вместо подтягивания всего огромного Widget.h:\n// #include &quot;Widget.h&quot; (втягивает 50 вложенных хедеров)\nclass Widget; // Размер файла на этапе препроцессинга не раздувается\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gamesfromwithin.com/physical-structure-and-c-part-1\">Games from Within: Physical Structure and C++ (Noel Llopis)</a></p>\n","group":"Ниже среднего уровня","line":1140},{"id":"content/01_cpp/14_macro_and_build.md:444506bf5410608f:1","question":"<p>Когда forward declaration недостаточно?</p>\n","answer":"<p><strong>Ответ:</strong> Предварительного объявления недостаточно, когда компилятору необходимо знать полный тип (complete type): 1) создание экземпляра по значению (<code>T obj;</code>); 2) наследование от типа (<code>class Derived : public Base</code>); 3) вызов оператора <code>sizeof(T)</code> или <code>alignof(T)</code>; 4) обращение к членам и методам (<code>ptr-&gt;foo()</code>); 5) использование в контейнерах стандартной библиотеки (например, по значению <code>std::vector&lt;T&gt;</code> требует полного типа при инстанцировании ряда методов).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">class Worker;\n// Worker w; // Ошибка: incomplete type 'Worker'\n// sizeof(Worker); // Ошибка: invalid application of 'sizeof' to incomplete type\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/type#Incomplete_type\">cppreference: Incomplete type restrictions</a></p>\n","group":"Ниже среднего уровня","line":1154},{"id":"content/01_cpp/14_macro_and_build.md:a3d077f12d260435:1","question":"<p>Можно ли объявить класс вперёд и хранить его по значению?</p>\n","answer":"<p><strong>Ответ:</strong> Нет, нельзя. Чтобы выделить память под переменную или поле объекта на стеке, в статической памяти или внутри другого класса, компилятор обязан точно знать его размер в байтах и требования к выравниванию (alignment). Неполный тип этих данных не предоставляет.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">class Engine;\n\nstruct Rocket {\n    Engine engine; // Ошибка компиляции: field 'engine' has incomplete type 'Engine'\n};\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/basic.types.general\">ISO C++ Standard: Incomplete types (§ 6.8.1 [basic.types.general])</a></p>\n","group":"Ниже среднего уровня","line":1168},{"id":"content/01_cpp/14_macro_and_build.md:974ae4291de69a79:1","question":"<p>Почему для поля-объекта нужен полный тип?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор формирует память класса как непрерывную структуру со строгими смещениями (offsets) каждого поля. Чтобы вычислить общий размер родительского класса и определить смещение следующих за ним полей, компилятору критически необходим точный размер и выравнивание каждого вложенного поля-объекта, что возможно узнать только из полного определения.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#include &quot;Engine.h&quot; // Обязателен полный тип!\n\nstruct Car {\n    Engine e; // Компилятору нужно знать sizeof(Engine),\n    int speed; // чтобы знать, по какому смещению расположить 'speed'\n};\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/object\">cppreference: Objects and alignment</a></p>\n","group":"Ниже среднего уровня","line":1184},{"id":"content/01_cpp/14_macro_and_build.md:0d8c35008dec30d9:1","question":"<p>Почему для указателя или ссылки часто достаточно неполного типа?</p>\n","answer":"<p><strong>Ответ:</strong> На конкретной аппаратной платформе все объектные указатели и ссылки имеют фиксированный, заранее известный компилятору размер (обычно 8 байт на 64-битной архитектуре и 4 байта на 32-битной), вне зависимости от того, на объект какого типа и размера они указывают.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">class UnknownClass; // Incomplete type\n\nstruct Container {\n    UnknownClass* ptr; // Компилятор знает: размер ptr ровно 8 байт (на x86_64)\n};\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.stroustrup.com/\">Bjarne Stroustrup: The C++ Programming Language (Chapter: Pointers and Arrays)</a></p>\n","group":"Ниже среднего уровня","line":1201},{"id":"content/01_cpp/14_macro_and_build.md:45588d3d49e6879b:1","question":"<p>Что такое циклические зависимости между заголовками?</p>\n","answer":"<p><strong>Ответ:</strong> Циклическая зависимость (circular dependency) возникает, когда заголовочный файл <code>A.h</code> включает в себя <code>B.h</code>, а <code>B.h</code>, прямо или косвенно через цепочку других файлов, включает в себя <code>A.h</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// File: A.h\n#include &quot;B.h&quot;\nstruct A { B* b; };\n\n// File: B.h\n#include &quot;A.h&quot;\nstruct B { A* a; };\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.informit.com/store/large-scale-c-plus-plus-software-design-9780201633627\">Large-Scale C++ Software Design (John Lakos)</a></p>\n","group":"Ниже среднего уровня","line":1217},{"id":"content/01_cpp/14_macro_and_build.md:48b2abc9c2cbc53f:1","question":"<p>Почему взаимные <code>#include</code> между заголовками — плохая идея?</p>\n","answer":"<p><strong>Ответ:</strong> Из-за include guard один из заголовков неизбежно будет прочитан препроцессором до конца раньше другого, в результате чего второй заголовок попытается использовать типы первого еще до того, как они будут определены компилятором. Это приводит к загадочным ошибкам компиляции вроде «unknown type name» и сильнейшей связанности архитектуры (tight coupling).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// При разворачивании цикла A.h -&gt; B.h -&gt; A.h:\n// Guard файла A.h уже установлен, B.h не получит определение struct A\n// Ошибка: 'A' has not been declared inside B.h\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines#sf-source-files\">C++ Core Guidelines: Circular dependencies</a></p>\n","group":"Ниже среднего уровня","line":1235},{"id":"content/01_cpp/14_macro_and_build.md:7fa8f7595c2ca185:1","question":"<p>Как forward declaration помогает разрывать циклические зависимости?</p>\n","answer":"<p><strong>Ответ:</strong> Forward declaration позволяет избавиться от директивы <code>#include</code> в одном или обоих заголовочных файлах, заменяя её опережающим объявлением класса для указателей/ссылок. Полные определения переносятся в <code>.cpp</code> файлы, в которых цикличность включений уже не вызывает проблем со сборкой.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// A.h\nclass B; // Разорвали цикл: не включаем B.h\nstruct A {\n    B* b;\n};\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://google.github.io/styleguide/cppguide.html#Forward_Declarations\">Google C++ Style Guide: Forward Declarations</a></p>\n","group":"Ниже среднего уровня","line":1249},{"id":"content/01_cpp/14_macro_and_build.md:04440944877bd94d:1","question":"<p>Что такое транзитивное подключение заголовков?</p>\n","answer":"<p><strong>Ответ:</strong> Транзитивное подключение — это ситуация, при которой исходный файл <code>main.cpp</code> подключает <code>A.h</code>, а <code>A.h</code> внутри себя подключает <code>B.h</code>. В результате <code>main.cpp</code> косвенно (транзитивно) получает доступ ко всем объявлениям из <code>B.h</code>, даже если сам явно не писал <code>#include &quot;B.h&quot;</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Library.h включает &lt;string&gt;\n// User.cpp:\n#include &quot;Library.h&quot;\nstd::string text = &quot;Hello&quot;; // Работает &quot;случайно&quot; за счет транзитивности\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/include\">cppreference: Source file inclusion</a></p>\n","group":"Ниже среднего уровня","line":1265},{"id":"content/01_cpp/14_macro_and_build.md:c39f311a20d5b8e7:1","question":"<p>Почему “заголовок подключился случайно через другой заголовок” — опасная зависимость?</p>\n","answer":"<p><strong>Ответ:</strong> Это делает код хрупким. Если автор промежуточного заголовка (<code>A.h</code>) проведет рефакторинг и удалит ставший ненужным <code>#include &quot;B.h&quot;</code>, весь зависимый код (<code>User.cpp</code>), который неявно полагался на транзитивный заголовок, сломается с ошибками компиляции, хотя внешне логика <code>User.cpp</code> никак не менялась (проблема «include what you use»).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// В Library.h убрали #include &lt;string&gt;\n// User.cpp внезапно падает при сборке: error: 'string' in namespace 'std' does not name a type\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://include-what-you-use.org/\">Include What You Use (IWYU) Concept</a></p>\n","group":"Ниже среднего уровня","line":1280},{"id":"content/01_cpp/14_macro_and_build.md:ae2fbf6609c65f3e:1","question":"<p>Почему хороший заголовок должен быть самодостаточным?</p>\n","answer":"<p><strong>Ответ:</strong> Самодостаточный заголовок (self-contained header) включает в себя все зависимости, необходимые для его собственной успешной компиляции. Это гарантирует, что порядок подключения заголовков в <code>.cpp</code> файлах пользователей не будет иметь значения, и подключение заголовка в чистом файле никогда не приведет к ошибкам компиляции из-за отсутствующих типов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// MyHeader.h обязан сам включить &lt;string&gt;, если использует ее в интерфейсе:\n#pragma once\n#include &lt;string&gt; // Без этого файл несамодостаточен\n\nvoid greet(const std::string&amp; name);\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://google.github.io/styleguide/cppguide.html#Self_contained_Headers\">Google C++ Style Guide: Self-contained Headers</a></p>\n","group":"Ниже среднего уровня","line":1293},{"id":"content/01_cpp/14_macro_and_build.md:175137b7696b9fca:1","question":"<p>Что значит, что заголовок должен компилироваться сам по себе?</p>\n","answer":"<p><strong>Ответ:</strong> Это значит, что если создать изолированный тестовый файл <code>test.cpp</code>, состоящий всего из одной строчки <code>#include &quot;header.h&quot;</code>, он должен компилироваться компилятором без каких-либо ошибок. Заголовок не должен неявно требовать, чтобы перед ним обязательно был подключен какой-то другой файл (например, <code>&lt;windows.h&gt;</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Проверка самодостаточности заголовка:\necho '#include &quot;my_header.h&quot;' &gt; test.cpp &amp;&amp; g++ -c test.cpp\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines#sf11-make-header-files-self-contained\">C++ Core Guidelines: SF.11 Make header files self-contained</a></p>\n","group":"Ниже среднего уровня","line":1309},{"id":"content/01_cpp/14_macro_and_build.md:308a10e7b9104b51:1","question":"<p>Что такое минимизация зависимостей на уровне include?</p>\n","answer":"<p><strong>Ответ:</strong> Это практика проектирования C++-кода, направленная на сокращение количества директив <code>#include</code> внутри заголовочных файлов до строго необходимого минимума. Достигается за счет использования предварительных объявлений (forward declarations), выноса деталей реализации в <code>.cpp</code> файлы и применения идиомы PIMPL (Pointer to Implementation).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Pimpl идиома для скрытия зависимостей:\nclass NetworkClient {\n    struct Impl; // Все тяжелые сокеты спрятаны внутри Impl\n    Impl* pimpl;\n};\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/pimpl\">cppreference: PIMPL Idiom</a></p>\n","group":"Ниже среднего уровня","line":1322},{"id":"content/01_cpp/14_macro_and_build.md:4aaacf0a61569b1c:1","question":"<p>Почему слишком много <code>#include</code> замедляет сборку?</p>\n","answer":"<p><strong>Ответ:</strong> Препроцессор работает методом примитивной текстовой вставки. Включение одного тяжелого стандартного заголовка (например, <code>&lt;iostream&gt;</code> или <code>&lt;vector&gt;</code>) может развернуть исходный файл в десятки тысяч строк кода. Если 100 исходных файлов проекта подключают этот заголовок, компилятор вынужден парсить, строить AST и проверять типы для этих миллионов строк снова и снова для каждого <code>.cpp</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Файл из 3 строк:\n# #include &lt;iostream&gt;\n# int main() {}\n# После препроцессора (g++ -E) превращается в ~30 000 строк кода!\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gamesfromwithin.com/physical-structure-and-c-part-2\">Games from Within: Physical Structure and C++</a></p>\n","group":"Ниже среднего уровня","line":1338},{"id":"content/01_cpp/14_macro_and_build.md:c71cfd367a67d911:1","question":"<p>Что такое время компиляции проекта?</p>\n","answer":"<p><strong>Ответ:</strong> Время компиляции (Build Time / Compilation Time) — это суммарное физическое время, затрачиваемое инструментами сборки (препроцессором, компилятором, ассемблером и линкером) на преобразование дерева исходных файлов проекта в готовые исполняемые бинарные модули или библиотеки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Замер времени сборки проекта:\ntime cmake --build . --parallel 8\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://llvm.org/docs/BenchmarkingBuildTimes.html\">LLVM Docs: Benchmarking Build Times</a></p>\n","group":"Ниже среднего уровня","line":1353},{"id":"content/01_cpp/14_macro_and_build.md:639a593140f322e9:1","question":"<p>Почему изменение одного заголовка может вызвать перекомпиляцию множества файлов?</p>\n","answer":"<p><strong>Ответ:</strong> Системы сборки (Make, Ninja) отслеживают зависимости на основе графа включений. Если заголовочный файл меняется (изменяется timestamp файла), система сборки считает невалидными и отправляет на обязательную перекомпиляцию все без исключения единицы трансляции, которые прямо или транзитивно включают данный заголовок.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>      [Core.h] (изменен)\n      /      \\\n  [A.cpp]   [B.cpp]  -&gt; Оба файла перекомпилируются заново!\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.gnu.org/software/make/manual/make.html#How-Make-Works\">GNU Make Manual: How Make Figures Out Out-of-date Targets</a></p>\n","group":"Ниже среднего уровня","line":1366},{"id":"content/01_cpp/14_macro_and_build.md:a4258f77d8465866:1","question":"<p>Что такое dependency graph в сборке?</p>\n","answer":"<p><strong>Ответ:</strong> Dependency graph (граф зависимостей) — это ориентированный ациклический граф (DAG), узлами которого являются файлы (исходные, заголовочные, объектные, исполняемые), а ребрами — отношения зависимости между ними. Он определяет строгий частичный порядок шагов сборки и показывает, что необходимо пересобрать при изменении конкретного узла.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>main.cpp -&gt; main.o \\\n                     -&gt; app.exe (линковка зависит от main.o и math.o)\nmath.cpp -&gt; math.o /\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://ninja-build.org/manual.html\">Ninja Build System Manual: Concepts</a></p>\n","group":"Ниже среднего уровня","line":1380},{"id":"content/01_cpp/14_macro_and_build.md:16b7769fd4deb3c4:1","question":"<p>Что такое инкрементальная сборка?</p>\n","answer":"<p><strong>Ответ:</strong> Инкрементальная сборка — это механизм оптимизации системы сборки, при котором компилируются только те файлы, которые были изменены с момента предыдущей сборки (или зависят от изменившихся файлов), в то время как результаты неизмененных файлов повторно используются в готовом виде.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Изменили только user.cpp:\nninja\n# [1/2] Compiling user.cpp -&gt; user.o\n# [2/2] Linking app.exe (быстрая сборка без перекомпиляции остальных 100 файлов)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/manual/cmake-buildsystem.7.html\">CMake Documentation: Dependency Checking</a></p>\n","group":"Ниже среднего уровня","line":1394},{"id":"content/01_cpp/14_macro_and_build.md:daed6c38446559c4:1","question":"<p>Чем полная сборка отличается от инкрементальной?</p>\n","answer":"<p><strong>Ответ:</strong> При полной сборке (clean build) все промежуточные артефакты (кэши, <code>.o</code>, <code>.obj</code>, <code>.a</code>, бинарники) удаляются, и проект перекомпилируется абсолютно с нуля. При инкрементальной сборке система сравнивает метки времени (timestamps) или хеши файлов и компилирует строго дельту изменений, экономя время разработчика.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Полная сборка:\nmake clean &amp;&amp; make -j8\n# Инкрементальная сборка:\nmake -j8\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/visualstudio/ide/building-and-cleaning-projects-and-solutions-in-visual-studio\">Microsoft Docs: Building Projects in Visual Studio</a></p>\n","group":"Ниже среднего уровня","line":1409},{"id":"content/01_cpp/14_macro_and_build.md:20d3d161540e5c90:1","question":"<p>Почему инкрементальная сборка так важна в больших проектах?</p>\n","answer":"<p><strong>Ответ:</strong> В больших проектах (таких как Chromium, LLVM, игровые движки) полная сборка может занимать от 40 минут до нескольких часов даже на мощных многопоточных серверах. Быстрая инкрементальная сборка (за секунды) критически необходима для сохранения продуктивного цикла обратной связи разработчика (Edit-Compile-Run-Test).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Инкрементальная пересборка 1 файла в LLVM занимает ~5 секунд,\n# тогда как полная сборка проекта требует около 1 часа.\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://llvm.org/docs/GettingStarted.html\">LLVM Project: Getting Started with the LLVM System (Build Performance)</a></p>\n","group":"Ниже среднего уровня","line":1424},{"id":"content/01_cpp/14_macro_and_build.md:d8a5fb04484abcc6:1","question":"<p>Что такое флаг компилятора?</p>\n","answer":"<p><strong>Ответ:</strong> Флаг компилятора — это параметр или аргумент командной строки, передаваемый компилятору для изменения его режима работы: управления уровнем оптимизации, включения предупреждений, выбора стандарта языка, генерации отладочной информации или определения макросов препроцессора.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># -Wall, -Wextra (предупреждения), -O2 (оптимизация), -std=c++20 (стандарт)\ng++ -std=c++20 -O2 -Wall -Wextra main.cpp -o main\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/gcc/Option-Summary.html\">GCC Online Docs: Option Summary</a></p>\n","group":"Ниже среднего уровня","line":1437},{"id":"content/01_cpp/14_macro_and_build.md:b62ac1af0bae6fa6:1","question":"<p>Для чего нужны ключи командной строки компилятора?</p>\n","answer":"<p><strong>Ответ:</strong> Ключи командной строки компилятора служат интерфейсом настройки всего процесса трансляции. Они определяют пути поиска заголовочных файлов (<code>-I</code>) и библиотек (<code>-L</code>), целевую архитектуру процессора (<code>-march</code>), санитайзеры (<code>-fsanitize=address</code>), генерацию отладочных символов (<code>-g</code>) и параметры линкера, позволяя адаптировать сборку под разработку, тестирование или релиз.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Подключение директории хедеров (-I) и включение AddressSanitizer (-fsanitize=address):\ng++ -I./include -g -fsanitize=address main.cpp -o debug_app\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/UsersManual.html\">Clang Compiler User's Manual</a></p>\n","group":"Ниже среднего уровня","line":1450},{"id":"content/01_cpp/14_macro_and_build.md:c87537d0c0946e86:1","question":"<p>Что такое условная компиляция?</p>\n","answer":"<p><strong>Ответ:</strong> Условная компиляция — это механизм препроцессора, позволяющий включать или исключать фрагменты исходного кода из финального потока компиляции (единицы трансляции) на основе заданных условий: наличия объявленных макроопределений, значений целочисленных констант или проверки версий компилятора.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#if defined(_WIN32)\n    #include &lt;windows.h&gt;\n#elif defined(__linux__)\n    #include &lt;unistd.h&gt;\n#endif\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/conditional?utm_source=gemini\">cppreference: Conditional inclusion</a></p>\n","group":"Средний уровень","line":1466},{"id":"content/01_cpp/14_macro_and_build.md:344f352fbdb91909:1","question":"<p>Когда используют <code>#if</code>, <code>#ifdef</code>, <code>#ifndef</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директивы <code>#ifdef</code> и <code>#ifndef</code> используют для проверки факта объявления идентификатора препроцессора (часто в include guards или для включения платформозависимых фичей). Директива <code>#if</code> применяется при необходимости вычисления составных булевых или целочисленных выражений (<code>#if FOO &gt; 2 &amp;&amp; defined(BAR)</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifndef BUFFER_SIZE\n#define BUFFER_SIZE 1024\n#endif\n\n#if (BUFFER_SIZE * 2) &gt; 4096\n    #error &quot;Buffer is too large&quot;\n#endif\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/cpp/Conditionals.html?utm_source=gemini\">GCC Docs: Conditionals</a></p>\n","group":"Средний уровень","line":1482},{"id":"content/01_cpp/14_macro_and_build.md:5b4fda9c7ace9753:1","question":"<p>Что такое макрос-переключатель конфигурации?</p>\n","answer":"<p><strong>Ответ:</strong> Макрос-переключатель (feature toggle / configuration flag) — это препроцессорный идентификатор, значение или наличие которого передаётся через систему сборки или конфигурационный заголовок для изменения поведения, состава компилируемых компонентов или выбора платформенных API в проекте.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Передается через флаг компилятора: -DENABLE_LOGGING=1\n#if ENABLE_LOGGING\n    #define LOG(x) std::cout &lt;&lt; x &lt;&lt; '\\n'\n#else\n    #define LOG(x) ((void)0)\n#endif\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#es31-dont-use-macros-for-constants-or-functions\">C++ Core Guidelines: Preprocessor macros</a></p>\n","group":"Средний уровень","line":1500},{"id":"content/01_cpp/14_macro_and_build.md:3303441f0fb83207:1","question":"<p>Как с помощью препроцессора включать или отключать части кода?</p>\n","answer":"<p><strong>Ответ:</strong> Код помещается в ветви <code>#if ... #else ... #endif</code>. Те фрагменты, условие для которых вычислилось в ноль (false), препроцессор просто отсекает — компилятор не производит над ними лексический и синтаксический анализ, и они не попадают в таблицу символов и бинарный файл.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#if 0\n    // Этот код полностью игнорируется компилятором\n    temporary_disabled_function();\n#endif\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/conditional?utm_source=gemini\">cppreference: Conditional inclusion</a></p>\n","group":"Средний уровень","line":1517},{"id":"content/01_cpp/14_macro_and_build.md:1c899075150a6a72:1","question":"<p>Что такое debug/release-конфигурации сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Это стандартные наборы флагов сборки (build profiles), настроенные под конкретные цели: конфигурация Debug оптимизирована для простоты отладки и быстрой компиляции, а конфигурация Release — для минимизации размера и максимизации скорости выполнения конечной программы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Debug: генерация отладки (-g), без оптимизаций (-O0)\ng++ -g -O0 main.cpp -o app_debug\n\n# Release: макс. скорость (-O3), выключение проверок (-DNDEBUG)\ng++ -O3 -DNDEBUG main.cpp -o app_release\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?utm_source=gemini\">CMake Docs: CMAKE_BUILD_TYPE</a></p>\n","group":"Средний уровень","line":1532},{"id":"content/01_cpp/14_macro_and_build.md:5da5c35a5b8d547e:1","question":"<p>Чем debug-сборка обычно отличается от release-сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Debug-сборка компилируется без инлайнинга и переупорядочивания инструкций (<code>-O0</code>), содержит отладочную информацию (таблицы DWARF/PDB), оставляет включенными макросы <code>assert</code>, а также может включать дополнительные рантайм-проверки стандартной библиотеки (например, <code>_GLIBCXX_DEBUG</code>). Release-сборка компилируется с оптимизациями (<code>-O2</code> / <code>-O3</code>), вырезанными ассертами (<code>-DNDEBUG</code>) и часто с удалёнными символами отладки (<code>strip</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifdef NDEBUG\n    // Специфичный для Release быстрый путь без проверок\n#else\n    // Специфичная для Debug валидация внутренних структур данных\n#endif\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/visualstudio/debugger/how-to-set-debug-and-release-configurations?utm_source=gemini\">Microsoft Docs: Understanding Debug and Release configurations</a></p>\n","group":"Средний уровень","line":1548},{"id":"content/01_cpp/14_macro_and_build.md:192b6950246c7a1f:1","question":"<p>Что такое оптимизации компилятора?</p>\n","answer":"<p><strong>Ответ:</strong> Оптимизации компилятора — это автоматические трансформации промежуточного представления (IR) и машинного кода, сохраняющие наблюдаемое поведение программы (as-if rule), но направленные на ускорение работы, сокращение потребления памяти, уменьшение размера инструкций или энергопотребления процессора.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Исходный код:\nint calc() { return 2 * 4; }\n// После Constant Folding и Inlining компилятор просто подставит число 8:\n// mov eax, 8\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://llvm.org/docs/Passes.html?utm_source=gemini\">LLVM Project: Optimization Passes</a></p>\n","group":"Средний уровень","line":1564},{"id":"content/01_cpp/14_macro_and_build.md:aa9f9fc347655938:1","question":"<p>Почему поведение программы в debug и release может различаться?</p>\n","answer":"<p><strong>Ответ:</strong> В Release-сборке агрессивные оптимизации компилятора полагаются на предположение об отсутствии в коде Неопределенного Поведения (Undefined Behavior). Если в программе есть чтение неинициализированной памяти, выход за границы массива или гонки данных, компилятор может выкинуть целые ветки <code>if</code>, переупорядочить операции или изменить распределение переменных на регистрах, из-за чего скрытые баги проявляются только в Release.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">int get_val() {\n    int x; // Неинициализировано. В Debug стек часто забит нулями,\n    return x; // а в Release вернется произвольный мусор из регистра.\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.sei.cmu.edu/confluence/display/cplusplus/CC.+Undefined+Behavior?utm_source=gemini\">SEI CERT C++: Undefined Behavior</a></p>\n","group":"Средний уровень","line":1579},{"id":"content/01_cpp/14_macro_and_build.md:2168e7f4179ca17a:1","question":"<p>Что такое символы отладки?</p>\n","answer":"<p><strong>Ответ:</strong> Символы отладки (Debug Symbols / Debug Information) — это специальные метаданные (форматы DWARF на Linux, PDB на Windows), связывающие машинный код и смещения адресов в памяти с исходным кодом: номерами строк, именами исходных файлов, названиями локальных переменных и их типами.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Флаг компилятора для генерации символов отладки:\ng++ -g main.cpp -o app\n# Просмотр секций DWARF утилитой objdump:\nobjdump --dwarf=info app\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://dwarfstd.org/?utm_source=gemini\">DWARF Debugging Information Format Standard</a></p>\n","group":"Средний уровень","line":1594},{"id":"content/01_cpp/14_macro_and_build.md:69d5e1f7ac3278f9:1","question":"<p>Для чего нужна отладочная информация в бинарнике?</p>\n","answer":"<p><strong>Ответ:</strong> Она необходима отладчикам (GDB, LLDB, Visual Studio Debugger) и профилировщикам для построения читаемого стека вызовов (stack trace) при крашах, установки точек останова (breakpoints) по строкам кода, пошагового выполнения программы и отображения текущих значений локальных структур и переменных.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-text\"># Стек вызова с отладочной информацией:\n#0  main () at main.cpp:14\n# Стек вызова БЕЗ отладочной информации:\n#0  0x0000000000401126 in ?? ()\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://sourceware.org/gdb/current/onlinedocs/gdb.html/Symbols.html?utm_source=gemini\">GDB Documentation: Debugging Information</a></p>\n","group":"Средний уровень","line":1609},{"id":"content/01_cpp/14_macro_and_build.md:049758c97607d202:1","question":"<p>Что такое <code>NDEBUG</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>NDEBUG</code> (No Debug) — это стандартный макрос языка C и C++, определенный спецификацией ISO. Если этот макрос объявлен до включения заголовка <code>&lt;cassert&gt;</code> (или <code>&lt;assert.h&gt;</code>), он отключает работу макроса <code>assert</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Передача компилятору для выключения assert в релизе:\ng++ -DNDEBUG -O3 main.cpp -o main\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/cassert.syn?utm_source=gemini\">ISO C++ Standard: C debugging libraries (§ 19.3 [cassert.syn])</a></p>\n","group":"Средний уровень","line":1624},{"id":"content/01_cpp/14_macro_and_build.md:63c5b47afc9f0561:1","question":"<p>Как <code>NDEBUG</code> связан с <code>assert</code>?</p>\n","answer":"<p><strong>Ответ:</strong> В заголовке <code>&lt;cassert&gt;</code> заложена условная директива: если макрос <code>NDEBUG</code> объявлен, макрос <code>assert(expr)</code> разворачивается в пустоту <code>((void)0)</code>. Если <code>NDEBUG</code> не объявлен, <code>assert(expr)</code> генерирует проверку выражения и вызов аварийного завершения <code>abort()</code> при ложном результате.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifdef NDEBUG\n    #define assert(condition) ((void)0)\n#else\n    #define assert(condition) /* код печати ошибки и abort() */\n#endif\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/error/assert?utm_source=gemini\">cppreference: assert</a></p>\n","group":"Средний уровень","line":1637},{"id":"content/01_cpp/14_macro_and_build.md:1c7b899db8aa7ee8:1","question":"<p>Что делает макрос <code>assert</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Макрос <code>assert</code> оценивает булево выражение в рантайме. Если выражение ложно (<code>false</code>), он выводит в стандартный поток ошибок диагностическое сообщение (текст условия, имя файла <code>__FILE__</code>, номер строки <code>__LINE__</code> и имя функции) и прерывает выполнение программы системным вызовом <code>std::abort()</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#include &lt;cassert&gt;\n\nvoid divide(int a, int b) {\n    assert(b != 0); // Если b == 0, программа упадет с ассертом\n    int res = a / b;\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/error/assert?utm_source=gemini\">cppreference: assert</a></p>\n","group":"Средний уровень","line":1653},{"id":"content/01_cpp/14_macro_and_build.md:c2fb6b24654779a0:1","question":"<p>Почему <code>assert</code> относится и к препроцессору, и к сборке?</p>\n","answer":"<p><strong>Ответ:</strong> К препроцессору он относится потому, что реализован как макрос, который переопределяется при каждом включении <code>&lt;cassert&gt;</code> в зависимости от директивы <code>#define NDEBUG</code>. К сборке он относится потому, что управление его поведением во всем проекте обычно централизованно задается сборочной системой через флаг <code>-DNDEBUG</code> в зависимости от типа сборки (Debug/Release).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\"># CMake автоматически добавляет -DNDEBUG для конфигурации Release\ntarget_compile_definitions(my_target PRIVATE $&lt;$&lt;CONFIG:Release&gt;:NDEBUG&gt;)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/manual/cmake-generator-expressions.7.html?utm_source=gemini\">CMake Documentation: Generator Expressions</a></p>\n","group":"Средний уровень","line":1670},{"id":"content/01_cpp/14_macro_and_build.md:d8a2f6e67a58fcdd:1","question":"<p>Что такое predefined macros?</p>\n","answer":"<p><strong>Ответ:</strong> Predefined macros (предопределенные макросы) — это служебные макросы, которые компилятор автоматически предоставляет препроцессору без явного объявления со стороны разработчика. Они содержат информацию о текущем компиляторе, версии стандарта C++, дате сборки, операционной системе и целевой процессорной архитектуре.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#if __cplusplus &gt;= 202002L\n    // Код для стандарта C++20 и выше\n#endif\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini#Predefined_macros\">cppreference: Predefined preprocessor symbols</a></p>\n","group":"Средний уровень","line":1683},{"id":"content/01_cpp/14_macro_and_build.md:f9d7ecc590f5ddd6:1","question":"<p>Что делают <code>__FILE__</code>, <code>__LINE__</code>, <code>__DATE__</code>, <code>__TIME__</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Это стандартные макросы препроцессора, которые разворачиваются в: <code>__FILE__</code> — строковый литерал с путем к текущему компилируемому файлу; <code>__LINE__</code> — целое число, равное номеру текущей строки; <code>__DATE__</code> — дату препроцессинга (&quot;Mmm dd yyyy&quot;); <code>__TIME__</code> — время препроцессинга (&quot;hh:mm:ss&quot;).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">std::cout &lt;&lt; &quot;Compiled on: &quot; &lt;&lt; __DATE__ &lt;&lt; &quot; at &quot; &lt;&lt; __TIME__ &lt;&lt; '\\n';\nstd::cout &lt;&lt; &quot;Error at &quot; &lt;&lt; __FILE__ &lt;&lt; &quot;:&quot; &lt;&lt; __LINE__ &lt;&lt; '\\n';\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini#Predefined_macros\">cppreference: Predefined macros</a></p>\n","group":"Средний уровень","line":1697},{"id":"content/01_cpp/14_macro_and_build.md:f341218d2ec1da61:1","question":"<p>Для чего полезен <code>__func__</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Идентификатор <code>__func__</code> возвращает имя текущей функции в виде статического константного массива символов. Он активно используется в библиотеках логирования, профилирования и обработчиках ошибок для автоматической фиксации точки выполнения без ручного хардкода имени функции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">void process_order() {\n    std::cout &lt;&lt; &quot;Entering function: &quot; &lt;&lt; __func__ &lt;&lt; '\\n'; // Напечатает: process_order\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/function?utm_source=gemini#__func__\">cppreference: <strong>func</strong></a></p>\n","group":"Средний уровень","line":1710},{"id":"content/01_cpp/14_macro_and_build.md:9cc153d2f3170631:1","question":"<p>Чем <code>__func__</code> отличается от препроцессорных макросов?</p>\n","answer":"<p><strong>Ответ:</strong> В отличие от препроцессорных макросов (<code>__FILE__</code>, <code>__LINE__</code>), которые раскрываются до этапа синтаксического анализа, <code>__func__</code> — это не макрос, а неявно объявляемая компилятором локальная переменная (как если бы в начале каждой функции компилятор добавил <code>static const char __func__[] = &quot;имя_функции&quot;;</code>). Поэтому к нему нельзя применить операции конкатенации строк препроцессора.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Ошибка препроцессора:\n// const char* msg = &quot;In function: &quot; __func__; // НЕ сработает, так как __func__ не строковый литерал препроцессора\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/dcl.fct.def.general?utm_source=gemini\">ISO C++ Standard: Function definitions (§ 9.5.1 [dcl.fct.def.general])</a></p>\n","group":"Средний уровень","line":1724},{"id":"content/01_cpp/14_macro_and_build.md:17fdae5d8ef71c68:1","question":"<p>Что такое token pasting оператор <code>##</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Оператор <code>##</code> (склейка токенов) используется внутри тела макроса для слияния двух независимых синтаксических токенов в один новый идентификатор или литерал во время работы препроцессора.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define DECLARE_VAR(type, id) type var_##id\n\nDECLARE_VAR(int, counter); // Разворачивается в: int var_counter;\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini#Operator_##\">cppreference: Replacing text macros (## operator)</a></p>\n","group":"Средний уровень","line":1737},{"id":"content/01_cpp/14_macro_and_build.md:12f0d8d934213dbe:1","question":"<p>Что такое stringizing оператор <code>#</code> внутри макросов?</p>\n","answer":"<p><strong>Ответ:</strong> Оператор <code>#</code> (стрингификация / превращение в строку) ставится перед параметром макроса с параметрами и превращает переданный аргумент в строковый литерал, автоматически обрамляя его двойными кавычками и экранируя внутренние спецсимволы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define PRINT_EXPR(expr) std::cout &lt;&lt; #expr &lt;&lt; &quot; = &quot; &lt;&lt; (expr) &lt;&lt; '\\n'\n\nPRINT_EXPR(2 + 2); // Развернется в: std::cout &lt;&lt; &quot;2 + 2&quot; &lt;&lt; &quot; = &quot; &lt;&lt; (2 + 2) &lt;&lt; '\\n';\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/preprocessor/replace?utm_source=gemini#Operator_#\">cppreference: Replacing text macros (# operator)</a></p>\n","group":"Средний уровень","line":1751},{"id":"content/01_cpp/14_macro_and_build.md:61b455cf611161d2:1","question":"<p>Для чего используются <code>#</code> и <code>##</code> в макросах?</p>\n","answer":"<p><strong>Ответ:</strong> Они используются для шаблонной кодогенерации (boilerplate generation), создания таблиц связывания (X-Macros), генерации уникальных имен идентификаторов (например, по номеру строки или переданному суффиксу), а также для автоматического захвата исходного C++-кода в строковые логи и ассерты.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define CHECK(cond) if (!(cond)) log_failed(#cond, __FILE__, __LINE__)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/cpp/Concatenation.html?utm_source=gemini\">GCC Docs: Preprocessor Concatenation</a></p>\n","group":"Средний уровень","line":1765},{"id":"content/01_cpp/14_macro_and_build.md:46e2bd3558b9e4c8:1","question":"<p>Почему макросы с параметрами требуют особой осторожности со скобками?</p>\n","answer":"<p><strong>Ответ:</strong> Препроцессор не анализирует структуру выражений, а производит «слепую» подстановку текста. Если параметр макроса или всё тело макроса не обёрнуты в круглые скобки, приоритет окружающих операторов в точке подстановки может исказить математическую логику выражения.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define MULTIPLY(a, b) a * b\nint res = MULTIPLY(1 + 2, 3); // 1 + 2 * 3 = 7, а ожидалось (1 + 2) * 3 = 9!\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.sei.cmu.edu/confluence/display/c/PRE01-C.+Use+parentheses+within+macros+around+parameter+names?utm_source=gemini\">SEI CERT C++: PRE01-C. Use parentheses within macros around parameter names</a></p>\n","group":"Средний уровень","line":1777},{"id":"content/01_cpp/14_macro_and_build.md:baa8b3180b3b459d:1","question":"<p>Чем опасен макрос <code>#define SQR(x) x*x</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Этот макрос страдает сразу от двух проблем: отсутствия скобок вокруг аргументов (что ломает приоритет операций) и отсутствия скобок вокруг всего выражения, а также двойного вычисления аргумента, если в качестве <code>x</code> передается выражение с побочным эффектом.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define SQR(x) x*x\nint val = SQR(2 + 3); // 2 + 3 * 2 + 3 = 11 вместо 25!\nint res = 100 / SQR(2); // 100 / 2 * 2 = 100 вместо 25!\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.learncpp.com/cpp-tutorial/unnamed-and-inline-namespaces/?utm_source=gemini\">LearnCpp: Macro pitfalls</a></p>\n","group":"Средний уровень","line":1790},{"id":"content/01_cpp/14_macro_and_build.md:10ce2a41c8a0796e:1","question":"<p>Почему макросы могут давать неожиданный результат из-за приоритета операторов?</p>\n","answer":"<p><strong>Ответ:</strong> Поскольку подстановка токенов происходит до построения синтаксического дерева (AST), окружающие макрос операции (сложение, сдвиги, тернарный оператор) связываются с неэкранированными внутренними частями макроса в соответствии с глобальной таблицей приоритетов C++.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define ADD(a, b) a + b\nint res = ADD(1, 2) * ADD(3, 4); // 1 + 2 * 3 + 4 = 1 + 6 + 4 = 11, а не 3 * 7 = 21!\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#es31-dont-use-macros-for-constants-or-functions\">C++ Core Guidelines: ES.31</a></p>\n","group":"Средний уровень","line":1804},{"id":"content/01_cpp/14_macro_and_build.md:b4b1f582d5e1a1df:1","question":"<p>Почему у макросов могут быть побочные эффекты?</p>\n","answer":"<p><strong>Ответ:</strong> Если параметр используется в теле макроса дважды или более раз, переданное в качестве аргумента выражение будет физически продублировано препроцессором и вычислено в рантайме столько же раз, что приводит к повторному вызову функций или повторным модификациям переменных.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define MAX(a, b) ((a) &gt; (b) ? (a) : (b))\nint a = 5, b = 2;\nint m = MAX(a++, b); // a инкрементируется ДВАЖДЫ!\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.sei.cmu.edu/confluence/display/c/PRE31-C.+Avoid+side+effects+in+arguments+to+unsafe+macros?utm_source=gemini\">SEI CERT C++: PRE31-C. Avoid side effects in arguments to unsafe macros</a></p>\n","group":"Средний уровень","line":1817},{"id":"content/01_cpp/14_macro_and_build.md:71e5eecf65a9f088:1","question":"<p>Чем опасен макрос с аргументом вроде <code>i++</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Если макрос дублирует аргумент, <code>i++</code> выполнится несколько раз, что приведёт к искажению данных. Если же один и тот же параметр модифицируется в выражении без точек следования (sequence points / sequenced-before relationships), это влечет за собой Неопределенное Поведение (UB).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define SQUARE(x) ((x) * (x))\nint i = 2;\nint res = SQUARE(i++); // UB до C++17, неожиданное значение (i инкрементируется дважды)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/eval_order?utm_source=gemini\">cppreference: Order of evaluation</a></p>\n","group":"Средний уровень","line":1831},{"id":"content/01_cpp/14_macro_and_build.md:3c7db3312e9d9b98:1","question":"<p>Почему макрос не стоит использовать там, где лучше функция или шаблон?</p>\n","answer":"<p><strong>Ответ:</strong> Макросы не уважают пространства имен (<code>namespace</code>), не проверяют соответствие типов аргументов, игнорируют правила областей видимости (scope), не могут быть нормально отлажены в интерактивном дебаггере (нельзя зайти внутрь макроса по <code>F11</code>) и часто порождают труднопонимаемые ошибки компилятора.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Плохо:\n#define MIN(a, b) (((a) &lt; (b)) ? (a) : (b))\n\n// Идеально на C++:\ntemplate &lt;typename T&gt;\nconstexpr const T&amp; min(const T&amp; a, const T&amp; b) {\n    return (a &lt; b) ? a : b;\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#es32-use-const-constexpr-or-inline-functions-instead-of-macros\">C++ Core Guidelines: ES.32 Use const, constexpr, or inline functions instead of macros</a></p>\n","group":"Средний уровень","line":1845},{"id":"content/01_cpp/14_macro_and_build.md:3881bee0e2a83498:1","question":"<p>Когда макрос всё же уместен?</p>\n","answer":"<p><strong>Ответ:</strong> Макросы остаются незаменимыми в узких сценариях: 1) включение include guards; 2) условная компиляция и платформозависимый код (<code>#ifdef _WIN32</code>); 3) сбор диагностических метаданных компилятора (<code>__FILE__</code>, <code>__LINE__</code>); 4) генерация табличного кода (X-macros); 5) интеграция со сторонними библиотеками тестирования и рефлексии (Google Test: <code>TEST(Suite, Case)</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define ASSERT_NOT_NULL(ptr) \\\n    if (!(ptr)) throw std::runtime_error(std::string(&quot;Null pointer in &quot;) + __FILE__)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://google.github.io/styleguide/cppguide.html?utm_source=gemini#Preprocessor_Macros\">Google C++ Style Guide: Preprocessor Macros</a></p>\n","group":"Средний уровень","line":1864},{"id":"content/01_cpp/14_macro_and_build.md:0f9f51276e635365:1","question":"<p>Что такое include path?</p>\n","answer":"<p><strong>Ответ:</strong> Include path (путь поиска включений) — это список системных и пользовательских каталогов файловой системы, в которых препроцессор по очереди ищет файлы, указанные в директивах <code>#include &lt;...&gt;</code> или <code>#include &quot;...&quot;</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Добавление каталога в include path через флаг -I:\ng++ -I/usr/local/include/mylib -I./include main.cpp -o main\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/cpp/Search-Path.html?utm_source=gemini\">GCC Online Docs: Search Path</a></p>\n","group":"Средний уровень","line":1877},{"id":"content/01_cpp/14_macro_and_build.md:e2c67282d59071b2:1","question":"<p>Как компилятор ищет заголовочные файлы?</p>\n","answer":"<p><strong>Ответ:</strong> При <code>#include &quot;file.h&quot;</code> препроцессор сначала ищет файл в каталоге текущего компилируемого файла, а затем обходит каталоги из флагов <code>-I</code> и стандартные системные папки. При <code>#include &lt;file.h&gt;</code> поиск сразу начинается со списка путей, переданных флагу <code>-I</code>, и стандартных системных директорий.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#include &quot;my_local.h&quot; // Поиск: текущая папка -&gt; -I -&gt; системные пути\n#include &lt;vector&gt;     // Поиск: -I -&gt; системные пути (текущая папка пропускается)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/cpp/Include-Syntax.html?utm_source=gemini\">GCC Online Docs: Include Syntax</a></p>\n","group":"Средний уровень","line":1890},{"id":"content/01_cpp/14_macro_and_build.md:579f7ace7103045b:1","question":"<p>Что такое системные заголовки?</p>\n","answer":"<p><strong>Ответ:</strong> Системные заголовки — это файлы заголовочных интерфейсов, поставляемые вместе со стандартной библиотекой C++ (например, <code>&lt;iostream&gt;</code>, <code>&lt;memory&gt;</code>) или системными библиотеками ОС (POSIX, WinAPI). Компилятор обращается с ними особо: по умолчанию подавляет большинство варнингов и ускоряет парсинг.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Флаг -isystem добавляет путь с трактовкой заголовков как системных:\ng++ -isystem ./third_party/boost main.cpp -o main\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/cpp/System-Headers.html?utm_source=gemini\">GCC Online Docs: System Headers</a></p>\n","group":"Средний уровень","line":1903},{"id":"content/01_cpp/14_macro_and_build.md:82fd0f3d28695931:1","question":"<p>Что такое пользовательские заголовки?</p>\n","answer":"<p><strong>Ответ:</strong> Пользовательские заголовки — это файлы проекта или локальных библиотек (обычно с расширениями <code>.h</code>, <code>.hpp</code>), написанные разработчиками приложения. Для них компилятор применяет все включенные уровни предупреждений (<code>-Wall</code>, <code>-Wextra</code>, <code>-Werror</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#include &quot;controllers/user_controller.hpp&quot; // Пользовательский заголовок проекта\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.learncpp.com/cpp-tutorial/header-files/?utm_source=gemini\">LearnCpp: Header files</a></p>\n","group":"Средний уровень","line":1916},{"id":"content/01_cpp/14_macro_and_build.md:a230a83c7ef2be8f:1","question":"<p>Что такое precompiled headers?</p>\n","answer":"<p><strong>Ответ:</strong> Precompiled Headers (PCH) — это технология ускорения сборки, при которой группа редко изменяемых тяжелых заголовков (STL, Boost, API ОС) парсится и компилируется компилятором один раз в бинарный образ (дамп AST и таблицы символов с расширением <code>.gch</code> в GCC или <code>.pch</code> в MSVC).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-bash\"># Создание PCH в GCC:\ng++ -x c++-header pch.h -o pch.h.gch\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/PCHInternals.html?utm_source=gemini\">Clang Docs: Precompiled Headers</a></p>\n","group":"Средний уровень","line":1928},{"id":"content/01_cpp/14_macro_and_build.md:36ed28416b4813f8:1","question":"<p>Для чего используют precompiled headers?</p>\n","answer":"<p><strong>Ответ:</strong> PCH используют для устранения главного узкого места компиляции C++ — повторного парсинга сотен тысяч строк шаблонного кода стандартной библиотеки и сторонних SDK в каждой отдельной единице трансляции, сокращая общее время пересборки проекта на 30–70%.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// pch.h\n#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;map&gt;\n#include &lt;algorithm&gt;\n#include &lt;windows.h&gt;\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/cpp/build/creating-precompiled-header-files?utm_source=gemini\">Microsoft Docs: Precompiled Header Files</a></p>\n","group":"Средний уровень","line":1941},{"id":"content/01_cpp/14_macro_and_build.md:2fb9051795258155:1","question":"<p>Какие плюсы дают precompiled headers?</p>\n","answer":"<p><strong>Ответ:</strong> Главный плюс — резкое ускорение компиляции каждой единицы трансляции, так как компилятор загружает готовые структуры данных из оперативной памяти или файла вместо долгого лексического разбора, раскрытия макросов и шаблонного анализа.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\"># Включение PCH через современный CMake:\ntarget_precompile_headers(my_project PRIVATE pch.h)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/command/target_precompile_headers.html?utm_source=gemini\">CMake Docs: target_precompile_headers</a></p>\n","group":"Средний уровень","line":1958},{"id":"content/01_cpp/14_macro_and_build.md:45ad308bee5ba147:1","question":"<p>Какие недостатки или сложности есть у precompiled headers?</p>\n","answer":"<p><strong>Ответ:</strong> Недостатки: 1) при изменении самого PCH пересобирается весь проект; 2) тяжелый бинарный кэш PCH может занимать гигабайты дисковой памяти; 3) скрывает реальные зависимости исходных файлов («случайные зависимости»); 4) требует строгой идентичности ключей компиляции во всех <code>.cpp</code> файлах.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-text\"># Ошибка компилятора при расхождении флагов:\n# fatal error: file 'pch.h.gch' was not built with the same compiler options\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/gcc/Precompiled-Headers.html?utm_source=gemini\">GCC Online Docs: Precompiled Headers Caveats</a></p>\n","group":"Средний уровень","line":1971},{"id":"content/01_cpp/14_macro_and_build.md:2a240e657bc38d86:1","question":"<p>Что такое translation unit boundary?</p>\n","answer":"<p><strong>Ответ:</strong> Translation unit boundary (граница единицы трансляции) — это логический и физический барьер видимости между отдельными процессами компиляции <code>.cpp</code> файлов. Всё, что находится внутри единицы трансляции без явного экспорта через таблицу символов линкера, изолировано и недоступно другим <code>.cpp</code> файлам.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// a.cpp\nstatic int internal_data = 42; // Не выходит за границу TU 'a.cpp'\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/translation_phases?utm_source=gemini\">cppreference: Translation units</a></p>\n","group":"Средний уровень","line":1984},{"id":"content/01_cpp/14_macro_and_build.md:7873ed0387b438de:1","question":"<p>Почему внутренние детали <code>.cpp</code> не видны напрямую другим <code>.cpp</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор C++ работает по модели независимой компиляции: каждый <code>.cpp</code> файл компилируется изолированным процессом без состояния и памяти о существовании других файлов проекта. Единственное связующее звено между ними — это экспортируемые имена в таблице символов объектных файлов, сводимые линкером на более позднем этапе.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// file1.cpp\nvoid secret_impl() {}\n\n// file2.cpp\n// secret_impl(); // Ошибка: компилятор не знает о существовании функции без объявления\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.stroustrup.com/?utm_source=gemini\">Bjarne Stroustrup: Design and Evolution of C++ (Separate Compilation)</a></p>\n","group":"Средний уровень","line":1997},{"id":"content/01_cpp/14_macro_and_build.md:486c56e83c43ea8e:1","question":"<p>Что такое external linkage?</p>\n","answer":"<p><strong>Ответ:</strong> External linkage (внешняя компоновка) — это свойство идентификатора (функции или переменной), означающее, что его имя экспортируется в глобальную таблицу символов объектного файла, благодаря чему он может быть найден, связан и использован другими единицами трансляции программы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Обычная глобальная функция имеет external linkage:\nvoid global_action() {} // Символ 'global_action' доступен линкеру из любого .o файла\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/storage_duration?utm_source=gemini#Linkage\">cppreference: Storage duration and linkage</a></p>\n","group":"Средний уровень","line":2013},{"id":"content/01_cpp/14_macro_and_build.md:186fd33ed8e714f8:1","question":"<p>Что такое internal linkage?</p>\n","answer":"<p><strong>Ответ:</strong> Internal linkage (внутренняя компоновка) — это свойство идентификатора, означающее, что символ локализован строго внутри своей единицы трансляции. Компилятор помечает такой символ как локальный (Local) в таблице символов объектного файла, и линкер игнорирует его при попытках внешнего связывания.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Символ local_action виден ТОЛЬКО внутри этого .cpp файла:\nstatic void local_action() {}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/storage_duration?utm_source=gemini#Linkage\">cppreference: Storage duration and linkage</a></p>\n","group":"Средний уровень","line":2026},{"id":"content/01_cpp/14_macro_and_build.md:e00a4231f81d613c:1","question":"<p>Как <code>static</code> на уровне файла влияет на linkage?</p>\n","answer":"<p><strong>Ответ:</strong> Ключевое слово <code>static</code>, примененное к глобальной переменной или функции на уровне файла (file scope), принудительно меняет ее компоновку с внешней (external) на внутреннюю (internal). Это скрывает сущность от остальных единиц трансляции и предотвращает конфликты имен (ODR violations) при линковке.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// a.cpp\nstatic int id = 1; // Уникален для a.cpp\n// b.cpp\nstatic int id = 2; // Уникален для b.cpp, конфликта не будет\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/basic.link?utm_source=gemini\">ISO C++ Standard: Linkage (§ 6.6 [basic.link])</a></p>\n","group":"Средний уровень","line":2039},{"id":"content/01_cpp/14_macro_and_build.md:75d3eb90e2e52268:1","question":"<p>Что такое анонимное namespace и зачем его используют в <code>.cpp</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Анонимное (безымянное) namespace — это конструкция <code>namespace { ... }</code>, которая присваивает всем объявленным внутри неё типам, функциям и переменным внутреннюю компоновку (internal linkage), делая их доступными исключительно в пределах текущей единицы трансляции и предотвращая утечку внутренних деталей реализации в глобальный скоуп.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">namespace {\n    void helper_logic() { /* доступно только в текущем .cpp */ }\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/namespace?utm_source=gemini#Unnamed_namespaces\">cppreference: Namespaces (Unnamed namespaces)</a></p>\n","group":"Средний уровень","line":2054},{"id":"content/01_cpp/14_macro_and_build.md:7d9faf4b0457c031:1","question":"<p>Чем <code>static</code> на уровне файла отличается от анонимного namespace?</p>\n","answer":"<p><strong>Ответ:</strong> Спецификатор <code>static</code> может быть применен только к функциям и объектам (переменным), но не может скрывать определения пользовательских типов (классов, структур, перечислений). Анонимное пространство имен C++ является современным стандартом и способно скрывать абсолютно любые сущности, включая целые классы и шаблоны.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">namespace {\n    // Внутренний вспомогательный класс (через static так сделать нельзя):\n    struct LocalHelper {\n        int x;\n    };\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#sf21-dont-use-an-unnamed-namespace-in-a-header\">C++ Core Guidelines: SF.21 Don't use an unnamed namespace in a header</a></p>\n","group":"Средний уровень","line":2068},{"id":"content/01_cpp/14_macro_and_build.md:1a51603b3b97c6ad:1","question":"<p>Что такое name mangling?</p>\n","answer":"<p><strong>Ответ:</strong> Name mangling (декорирование имен) — это механизм компилятора C++, шифрующий имя функции, информацию о её пространстве имен, классе, а также типах всех принимаемых параметров в единую уникальную строку символов. Это позволяет линкеру различать перегруженные функции с одинаковым именем.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-text\">// Исходная функция C++:\nvoid print(int);\n// Превращается компилятором (Itanium ABI) в мангированное имя:\n_Z5printi\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://itanium-cxx-abi.github.io/cxx-abi/abi.html?utm_source=gemini#mangling\">Itanium C++ ABI: External Names (Mangler)</a></p>\n","group":"Средний уровень","line":2085},{"id":"content/01_cpp/14_macro_and_build.md:088d79878b8f8300:1","question":"<p>Почему в C++ имена функций и шаблонов кодируются сложнее, чем в C?</p>\n","answer":"<p><strong>Ответ:</strong> Язык C не поддерживает перегрузку функций, пространства имен, классы и шаблоны, поэтому символ в C практически совпадает с именем функции (<code>print</code> -&gt; <code>_print</code>). В C++ одновременно могут существовать функции <code>foo(int)</code> и <code>foo(double)</code> в разных неймспейсах, поэтому линкеру требуется уникальный текстовый идентификатор, содержащий всю сигнатуру.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-text\">namespace Math {\n    double square(double); // _ZN4Math6squareEd\n    int square(int);       // _ZN4Math6squareEi\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/cpp/build/reference/decorated-names?utm_source=gemini\">Microsoft Docs: Decorated Names</a></p>\n","group":"Средний уровень","line":2100},{"id":"content/01_cpp/14_macro_and_build.md:b40ac78004ab745b:1","question":"<p>Зачем нужен <code>extern &quot;C&quot;</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Конструкция <code>extern &quot;C&quot;</code> указывает компилятору C++ использовать правила связывания и соглашения о вызовах языка C (C linkage), полностью отключая name mangling для объявленных под ней функций, что позволяет линкеру связать их по простому, недекорированному имени.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">extern &quot;C&quot; {\n    void legacy_c_function(int a); // В таблице символов останется просто legacy_c_function\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/language_linkage?utm_source=gemini\">cppreference: Language linkage</a></p>\n","group":"Средний уровень","line":2115},{"id":"content/01_cpp/14_macro_and_build.md:1cd27a1c7ece4027:1","question":"<p>Когда используют <code>extern &quot;C&quot;</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Его используют в двух ключевых случаях: 1) при подключении C-библиотек к C++-проекту; 2) при создании динамических библиотек на C++, функции которых планируется экспортировать наружу для вызова из чистого C, Python (через <code>ctypes</code>/<code>CFFI</code>), C# (P/Invoke) или через системный <code>dlsym</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#ifdef __cplusplus\nextern &quot;C&quot; {\n#endif\n\nvoid my_api_call();\n\n#ifdef __cplusplus\n}\n#endif\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.learncpp.com/cpp-tutorial/language-linkage-and-calling-c-functions-from-c/?utm_source=gemini\">LearnCpp: Calling C code from C++</a></p>\n","group":"Средний уровень","line":2129},{"id":"content/01_cpp/14_macro_and_build.md:3538ebc9c290e05e:1","question":"<p>Почему <code>extern &quot;C&quot;</code> важен при взаимодействии с C-библиотеками?</p>\n","answer":"<p><strong>Ответ:</strong> Если подключить заголовок библиотеки на C в C++-файл без <code>extern &quot;C&quot;</code>, компилятор C++ сгенерирует для вызовов функций декорированные имена (mangled names). Однако скомпилированная C-библиотека экспортирует простые имена без декораций. В результате на этапе компоновки линкер выдаст ошибку <code>undefined reference</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-text\"># Без extern &quot;C&quot; линкер ищет _Z7executev вместо execute:\n# undefined reference to `_Z7executev'\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://docs.oracle.com/cd/E19205-01/819-5267/bkafd/index.html?utm_source=gemini\">Oracle Solaris Studio C++ User's Guide: Mixing C and C++</a></p>\n","group":"Средний уровень","line":2149},{"id":"content/01_cpp/14_macro_and_build.md:5efc7ed0bd5ec7b6:1","question":"<p>Что такое ABI compatibility?</p>\n","answer":"<p><strong>Ответ:</strong> ABI (Application Binary Interface) compatibility — это совместимость между скомпилированными бинарными модулями на низком машинном уровне. Она определяет идентичность: правил декорирования имен (name mangling), расположения полей структур в памяти (размеры и выравнивания), соглашений о вызовах (calling conventions), обработки исключений и устройства таблиц виртуальных методов (<code>vtable</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-text\">Если Библиотека А собрана с размером std::string в 32 байта (GCC 5+ C++11 ABI),\nа Программа Б с размером в 8 байт (старый CoW ABI), их вызовы приведут к крашу памяти.\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/libstdc++/manual/using_dual_abi.html?utm_source=gemini\">GCC Wiki: Dual C++11 ABI</a></p>\n","group":"Средний уровень","line":2162},{"id":"content/01_cpp/14_macro_and_build.md:301e68cf4bc9f4a3:1","question":"<p>Почему несовместимость ABI может ломать линковку или работу программы?</p>\n","answer":"<p><strong>Ответ:</strong> Несовместимость может проявляться на двух этапах. На этапе линковки: разные форматы декорирования имен сделают невозможным связывание символов (undefined reference). На этапе выполнения: несовпадение соглашений о вызове (повреждение стека), разные смещения полей в структурах или несовместимые таблицы виртуальных методов приведут к повреждению памяти, сбою в регистрах и Segmentation Fault.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Библиотека скомпилирована с: struct Packet { int id; double val; };\n// Приложение скомпилировано с: struct Packet { int id; float val; };\n// Передача Packet* приведет к чтению битого значения val в рантайме.\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://community.kde.org/Policies/Binary_Compatibility_Issues_With_C%2B%2B?utm_source=gemini\">KDE Community: Binary Compatibility Issues</a></p>\n","group":"Средний уровень","line":2175},{"id":"content/01_cpp/14_macro_and_build.md:88a1f265568ae5ca:1","question":"<p>Что такое статическая линковка?</p>\n","answer":"<p><strong>Ответ:</strong> Статическая линковка — это этап компоновки, при котором линкер извлекает необходимый объектный код из статических библиотек (<code>.a</code> / <code>.lib</code>) и физически копирует его непосредственно в итоговый исполняемый файл или динамическую библиотеку, разрешая все смещения адресов на этапе сборки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Линковка статической библиотеки libmath.a в бинарник:\ng++ main.o libmath.a -o app_static\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.iecc.com/linker/?utm_source=gemini\">Levine, J. R. &quot;Linkers and Loaders&quot;</a></p>\n","group":"Выше среднего уровня","line":2192},{"id":"content/01_cpp/14_macro_and_build.md:2665ab62032c104e:1","question":"<p>Что такое динамическая линковка?</p>\n","answer":"<p><strong>Ответ:</strong> Динамическая линковка — это механизм, при котором на этапе сборки в бинарник внедряется лишь таблица импорта (список имен требуемых библиотек и символов), а связывание машинного кода и разрешение фактических адресов откладывается до момента запуска программы или вызова функции с помощью загрузчика операционной системы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Динамическое связывание с libmath.so:\ng++ main.o -L. -lmath -o app_dynamic\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://man7.org/linux/man-pages/man8/ld.so.8.html?utm_source=gemini\">Linux man-pages: ld.so(8)</a></p>\n","group":"Выше среднего уровня","line":2206},{"id":"content/01_cpp/14_macro_and_build.md:538af5abfe05383b:1","question":"<p>Какие плюсы у статической линковки?</p>\n","answer":"<p><strong>Ответ:</strong> Основные преимущества: автономность и портабельность бинарника (отсутствие внешних зависимостей типа &quot;DLL Hell&quot;), отсутствие задержек на разрешение символов при запуске ОС, а также потенциальная возможность линкеру удалить неиспользуемый мёртвый код (Dead Code Stripping / LTO).</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Автономный бинарник для развертывания в минимальном Docker-контейнере:\ng++ -static main.cpp -o standalone_app\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/gcc/Link-Options.html?utm_source=gemini\">GCC Online Docs: Options for Linking</a></p>\n","group":"Выше среднего уровня","line":2220},{"id":"content/01_cpp/14_macro_and_build.md:4c338e7e14914d73:1","question":"<p>Какие минусы у статической линковки?</p>\n","answer":"<p><strong>Ответ:</strong> Минусы: существенное увеличение размера исполняемого файла на диске, дублирование одного и того же машинного кода в оперативной памяти при параллельном запуске нескольких приложений, а также необходимость полной перекомпиляции и перелинковки бинарника при любом исправлении ошибок или уязвимостей в библиотеке.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Если 10 процессов используют static libcrypto, в RAM будет 10 копий кода библиотеки.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://csapp.cs.cmu.edu/?utm_source=gemini\">Computer Systems: A Programmer's Perspective (Bryant, O'Hallaron)</a></p>\n","group":"Выше среднего уровня","line":2234},{"id":"content/01_cpp/14_macro_and_build.md:e62389c544e0b2b8:1","question":"<p>Какие плюсы у динамической линковки?</p>\n","answer":"<p><strong>Ответ:</strong> Экономия дискового пространства и оперативной памяти за счёт совместного использования страниц кода (<code>.text</code>) несколькими процессами, а также возможность обновлять библиотеки (патчи безопасности, багфиксы) без пересборки зависимых клиентских исполняемых файлов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Обновление /usr/lib/libssl.so автоматически применяется ко всем запущенным в будущем программам.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.akkadia.org/drepper/dsohowto.pdf?utm_source=gemini\">Ulrich Drepper: How To Write Shared Libraries</a></p>\n","group":"Выше среднего уровня","line":2247},{"id":"content/01_cpp/14_macro_and_build.md:e29617892bf4dd81:1","question":"<p>Какие минусы у динамической линковки?</p>\n","answer":"<p><strong>Ответ:</strong> Риск несовместимости версий библиотек (DLL Hell / ABI mismatch), увеличение времени старта процесса из-за работы runtime loader, накладные расходы на вызовы функций через таблицы GOT/PLT (indirection overhead), а также зависимость от окружения целевой машины.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Ошибка при отсутствии библиотеки на целевой машине:\n# ./app: error while loading shared libraries: libcustom.so: cannot open shared object file\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://developer.apple.com/library/archive/documentation/DeveloperTools/Conceptual/DynamicLibraries/?utm_source=gemini\">Apple Developer: Dynamic Library Programming Topics</a></p>\n","group":"Выше среднего уровня","line":2260},{"id":"content/01_cpp/14_macro_and_build.md:9bf2d14084b6ba17:1","question":"<p>Что такое runtime loader?</p>\n","answer":"<p><strong>Ответ:</strong> Runtime loader (динамический загрузчик/линкер: <code>ld.so</code> в Linux, <code>dyld</code> в macOS, часть подсистемы ядра в Windows) — это системная программа, которая при запуске процесса читает метаданные бинарника (секции <code>.dynamic</code>), находит нужные разделяемые библиотеки, отображает их в виртуальную память и разрешает адреса символов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Явный запуск программы через системный runtime loader в Linux:\n/lib64/ld-linux-x86-64.so.2 ./my_program\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://man7.org/linux/man-pages/man8/ld-linux.so.8.html?utm_source=gemini\">Linux man-pages: ld-linux.so(8)</a></p>\n","group":"Выше среднего уровня","line":2274},{"id":"content/01_cpp/14_macro_and_build.md:8b9df379a5556a38:1","question":"<p>Как программа находит динамические библиотеки при запуске?</p>\n","answer":"<p><strong>Ответ:</strong> Загрузчик ищет библиотеки в строго определенном порядке: 1) пути, прошитые в самом бинарнике (<code>DT_RPATH</code>, затем <code>DT_RUNPATH</code>); 2) переменные окружения (<code>LD_LIBRARY_PATH</code> в Linux, <code>PATH</code> в Windows); 3) системный кэш конфигурации (например, <code>/etc/ld.so.cache</code>); 4) системные пути по умолчанию (<code>/lib64</code>, <code>/usr/lib64</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Добавление RPATH прямо в ELF-бинарник при сборке:\ng++ main.o -Wl,-rpath,'$ORIGIN/libs' -L./libs -lmylib -o app\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.debian.org/RpathIssue?utm_source=gemini\">Debian Wiki: RpathIssue</a></p>\n","group":"Выше среднего уровня","line":2288},{"id":"content/01_cpp/14_macro_and_build.md:dd9479d0fdb31912:1","question":"<p>Почему проблема “не найдена DLL/so” относится уже к этапу запуска, а не компиляции?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор и статический линкер завершают свою работу успешно, если им предоставлен интерфейс и библиотека импорта (заглушка со списком символов). Однако само тело динамической библиотеки физически подключается в память только загрузчиком ОС в момент вызова <code>execve()</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Сборка прошла без ошибок:\ng++ main.cpp -lfoo -o app\n# Но при выполнении:\n./app # ./app: error while loading shared libraries: libfoo.so: No such file or directory\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/windows/win32/dlls/dynamic-link-library-search-order?utm_source=gemini\">Microsoft Docs: Dynamic-Link Library Search Order</a></p>\n","group":"Выше среднего уровня","line":2302},{"id":"content/01_cpp/14_macro_and_build.md:55c978fb133fc317:1","question":"<p>Что такое экспорт символов из библиотеки?</p>\n","answer":"<p><strong>Ответ:</strong> Экспорт символов — это явное помещение идентификаторов функций, классов или переменных в публичную таблицу динамических символов (<code>.dynsym</code> в ELF или Export Table в PE). Только экспортированные символы могут быть вызваны внешними исполняемыми файлами или модулями.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#if defined(_MSC_VER)\n    #define API_EXPORT __declspec(dllexport)\n#else\n    #define API_EXPORT __attribute__((visibility(&quot;default&quot;)))\n#endif\n\nAPI_EXPORT void calculate();\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/cpp/cpp/dllexport-dllimport?utm_source=gemini\">Microsoft Docs: dllexport, dllimport</a></p>\n","group":"Выше среднего уровня","line":2318},{"id":"content/01_cpp/14_macro_and_build.md:77e0c5d83a51036a:1","question":"<p>Почему не все символы библиотеки должны быть публичными?</p>\n","answer":"<p><strong>Ответ:</strong> Ограничение экспорта уменьшает размер таблицы символов библиотеки, ускоряет загрузку программы динамическим загрузчиком, позволяет компилятору агрессивнее оптимизировать внутренние функции (inlining) и защищает архитектуру от использования приватных деталей реализации сторонним кодом.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Внутренняя функция не должна быть видна наружу:\n__attribute__((visibility(&quot;hidden&quot;))) void internal_crypto_helper() {}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/wiki/Visibility?utm_source=gemini\">GCC Wiki: Visibility</a></p>\n","group":"Выше среднего уровня","line":2337},{"id":"content/01_cpp/14_macro_and_build.md:96d84d4cc50816fd:1","question":"<p>Что такое visibility символов?</p>\n","answer":"<p><strong>Ответ:</strong> Visibility (видимость) символа — это атрибут в формате ELF, определяющий, как символ связывается между модулями. Основные уровни: <code>default</code> (экспортируется наружу, доступен другим бинарникам) и <code>hidden</code> (не попадает в динамическую таблицу символов, используется строго внутри текущей библиотеки).</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Сборка библиотеки с сокрытием всех символов по умолчанию:\ng++ -fvisibility=hidden -shared mylib.cpp -o libmylib.so\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/gcc/Code-Gen-Options.html?utm_source=gemini#index-fvisibility\">GCC Online Docs: Code Gen Options (-fvisibility)</a></p>\n","group":"Выше среднего уровня","line":2351},{"id":"content/01_cpp/14_macro_and_build.md:00296ea7e92916e1:1","question":"<p>Почему контроль visibility важен для библиотек?</p>\n","answer":"<p><strong>Ответ:</strong> Он предотвращает случайные конфликты одинаковых внутренних имен между разными библиотеками (symbol collision), ускоряет время линковки и загрузки, а также гарантирует сохранение стабильного бинарного интерфейса (ABI), скрывая приватные структуры данных.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Без скрытия два плагина с функцией helper() могут вызвать конфликт:\n# plugin_a: helper()\n# plugin_b: helper() -&gt; может вызваться реализация из plugin_a!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.akkadia.org/drepper/dsohowto.pdf?utm_source=gemini\">Ulrich Drepper: How To Write Shared Libraries</a></p>\n","group":"Выше среднего уровня","line":2365},{"id":"content/01_cpp/14_macro_and_build.md:8dbd613d405c408d:1","question":"<p>Что такое header-only библиотека?</p>\n","answer":"<p><strong>Ответ:</strong> Header-only библиотека — это библиотека, состоящая исключительно из заголовочных файлов (<code>.h</code> / <code>.hpp</code>), содержащих полные определения всех классов, <code>inline</code>-функций и шаблонов. Она не требует предварительной сборки в бинарные файлы (<code>.a</code>, <code>.so</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Использование nlohmann/json:\n#include &lt;nlohmann/json.hpp&gt;\n// Никаких дополнительных флагов линковки не требуется: g++ main.cpp\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.boost.org/doc/libs/release/more/getting_started/?utm_source=gemini\">Boost Community: Header-only Libraries</a></p>\n","group":"Выше среднего уровня","line":2380},{"id":"content/01_cpp/14_macro_and_build.md:9f142919705c8288:1","question":"<p>Какие плюсы у header-only библиотек?</p>\n","answer":"<p><strong>Ответ:</strong> Тривиальная интеграция в проект (достаточно просто добавить путь к папке заголовков через <code>-I</code>), отсутствие проблем с несовместимостью ABI компиляторов и флагов линковки, а также возможность глубоких межпроцедурных оптимизаций и инлайнинга компилятором.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># В CMake интеграция сводится к:\ntarget_include_directories(my_app PRIVATE path/to/header_only_lib)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: Header-only libraries</a></p>\n","group":"Выше среднего уровня","line":2395},{"id":"content/01_cpp/14_macro_and_build.md:9464a0ec26c0d451:1","question":"<p>Какие минусы у header-only библиотек?</p>\n","answer":"<p><strong>Ответ:</strong> Драматическое увеличение времени компиляции (код парсится и оптимизируется заново в каждом <code>.cpp</code> файле), раздувание размера объектных файлов и потребления RAM компилятором, а также то, что изменение любой строчки в библиотеке провоцирует пересборку всего зависимого проекта.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Включение тяжелой библиотеки во все файлы проекта может увеличить сборку с 10 секунд до 5 минут.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gamesfromwithin.com/physical-structure-and-c-part-2?utm_source=gemini\">Games from Within: Physical Structure and C++</a></p>\n","group":"Выше среднего уровня","line":2409},{"id":"content/01_cpp/14_macro_and_build.md:8491e8b234c7a0ee:1","question":"<p>Почему шаблоны часто реализуют прямо в заголовках?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор C++ инстанциирует шаблоны по требованию. Чтобы сгенерировать конкретный машинный код для заданного типа аргумента (например, <code>MyTemplate&lt;int&gt;</code>), компилятору в текущей единице трансляции необходимо видеть полное определение шаблона, а не только его объявление.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// template.h\ntemplate &lt;typename T&gt;\nT add(T a, T b) { return a + b; } // Тело доступно для генерации кода\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/function_template?utm_source=gemini\">cppreference: Function templates</a></p>\n","group":"Выше среднего уровня","line":2422},{"id":"content/01_cpp/14_macro_and_build.md:aa16b1c112292c5b:1","question":"<p>Что такое explicit instantiation шаблона?</p>\n","answer":"<p><strong>Ответ:</strong> Explicit instantiation (явное инстанцирование) — это директива компилятору принудительно сгенерировать машинный код для конкретных шаблонных параметров в строго определённой единице трансляции, либо запретить повторное инстанцирование в текущем файле (<code>extern template</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Array.cpp - генерирует тело для int:\ntemplate class Array&lt;int&gt;;\n\n// Array.h - запрещает дублирование компиляции в других файлах:\nextern template class Array&lt;int&gt;;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/class_template?utm_source=gemini#Explicit_instantiation\">cppreference: Explicit template instantiation</a></p>\n","group":"Выше среднего уровня","line":2437},{"id":"content/01_cpp/14_macro_and_build.md:b5dd1d420a5345b2:1","question":"<p>Когда explicit instantiation помогает сократить время сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Когда тяжелый шаблон используется с фиксированным ограниченным набором типов во множестве файлов проекта. С помощью <code>extern template</code> шаблон компилируется ровно один раз в одном объектном файле, предотвращая дублирование тяжелой работы оптимизатора в других <code>.cpp</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// template_heavy.h\nextern template class Matrix&lt;double&gt;; // Не инстанцировать в каждом включившем файле\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/temp.explicit?utm_source=gemini\">ISO C++ Standard: Explicit instantiation (§ 13.9.3 [temp.explicit])</a></p>\n","group":"Выше среднего уровня","line":2454},{"id":"content/01_cpp/14_macro_and_build.md:526df5f9ef2e76a0:1","question":"<p>Что такое unity build?</p>\n","answer":"<p><strong>Ответ:</strong> Unity build (Jumbo build) — это техника ускорения сборки, при которой несколько десятков исходных <code>.cpp</code> файлов проекта программно объединяются (через <code>#include</code>) в один большой файл и передаются компилятору как единая огромная единица трансляции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// unity_all.cpp\n#include &quot;file1.cpp&quot;\n#include &quot;file2.cpp&quot;\n#include &quot;file3.cpp&quot;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/prop_tgt/UNITY_BUILD.html?utm_source=gemini\">CMake Docs: CMAKE_UNITY_BUILD</a></p>\n","group":"Выше среднего уровня","line":2468},{"id":"content/01_cpp/14_macro_and_build.md:58e9da0bda86a614:1","question":"<p>Какие плюсы даёт unity build?</p>\n","answer":"<p><strong>Ответ:</strong> Колоссальное ускорение полной сборки (за счёт того, что общие заголовки парсятся один раз вместо сотен повторений), снижение накладных расходов на запуск процессов компилятора и возможность сквозных оптимизаций между объединяемыми файлами.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># В CMake включается одной строкой:\nset_target_properties(my_target PROPERTIES UNITY_BUILD ON)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://chromium.googlesource.com/chromium/src/+/master/docs/jumbo.md?utm_source=gemini\">The Chromium Projects: Jumbo Builds</a></p>\n","group":"Выше среднего уровня","line":2484},{"id":"content/01_cpp/14_macro_and_build.md:bbd8553c3f849c23:1","question":"<p>Какие минусы и риски есть у unity build?</p>\n","answer":"<p><strong>Ответ:</strong> Риск коллизии внутренних имен (статические переменные или макросы с одинаковыми именами в разных <code>.cpp</code> начинают конфликтовать), замедление инкрементальной сборки (изменение одной строчки ведет к пересборке всего unity-пакета) и маскировка пропущенных <code>#include</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// file1.cpp: #define BUFFER 1024\n// file2.cpp: #define BUFFER 2048 -&gt; Ошибка: 'BUFFER' macro redefined при Unity-сборке\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://devblogs.microsoft.com/cppblog/support-for-unity-jumbo-files-in-visual-studio/?utm_source=gemini\">C++ Team Blog: Support for Unity (Jumbo) Files in Visual Studio</a></p>\n","group":"Выше среднего уровня","line":2498},{"id":"content/01_cpp/14_macro_and_build.md:4a3df6a5b911f65e:1","question":"<p>Что такое dependency scanning?</p>\n","answer":"<p><strong>Ответ:</strong> Dependency scanning — это процедура предварительного анализа исходных файлов сборочной системой или препроцессором компилятора (<code>gcc -M</code>) для построения точного списка всех заголовочных файлов, от которых прямо или косвенно зависит компилируемый <code>.cpp</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Генерация списка зависимостей в формате Makefile:\ng++ -M -MP main.cpp\n# Вывод: main.o: main.cpp header1.h header2.h ...\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/gcc/Preprocessor-Options.html?utm_source=gemini#index-M\">GCC Online Docs: Preprocessor Options (-M)</a></p>\n","group":"Выше среднего уровня","line":2512},{"id":"content/01_cpp/14_macro_and_build.md:9b0a9fbf6ac262f0:1","question":"<p>Почему сборочная система отслеживает зависимости между файлами?</p>\n","answer":"<p><strong>Ответ:</strong> Чтобы обеспечить корректность инкрементальной сборки. Если разработчик модифицирует заголовочный файл <code>types.h</code>, сборочная система сверяет граф зависимостей и пересобирает исключительно те <code>.cpp</code> файлы, которые этот заголовок используют, сохраняя остальные объектники нетронутыми.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>[types.h] (изменен) -&gt; система видит связь и компилирует: g++ -c user.cpp\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.gnu.org/software/make/manual/make.html?utm_source=gemini#Prerequisite-Types\">GNU Make Manual: Prereqs and Dependencies</a></p>\n","group":"Выше среднего уровня","line":2527},{"id":"content/01_cpp/14_macro_and_build.md:1babf4391bb38dd0:1","question":"<p>Что такое Makefile?</p>\n","answer":"<p><strong>Ответ:</strong> Makefile — это файл конфигурации сборки, читаемый утилитой <code>make</code>. Он содержит декларативный набор правил: целей (targets), их зависимостей (prerequisites) и команд оболочки (recipes), которые необходимо выполнить для создания целевого файла.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-makefile\">app: main.o utils.o\n\tg++ main.o utils.o -o app\n\nmain.o: main.cpp\n\tg++ -c main.cpp\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.gnu.org/software/make/manual/?utm_source=gemini\">GNU Make Reference Manual</a></p>\n","group":"Выше среднего уровня","line":2540},{"id":"content/01_cpp/14_macro_and_build.md:ac77c5892410dbaf:1","question":"<p>Что такое CMake?</p>\n","answer":"<p><strong>Ответ:</strong> CMake — это кроссплатформенный метагенератор сборочных файлов. Он не компилирует код напрямую, а читает конфигурационные файлы <code>CMakeLists.txt</code> и генерирует нативные скрипты сборки под конкретную систему: Makefiles, Ninja-файлы, проекты Visual Studio или Xcode.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\">cmake_minimum_required(VERSION 3.20)\nproject(MyApp)\nadd_executable(MyApp main.cpp)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/documentation/?utm_source=gemini\">Kitware: CMake Documentation</a></p>\n","group":"Выше среднего уровня","line":2556},{"id":"content/01_cpp/14_macro_and_build.md:c79482960606bdb3:1","question":"<p>Для чего нужны системы сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Они автоматизируют процесс превращения исходного кода в бинарные артефакты: оркеструют запуск компиляторов и линкеров в параллельном режиме, вычисляют минимально необходимый граф инкрементальной сборки, находят внешние библиотеки и управляют конфигурациями (Debug/Release).</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Запуск параллельной сборки на всех ядрах процессора:\ncmake --build build --parallel\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cliutils.gitlab.io/modern-cmake/?utm_source=gemini\">Modern CMake Guidelines</a></p>\n","group":"Выше среднего уровня","line":2570},{"id":"content/01_cpp/14_macro_and_build.md:139a56711824d810:1","question":"<p>Чем сборочная система отличается от компилятора?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор обрабатывает единичные файлы (транслирует <code>.cpp</code> в <code>.o</code> или <code>.exe</code>). Сборочная система управляет компилятором на уровне всего проекта: определяет порядок вызова, передает правильные флаги, следит за временными метками тысяч файлов и запускает параллельные процессы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Компилятор: g++ -c main.cpp (знает только о main.cpp)\n# Build system: Ninja (знает граф всего проекта из 5000 файлов)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.aosabook.org/en/cmake.html?utm_source=gemini\">The Architecture of Open Source Applications: CMake</a></p>\n","group":"Выше среднего уровня","line":2584},{"id":"content/01_cpp/14_macro_and_build.md:3ba4dbbac9584c5f:1","question":"<p>Что такое target в сборочной системе?</p>\n","answer":"<p><strong>Ответ:</strong> Target (цель) — это основной логический строительный блок сборочной системы, представляющий собой конечный артефакт (исполняемый бинарник, библиотека) или абстрактное действие (запуск тестов), имеющий собственные исходники, зависимости и флаги сборки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\">add_library(EngineCore STATIC engine.cpp) # EngineCore — это target\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/manual/cmake-buildsystem.7.html?utm_source=gemini#targets\">CMake Docs: Targets</a></p>\n","group":"Выше среднего уровня","line":2598},{"id":"content/01_cpp/14_macro_and_build.md:a4fdd8c949020f18:1","question":"<p>Что такое executable target?</p>\n","answer":"<p><strong>Ответ:</strong> Executable target — это цель сборочной системы, результатом компиляции и компоновки которой является полноценный запускаемый файл с точкой входа (<code>main</code>), предназначенный для выполнения операционной системой.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\">add_executable(GameClient main.cpp player.cpp)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/command/add_executable.html?utm_source=gemini\">CMake Docs: add_executable</a></p>\n","group":"Выше среднего уровня","line":2610},{"id":"content/01_cpp/14_macro_and_build.md:41008151629f236e:1","question":"<p>Что такое library target?</p>\n","answer":"<p><strong>Ответ:</strong> Library target — это цель сборочной системы, компилирующая набор исходников в статический архив (<code>STATIC</code>) или разделяемую библиотеку (<code>SHARED</code>), предназначенные для связывания с другими таргетами проекта.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\">add_library(NetworkLib SHARED socket.cpp packet.cpp)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/command/add_library.html?utm_source=gemini\">CMake Docs: add_library</a></p>\n","group":"Выше среднего уровня","line":2622},{"id":"content/01_cpp/14_macro_and_build.md:ce8804be20c052ce:1","question":"<p>Что такое include directories в CMake или другой системе сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Это пути в файловой системе, которые система сборки транслирует во флаги компилятора <code>-I</code> (или <code>/I</code> в MSVC), указывая компилятору директории, в которых следует искать заголовочные файлы при обработке директив <code>#include</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\">target_include_directories(MyApp PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/include)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/command/target_include_directories.html?utm_source=gemini\">CMake Docs: target_include_directories</a></p>\n","group":"Выше среднего уровня","line":2634},{"id":"content/01_cpp/14_macro_and_build.md:42a26ccce4b71568:1","question":"<p>Что такое compile definitions?</p>\n","answer":"<p><strong>Ответ:</strong> Compile definitions — это препроцессорные макросы, передаваемые компилятору через флаги сборочной системы (например, <code>-DDEBUG=1</code>), которые становятся глобально доступными в исходном коде без необходимости объявлять их через <code>#define</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\">target_compile_definitions(MyApp PRIVATE USE_VULKAN=1 ENABLE_ASSERTS)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/command/target_compile_definitions.html?utm_source=gemini\">CMake Docs: target_compile_definitions</a></p>\n","group":"Выше среднего уровня","line":2646},{"id":"content/01_cpp/14_macro_and_build.md:d3aaf40d9e1c3c6f:1","question":"<p>Что такое compile options?</p>\n","answer":"<p><strong>Ответ:</strong> Compile options — это параметры и ключи командной строки, которые передаются непосредственно компилятору при сборке конкретной цели (например, уровни оптимизаций, флаги стандартов или включения предупреждений).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\">target_compile_options(MyApp PRIVATE -Wall -Wextra -Wpedantic)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/command/target_compile_options.html?utm_source=gemini\">CMake Docs: target_compile_options</a></p>\n","group":"Выше среднего уровня","line":2658},{"id":"content/01_cpp/14_macro_and_build.md:db5bd3d5781b7903:1","question":"<p>Чем private/public/interface зависимости отличаются в современных системах сборки?</p>\n","answer":"<p><strong>Ответ:</strong> <code>PRIVATE</code> означает, что зависимость (инклуды, библиотеки, флаги) нужна только самому таргету для сборки; <code>INTERFACE</code> означает, что зависимость нужна только тем потребителям, которые линкуются с данным таргетом; <code>PUBLIC</code> объединяет оба сценария: зависимость нужна и таргету, и всем его потребителям.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\">target_link_libraries(Renderer\n    PUBLIC   Math3D      # Нужно и рендереру, и тем, кто вызывает рендерер\n    PRIVATE  VulkanSDK   # Детали реализации рендерера, наружу не торчат\n)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/manual/cmake-buildsystem.7.html?utm_source=gemini#transitive-usage-requirements\">CMake Docs: Transitive Usage Requirements</a></p>\n","group":"Выше среднего уровня","line":2670},{"id":"content/01_cpp/14_macro_and_build.md:4fe63afe38a9b401:1","question":"<p>Почему правильное описание зависимостей важно для масштабируемости проекта?</p>\n","answer":"<p><strong>Ответ:</strong> Точное разграничение зависимостей защищает от «транзитивного загрязнения»: потребители таргета не получают лишних путей инклудов и флагов компиляции. Это минимизирует объём парсинга, разрывает лишние связи в графе и предотвращает массовые пересборки при правках внутренних файлов библиотек.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>Если сделать зависимость PRIVATE вместо PUBLIC, изменение внутренней библиотеки не сломает клиентов.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://crascit.com/professional-cmake/?utm_source=gemini\">Professional CMake (Craig Scott)</a></p>\n","group":"Выше среднего уровня","line":2685},{"id":"content/01_cpp/14_macro_and_build.md:96cde6942e94f23e:1","question":"<p>Что такое генерация build files?</p>\n","answer":"<p><strong>Ответ:</strong> Это шаг работы мета-систем сборки (CMake, Meson), на котором высокоуровневое декларативное описание проекта транслируется в низкоуровневые императивные файлы (типа <code>build.ninja</code> или <code>Makefile</code>), специфичные для выбранной платформы и компилятора.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Генерация файлов сборки Ninja:\ncmake -B build -G Ninja\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/manual/cmake.1.html?utm_source=gemini\">Kitware: CMake Generation Step</a></p>\n","group":"Выше среднего уровня","line":2698},{"id":"content/01_cpp/14_macro_and_build.md:83ca50480e4a1586:1","question":"<p>Чем generator вроде CMake отличается от непосредственного build tool вроде Make/Ninja?</p>\n","answer":"<p><strong>Ответ:</strong> Генератор (CMake) абстрагирует операционную систему и конкретные утилиты, позволяя писать конфигурацию один раз. Build tool (Make, Ninja) ничего не знает о C++ на уровне концепций — он просто исполняет конкретные графы команд операционной системы для обновления файлов по временным меткам.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>[CMakeLists.txt] -&gt; CMake (генератор) -&gt; [build.ninja] -&gt; Ninja (build tool) -&gt; [app.exe]\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cliutils.gitlab.io/modern-cmake/?utm_source=gemini\">Modern CMake Architecture</a></p>\n","group":"Выше среднего уровня","line":2712},{"id":"content/01_cpp/14_macro_and_build.md:06315fd0135dbf08:1","question":"<p>Что такое Ninja?</p>\n","answer":"<p><strong>Ответ:</strong> Ninja — это компактная и максимально легковесная система сборки, спроектированная инженерами Google для максимальной скорости инкрементальной сборки огромных проектов (исходно для Chromium). Её входные файлы не пишутся людьми вручную, а генерируются CMake или GN.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Сборка проекта через Ninja:\nninja -C build\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://ninja-build.org/?utm_source=gemini\">The Ninja Build System</a></p>\n","group":"Выше среднего уровня","line":2725},{"id":"content/01_cpp/14_macro_and_build.md:3168e4fea8dcea96:1","question":"<p>Почему Ninja часто быстрее для инкрементальной сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Ninja предельно упрощена: в ней отсутствуют сложные условия, функции или циклы (синтаксический анализ минимален). Она читает плоский граф сборки за миллисекунды, мгновенно проверяет timestamps файлов с помощью системных вызовов и полностью параллелит задачи без лишних блокировок.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Проверка «не нужно ли что-то пересобрать» в Make может занимать 10 сек, а в Ninja — 0.05 сек.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://ninja-build.org/manual.html?utm_source=gemini#_comparison_to_make\">Ninja Design: Comparison with Make</a></p>\n","group":"Выше среднего уровня","line":2739},{"id":"content/01_cpp/14_macro_and_build.md:2daa030fa6df0959:1","question":"<p>Что такое cross-compilation?</p>\n","answer":"<p><strong>Ответ:</strong> Cross-compilation (кросс-компиляция) — это процесс сборки исполняемого кода или библиотек на одной вычислительной системе (host, например x86_64 Linux), предназначенных для запуска на совершенно другой платформе (target, например ARMv8 Android или Embedded RTOS).</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Компиляция под ARM на x86_64 хосте:\naarch64-linux-gnu-g++ main.cpp -o app_arm\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.debian.org/CrossCompiling?utm_source=gemini\">Debian Wiki: CrossCompiling</a></p>\n","group":"Выше среднего уровня","line":2752},{"id":"content/01_cpp/14_macro_and_build.md:3ac61ace4b89f225:1","question":"<p>Что значит собирать программу под другую платформу?</p>\n","answer":"<p><strong>Ответ:</strong> Это значит генерировать машинные инструкции другой архитектуры набора команд (ISA, например RISC-V вместо x86), использовать системные вызовы и структуры данных целевой операционной системы, а также компоновать код со стандартными библиотеками, собранными под эту целевую систему.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Сборка из Linux для Windows с помощью MinGW:\nx86_64-w64-mingw32-g++ main.cpp -o app.exe\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.mingw-w64.org/?utm_source=gemini\">MinGW-w64 Project</a></p>\n","group":"Выше среднего уровня","line":2766},{"id":"content/01_cpp/14_macro_and_build.md:e515f50c681ac3f9:1","question":"<p>Какие сложности есть у cross-compilation?</p>\n","answer":"<p><strong>Ответ:</strong> Невозможность запуска промежуточных скомпилированных утилит для кодогенерации на хосте; необходимость наличия полного дерева системных библиотек целевой ОС (sysroot); различия в порядке байт (endianness), размерах базовых типов и ABI; сложности с запуском тестов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Ошибка при попытке запустить сгенерированный утилитный бинарник на хосте:\n# ./code_generator: cannot execute binary file: Exec format error\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://llvm.org/docs/HowToCrossCompileLLVM.html?utm_source=gemini\">LLVM Cross-compilation Guide</a></p>\n","group":"Выше среднего уровня","line":2780},{"id":"content/01_cpp/14_macro_and_build.md:6b82205d6906025d:1","question":"<p>Что такое toolchain?</p>\n","answer":"<p><strong>Ответ:</strong> Toolchain (инструментальная цепочка) — это связанный набор программных инструментов разработки, работающих согласованно для сборки ПО: препроцессор, компилятор, ассемблер, линкер, стандартная библиотека C/C++ и утилиты работы с бинарниками (binutils).</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Toolchain GCC: cpp -&gt; cc1plus -&gt; as -&gt; collect2 (ld)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/?utm_source=gemini\">GCC Online Docs: Toolchains</a></p>\n","group":"Выше среднего уровня","line":2794},{"id":"content/01_cpp/14_macro_and_build.md:066577691fba1215:1","question":"<p>Что входит в toolchain для C++?</p>\n","answer":"<p><strong>Ответ:</strong> 1) Компилятор C++ (<code>g++</code>, <code>clang++</code>); 2) Ассемблер (<code>as</code>); 3) Компоновщик (<code>ld</code>, <code>lld</code>, <code>gold</code>); 4) Архиватор статических библиотек (<code>ar</code>); 5) Заголовочные файлы и бинарники стандартной библиотеки (<code>libstdc++</code>/<code>libc++</code>); 6) Набор binutils (<code>objdump</code>, <code>nm</code>, <code>strip</code>, <code>readelf</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Просмотр состава установленного кросс-тулчейна:\nls /usr/bin/aarch64-linux-gnu-*\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.gnu.org/software/binutils/?utm_source=gemini\">GNU Binutils Project</a></p>\n","group":"Выше среднего уровня","line":2807},{"id":"content/01_cpp/14_macro_and_build.md:afcf2040affff087:1","question":"<p>Что такое компилятор, линкер, архиватор как части toolchain?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор преобразует текст C++ в машинные инструкции объектного файла; архиватор (<code>ar</code>) упаковывает группу <code>.o</code> файлов в индексированный архив статической библиотеки (<code>.a</code>); линкер (<code>ld</code>) связывает разрозненные объектные файлы и библиотеки в единый монолитный бинарник с разрешением адресов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Цепочка:\ng++ -c a.cpp b.cpp          # Компилятор -&gt; a.o, b.o\nar rcs libtools.a a.o b.o   # Архиватор -&gt; libtools.a\ng++ main.o -L. -ltools -o app # Линкер -&gt; app\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://csapp.cs.cmu.edu/?utm_source=gemini\">CSAPP: Linking and Toolchain Stages</a></p>\n","group":"Выше среднего уровня","line":2821},{"id":"content/01_cpp/14_macro_and_build.md:d48892e15a5d9405:1","question":"<p>Что такое sysroot?</p>\n","answer":"<p><strong>Ответ:</strong> Sysroot (системный корень) — это изолированная директория на хост-машине, имитирующая корневую файловую систему целевой ОС (<code>/usr/include</code>, <code>/lib</code>, <code>/usr/lib</code>), в которой находятся необходимые для компиляции и линковки заголовочные файлы и динамические библиотеки целевой платформы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Сборка с указанием целевого системного корня:\nclang++ --target=aarch64-linux-gnu --sysroot=/opt/rpi-sysroot main.cpp\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/ClangCommandLineReference.html?utm_source=gemini#cmdoption-clang-sysroot\">Clang Docs: Sysroot option</a></p>\n","group":"Выше среднего уровня","line":2837},{"id":"content/01_cpp/14_macro_and_build.md:393197b6fe53e124:1","question":"<p>Почему системные заголовки и библиотеки цели важны при cross-compilation?</p>\n","answer":"<p><strong>Ответ:</strong> Заголовки хоста содержат объявления и макросы под архитектуру текущего ПК, а библиотеки хоста скомпилированы под его процессор. Попытка линковки с ними приведет либо к генерации несовместимых системных вызовов, либо к фатальной ошибке линкера о несовпадении архитектур.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Ошибка линкера при использовании библиотеки хоста:\n# aarch64-linux-gnu-ld: main.o: Relocations in generic ELF (EM: 62) - wrong architecture\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.debian.org/ToolChain/CrossPorters?utm_source=gemini\">Debian Cross-Toolchains</a></p>\n","group":"Выше среднего уровня","line":2851},{"id":"content/01_cpp/14_macro_and_build.md:098693cb6866c224:1","question":"<p>Что такое target triple?</p>\n","answer":"<p><strong>Ответ:</strong> Target triple — это каноническая строка формата <code>архитектура-поставщик-операционная_система-окружение</code> (например, <code>x86_64-pc-linux-gnu</code> или <code>armv7-unknown-linux-gnueabihf</code>), однозначно идентифицирующая целевую аппаратную и программную платформу для компилятора.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Вызов Clang с явным указанием target triple:\nclang++ -target x86_64-w64-mingw32 main.cpp\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/CrossCompilation.html?utm_source=gemini#target-triple\">Clang Docs: Target Triple</a></p>\n","group":"Выше среднего уровня","line":2865},{"id":"content/01_cpp/14_macro_and_build.md:4f7160526c63833a:1","question":"<p>Почему архитектура, ОС и ABI влияют на процесс сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Архитектура задает набор инструкций и регистров; ОС определяет формат исполняемого файла (ELF, PE, Mach-O) и API системных вызовов; ABI определяет порядок передачи аргументов через стек и регистры, правила манглинга и выравнивание типов в памяти. Их расхождение делает выполнение бинарника невозможным.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Порядок байт (Endianness):\n// Big-Endian сеть vs Little-Endian память процессора требует адаптации бинарных структур.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://refspecs.linuxfoundation.org/elf/gabi4+/contents.html?utm_source=gemini\">System V ABI Specification</a></p>\n","group":"Выше среднего уровня","line":2879},{"id":"content/01_cpp/14_macro_and_build.md:9670b4749dee1aa0:1","question":"<p>Что такое dependency hell в C++-проектах?</p>\n","answer":"<p><strong>Ответ:</strong> Dependency hell — это ситуация, при которой проект зависит от двух независимых библиотек (A и B), каждая из которых в свою очередь требует разные, взаимно несовместимые версии третьей библиотеки C (или стандартной библиотеки с разным ABI), что приводит к невозможности их совместной сборки или линковки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>      [Мой проект]\n      /          \\\n   [LibA]       [LibB]\n     |            |\n [LibC v1.0]  [LibC v2.0]  -&gt; Конфликт символов при линковке (duplicate symbol / ABI clash)!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://docs.conan.io/2/knowledge/guidelines.html?utm_source=gemini\">Modern C++ and Package Managers (Conan Docs)</a></p>\n","group":"Сложный уровень","line":2896},{"id":"content/01_cpp/14_macro_and_build.md:cae98f498b4ddbac:1","question":"<p>Почему неправильная структура заголовков резко ухудшает масштабируемость сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Из-за транзитивных включений компилятор вынужден многократно парсить избыточный код. Это приводит к комбинаторному взрыву времени трансляции (время сборки растёт квадратично от числа зависимостей), а любое изменение приватного члена класса вызывает лавинообразную перекомпиляцию десятков файлов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Включение &lt;windows.h&gt; в базовый header проекта может увеличить объем\n// препроцессинга каждого .cpp файла на 1.5 миллиона строк!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.informit.com/store/large-scale-c-plus-plus-software-design-9780201633627?utm_source=gemini\">Large-Scale C++ Software Design (John Lakos)</a></p>\n","group":"Сложный уровень","line":2913},{"id":"content/01_cpp/14_macro_and_build.md:c6067c41cd302e74:1","question":"<p>Что такое pImpl-идиома и как она помогает уменьшать зависимости при сборке?</p>\n","answer":"<p><strong>Ответ:</strong> Идиома pImpl (Pointer to Implementation) переносит приватные поля данных и приватные методы класса из заголовочного файла в структуру реализации, описанную в <code>.cpp</code>. В заголовке остается лишь предварительное объявление структуры и указатель (<code>std::unique_ptr&lt;Impl&gt;</code>), что полностью скрывает сторонние <code>#include</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Window.h\nclass Window {\npublic:\n    Window();\n    ~Window();\nprivate:\n    struct Impl; // Forward declaration\n    std::unique_ptr&lt;Impl&gt; pImpl;\n};\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/pimpl?utm_source=gemini\">cppreference: PIMPL idiom</a></p>\n","group":"Сложный уровень","line":2927},{"id":"content/01_cpp/14_macro_and_build.md:8cb652974fcd8b97:1","question":"<p>Почему pImpl может ускорять пересборку больших проектов?</p>\n","answer":"<p><strong>Ответ:</strong> Изменение внутренних структур данных или добавление приватных полей в <code>Window::Impl</code> требует перекомпиляции исключительно одного файла <code>Window.cpp</code>. Все остальные файлы проекта, подключающие <code>Window.h</code>, остаются незатронутыми и системой сборки не пересобираются.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Изменение типа приватного поля в Impl не меняет заголовок Window.h.\n// Ninja пересоберет ровно 1 файл: Window.cpp -&gt; Window.o.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://herbsutter.com/gotw/_100/?utm_source=gemini\">Herb Sutter: Pimpl For Compile-Time Encapsulation</a></p>\n","group":"Сложный уровень","line":2947},{"id":"content/01_cpp/14_macro_and_build.md:c64845932dedd2e2:1","question":"<p>Что такое модульность на уровне физической структуры проекта?</p>\n","answer":"<p><strong>Ответ:</strong> Это организация кодовой базы в виде слабо связанных физических компонентов (библиотек, подпроектов), где каждый компонент имеет четко очерченный публичный API-интерфейс и скрытую реализацию, исключая циклические физические связи на уровне каталогов и файлов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>engine/\n  ├── include/core/public_api.h   (Интерфейс)\n  └── src/internal_impl.cpp       (Скрытая реализация)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.informit.com/store/large-scale-c-plus-plus-volume-i-process-and-architecture-9780201717068?utm_source=gemini\">John Lakos: Large-Scale C++ Volume I</a></p>\n","group":"Сложный уровень","line":2961},{"id":"content/01_cpp/14_macro_and_build.md:ab1c212b207e2ec6:1","question":"<p>Чем физическая зависимость файлов отличается от логической зависимости типов?</p>\n","answer":"<p><strong>Ответ:</strong> Логическая зависимость означает, что концепт <code>A</code> оперирует концептом <code>B</code> (например, вызывает метод). Физическая зависимость возникает, когда файл <code>A.h</code> содержит директиву <code>#include &quot;B.h&quot;</code>, заставляя сборочную систему пересобирать <code>A</code> при любой модификации файла <code>B.h</code>, даже если логически интерфейс <code>B</code> не поменялся.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Логическая зависимость есть (Car знает о Engine),\n// но физической зависимости в Car.h нет, если используется forward declaration: class Engine;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gamesfromwithin.com/physical-structure-and-c-part-1?utm_source=gemini\">Physical Code Architecture (Games from Within)</a></p>\n","group":"Сложный уровень","line":2976},{"id":"content/01_cpp/14_macro_and_build.md:d6fd862bb3caa99b:1","question":"<p>Что такое transitive compile cost?</p>\n","answer":"<p><strong>Ответ:</strong> Это совокупная вычислительная стоимость (время CPU и память), которую проект платит за то, что заголовок <code>A.h</code> транзитивно тянет за собой заголовок <code>B.h</code>. Каждый файл, подключивший <code>A.h</code>, неявно парсит всё поддерево <code>B.h</code>, даже если сущности из <code>B.h</code> в нем не используются.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Измерение влияния заголовка через флаг Clang:\nclang++ -ftime-trace main.cpp # Показывает график времени парсинга всех транзитивных заголовков\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://aras-p.info/blog/2019/01/16/time-trace-timeline-flame-chart-profiler-for-Clang/?utm_source=gemini\">Clang Docs: Time Trace feature</a></p>\n","group":"Сложный уровень","line":2990},{"id":"content/01_cpp/14_macro_and_build.md:5296b359be65290e:1","question":"<p>Почему “удобный include сейчас” может стать дорогим позже?</p>\n","answer":"<p><strong>Ответ:</strong> Добавление удобного инклуда (например, универсального <code>Common.h</code> со всеми утилитами) быстро распространяется по проекту. С ростом кодовой базы это приводит к огромным деревьям зависимостей, замедлению компиляции в десятки раз и невозможности распутать архитектуру без тотального рефакторинга.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Включили &quot;Common.h&quot; в 500 файлов. Теперь изменение одной утилиты заставляет пересобирать весь проект.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#sf1-keep-declarations-in-header-files\">C++ Core Guidelines: SF.1 Keep declarations in header files</a></p>\n","group":"Сложный уровень","line":3004},{"id":"content/01_cpp/14_macro_and_build.md:ab06c5e7a03b6ce7:1","question":"<p>Что такое самодостаточный заголовок и почему это важное правило?</p>\n","answer":"<p><strong>Ответ:</strong> Это заголовочный файл, содержащий внутри себя все директивы <code>#include</code>, необходимые для его компиляции в изоляции. Это предотвращает скрытые зависимости от порядка включения файлов пользователями и гарантирует стабильность интерфейса.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// MathVector.h\n#pragma once\n#include &lt;cstddef&gt; // Без него size_t ниже не скомпилируется сам по себе\nvoid resize_vector(size_t new_size);\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://google.github.io/styleguide/cppguide.html?utm_source=gemini#Self_contained_Headers\">Google C++ Style Guide: Self-contained Headers</a></p>\n","group":"Сложный уровень","line":3017},{"id":"content/01_cpp/14_macro_and_build.md:73142b3fc9ae47a5:1","question":"<p>Почему заголовок не должен полагаться на то, что кто-то до него уже что-то подключил?</p>\n","answer":"<p><strong>Ответ:</strong> Если заголовок несамодостаточен, перестановка строк <code>#include</code> в клиентском коде приведет к неожиданной ошибке компиляции «тип не определен». Заголовок становится хрупким и непригодным для повторного использования в других частях системы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Если my_header.h требует std::string, но сам ее не включает:\n#include &quot;my_header.h&quot; // Ошибка: unknown type 'string'\n#include &lt;string&gt;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#sf11-make-header-files-self-contained\">C++ Core Guidelines: SF.11 Make header files self-contained</a></p>\n","group":"Сложный уровень","line":3032},{"id":"content/01_cpp/14_macro_and_build.md:58515e4a0269bacf:1","question":"<p>Что такое include-what-you-use принцип?</p>\n","answer":"<p><strong>Ответ:</strong> Принцип IWYU гласит: каждый файл исходного кода (<code>.h</code> или <code>.cpp</code>) обязан явно включать именно те заголовки, символы и типы которых он непосредственно использует в своем тексте, и не должен полагаться на транзитивные включения из других заголовков.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// main.cpp использует std::cout, значит ОБЯЗАН явно написать:\n#include &lt;iostream&gt;\n// Даже если &lt;iostream&gt; уже подключен где-то внутри &quot;Framework.h&quot;\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://include-what-you-use.org/?utm_source=gemini\">Include What You Use Project</a></p>\n","group":"Сложный уровень","line":3047},{"id":"content/01_cpp/14_macro_and_build.md:7a178220f0730893:1","question":"<p>Почему include-what-you-use улучшает качество сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Он защищает код от поломок при рефакторинге сторонних заголовков, делает граф зависимостей точным и честным, а также позволяет безопасно удалять ненужные устаревшие директивы <code>#include</code>, ускоряя сборку.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Автоматический аудит зависимостей утилитой IWYU:\ninclude-what-you-use main.cpp\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://github.com/include-what-you-use/include-what-you-use/blob/master/docs/WhyIWYU.md?utm_source=gemini\">IWYU Documentation: Why IWYU</a></p>\n","group":"Сложный уровень","line":3061},{"id":"content/01_cpp/14_macro_and_build.md:02d1592cd884c7b3:1","question":"<p>Когда forward declaration лучше <code>#include</code>, а когда хуже?</p>\n","answer":"<p><strong>Ответ:</strong> Лучше: в заголовочных файлах для указателей, ссылок и сигнатур функций — это сокращает размер трансляции. Хуже: когда тип активно используется в шаблонах (например, <code>std::unique_ptr</code> требует полного типа для деструктора), а также если класс переименовывается или превращается в <code>using alias</code> (сломает чужие forward declarations).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Ошибка с std::unique_ptr и incomplete type:\nclass Incomplete;\nstruct Holder {\n    std::unique_ptr&lt;Incomplete&gt; ptr; // Ошибка в месте вызова деструктора Holder!\n};\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.oreilly.com/library/view/effective-modern-c/9781491903988/?utm_source=gemini\">Effective Modern C++ (Scott Meyers, Item 22)</a></p>\n","group":"Сложный уровень","line":3075},{"id":"content/01_cpp/14_macro_and_build.md:2befef2b40604582:1","question":"<p>Почему чрезмерный forward declaration тоже может ухудшать читаемость?</p>\n","answer":"<p><strong>Ответ:</strong> Ручные опережающие объявления дублируют метаданные типов. Если класс переносится в другое пространство имен или меняет параметры шаблона по умолчанию, приходится вручную обновлять десятки forward declarations по всему проекту вместо единой точки правки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Если в библиотеке изменили: template&lt;typename T, typename Alloc = ...&gt; class Vector;\n// Ручной &quot;template&lt;typename T&gt; class Vector;&quot; поломает компиляцию.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://google.github.io/styleguide/cppguide.html?utm_source=gemini#Forward_Declarations\">Google C++ Style Guide: Forward Declarations Considerations</a></p>\n","group":"Сложный уровень","line":3091},{"id":"content/01_cpp/14_macro_and_build.md:2d4c69e9a09429d8:1","question":"<p>Что такое deterministic build?</p>\n","answer":"<p><strong>Ответ:</strong> Детерминированная сборка — это процесс сборки, при котором подача на вход компилятора абсолютно идентичного набора исходных файлов, флагов и зависимостей гарантированно порождает побитово идентичный исполняемый файл или библиотеку.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Проверка детерминизма двух независимых сборок:\nsha256sum build1/app.exe build2/app.exe\n# Хеши должны полностью совпадать\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://reproducible-builds.org/?utm_source=gemini\">Reproducible Builds Project</a></p>\n","group":"Сложный уровень","line":3105},{"id":"content/01_cpp/14_macro_and_build.md:30a5500ea14e9e32:1","question":"<p>Почему воспроизводимая сборка важна для CI/CD и релизов?</p>\n","answer":"<p><strong>Ответ:</strong> Она исключает внедрение вредоносных закладок на этапе компиляции (supply chain attacks), позволяет надежно проверять целостность бинарников независимыми валидаторами, гарантирует корректность работы распределенных кэшей сборки (ccache) и облегчает расследование багов в продакшене.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Если сборка недетерминирована, распределенный кэш (ccache/sccache) не будет находить попадания (cache misses).\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://reproducible-builds.org/docs/buy-in/?utm_source=gemini\">Reproducible Builds: Why does it matter?</a></p>\n","group":"Сложный уровень","line":3120},{"id":"content/01_cpp/14_macro_and_build.md:8598af553f96b3f7:1","question":"<p>Что такое reproducible build?</p>\n","answer":"<p><strong>Ответ:</strong> Синоним термина deterministic build — методология разработки, при которой любой разработчик или сторонний аудитор может взять исходный код с зафиксированными версиями инструментов и воспроизвести готовый бинарник байт-в-байт.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Проверка проекта утилитой diffoscope:\ndiffoscope build_a/bin build_b/bin\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://reproducible-builds.org/docs/definition/?utm_source=gemini\">Reproducible Builds: Definition</a></p>\n","group":"Сложный уровень","line":3133},{"id":"content/01_cpp/14_macro_and_build.md:9f0927a1148bad7d:1","question":"<p>Почему дата, время, пути и окружение могут нарушать воспроизводимость сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Макросы <code>__DATE__</code> и <code>__TIME__</code> вшивают время текущей компиляции; макрос <code>__FILE__</code> и символы отладки сохраняют абсолютные пути к исходникам на диске конкретного разработчика (<code>/home/user/...</code>); порядок обхода файлов файловой системой может менять порядок линковки объектников.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Флаг компилятора для маппинга путей к стабильному виду:\ng++ -ffile-prefix-map=/home/builder/project=. main.cpp\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/gcc/Debugging-Options.html?utm_source=gemini#index-ffile-prefix-map\">GCC Online Docs: Options for Debugging (-ffile-prefix-map)</a></p>\n","group":"Сложный уровень","line":3147},{"id":"content/01_cpp/14_macro_and_build.md:ce1de7f8ef65b307:1","question":"<p>Что такое <code>compile_commands.json</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>compile_commands.json</code> (JSON Compilation Database) — это стандартизированный файл, содержащий точный список команд компиляции для каждого файла проекта (рабочая директория, флаги, пути поиска включений, макроопределения).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;directory&quot;: &quot;/home/user/project/build&quot;,\n    &quot;command&quot;: &quot;/usr/bin/c++ -I../include -O2 -c ../src/main.cpp&quot;,\n    &quot;file&quot;: &quot;../src/main.cpp&quot;\n  }\n]\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/JSONCompilationDatabase.html?utm_source=gemini\">Clang Docs: JSON Compilation Database Format Specification</a></p>\n","group":"Сложный уровень","line":3161},{"id":"content/01_cpp/14_macro_and_build.md:8406736b1ace7e60:1","question":"<p>Для чего он нужен инструментам анализа?</p>\n","answer":"<p><strong>Ответ:</strong> Языковые серверы (Clangd), статические анализаторы (Clang-Tidy) и IDE используют его, чтобы видеть код ровно так, как его видит компилятор при реальной сборке (с теми же флагами и заголовками), обеспечивая точную навигацию по коду и обнаружение ошибок без догадок.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Генерация файла в CMake:\ncmake -B build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clangd.llvm.org/installation#compile_commandsjson?utm_source=gemini\">Clangd Documentation: Getting started</a></p>\n","group":"Сложный уровень","line":3179},{"id":"content/01_cpp/14_macro_and_build.md:6f4a7ff606c9fb52:1","question":"<p>Что такое статический анализатор и как он связан с этапом сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Статический анализатор (Clang-Tidy, Cppcheck, SonarQube) — это инструмент, который парсит исходный код и находит баги, утечки ресурсов и нарушения кодстайла без запуска программы. Он тесно интегрируется в конвейер сборки, перехватывая единицы трансляции с полным набором флагов компиляции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\"># Интеграция clang-tidy прямо в процесс сборки CMake:\nset(CMAKE_CXX_CLANG_TIDY &quot;clang-tidy;-checks=-*,modernize-*,bugprone-*&quot;)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/extra/clang-tidy/?utm_source=gemini\">Clang-Tidy Documentation</a></p>\n","group":"Сложный уровень","line":3193},{"id":"content/01_cpp/14_macro_and_build.md:3061281dd8ab7a04:1","question":"<p>Что такое санитайзеры и почему они обычно настраиваются через флаги сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Санитайзеры (ASan, TSan, UBSan) — это динамические инструменты инструментирования кода. Они требуют флагов компилятора (например, <code>-fsanitize=address</code>), потому что компилятор обязан внедрить в генерируемый машинный код проверки вокруг каждого обращения к памяти, а линкер — подключить специальный рантайм мониторинга.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Сборка с санитайзером памяти:\ng++ -fsanitize=address -g main.cpp -o app_asan\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://github.com/google/sanitizers?utm_source=gemini\">Google Sanitizers Repository</a></p>\n","group":"Сложный уровень","line":3206},{"id":"content/01_cpp/14_macro_and_build.md:277704e05c36fa81:1","question":"<p>Что такое ASan?</p>\n","answer":"<p><strong>Ответ:</strong> ASan (AddressSanitizer) — это инструмент обнаружения ошибок работы с памятью: выхода за границы массивов на стеке и в куче (buffer overflow), использования освобожденной памяти (use-after-free), двойного освобождения (double free) и утечек памяти.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">int main() {\n    int* arr = new int[5];\n    delete[] arr;\n    return arr[0]; // ASan мгновенно остановит процесс: Heap-use-after-free\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/AddressSanitizer.html?utm_source=gemini\">Clang Docs: AddressSanitizer</a></p>\n","group":"Сложный уровень","line":3220},{"id":"content/01_cpp/14_macro_and_build.md:cf7917911cfa6b12:1","question":"<p>Что такое UBSan?</p>\n","answer":"<p><strong>Ответ:</strong> UBSan (UndefinedBehaviorSanitizer) — это детектор неопределенного поведения во время выполнения программы: выявляет знаковое целочисленное переполнение, сдвиги на недопустимое число бит, разыменование нулевых указателей и нарушение правил выравнивания.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Компиляция с UBSan:\ng++ -fsanitize=undefined main.cpp -o app_ubsan\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/UndefinedBehaviorSanitizer.html?utm_source=gemini\">Clang Docs: UndefinedBehaviorSanitizer</a></p>\n","group":"Сложный уровень","line":3236},{"id":"content/01_cpp/14_macro_and_build.md:764dc08864cd9e90:1","question":"<p>Что такое TSan?</p>\n","answer":"<p><strong>Ответ:</strong> TSan (ThreadSanitizer) — это инструмент обнаружения состояний гонки по данным (data races) в многопоточных программах. Он отслеживает одновременный несинхронизированный доступ разных потоков к одним и тем же ячейкам памяти, если хотя бы один из них выполняет запись.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Компиляция с TSan:\ng++ -fsanitize=thread -g main.cpp -o app_tsan\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/ThreadSanitizer.html?utm_source=gemini\">Clang Docs: ThreadSanitizer</a></p>\n","group":"Сложный уровень","line":3250},{"id":"content/01_cpp/14_macro_and_build.md:f296e5077969f978:1","question":"<p>Почему разные конфигурации сборки используют разные наборы флагов?</p>\n","answer":"<p><strong>Ответ:</strong> Флаги решают взаимоисключающие инженерные задачи: сборка с санитайзерами замедляет программу в 2–3 раза и раздувает память; Release-оптимизации разрушают соответствие строк кода машинным инструкциям и удаляют проверки; Debug-режим компилируется быстрее, но непригоден для замеров производительности.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>Debug:   -O0 -g\nRelease: -O3 -DNDEBUG\nASan:    -O1 -g -fsanitize=address -fno-omit-frame-pointer\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/variable/CMAKE_CXX_FLAGS_DEBUG.html?utm_source=gemini\">CMake Docs: CMAKE_CXX_FLAGS_DEBUG / RELEASE</a></p>\n","group":"Сложный уровень","line":3264},{"id":"content/01_cpp/14_macro_and_build.md:a4a2b04fda76cc7d:1","question":"<p>Что такое LTO — Link Time Optimization?</p>\n","answer":"<p><strong>Ответ:</strong> LTO (Link Time Optimization, межпроцедурная оптимизация) — это механизм, при котором компилятор на этапе сборки генерирует в объектные файлы не готовый ассемблер, а свое промежуточное представление (IR), передавая его линкеру для сквозного глобального анализа и оптимизации программы целиком.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Включение LTO в GCC/Clang:\ng++ -flto -O3 main.cpp utils.cpp -o app\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/wiki/LinkTimeOptimization?utm_source=gemini\">GCC Wiki: Link Time Optimization</a></p>\n","group":"Сложный уровень","line":3279},{"id":"content/01_cpp/14_macro_and_build.md:b70e57aa3cb208b0:1","question":"<p>На каком этапе работает LTO?</p>\n","answer":"<p><strong>Ответ:</strong> LTO работает на этапе компоновки (linking). Линкер активирует плагин компилятора, объединяет промежуточные представления всех входных объектных модулей в единый граф и запускает сквозные проходы оптимизации перед финальной генерацией машинного кода.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>main.o (IR) + utils.o (IR) -&gt; [LTO Линкер: инлайнинг между .o] -&gt; app (ELF/PE)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://llvm.org/docs/LinkTimeOptimization.html?utm_source=gemini\">LLVM Docs: Link Time Optimization Design</a></p>\n","group":"Сложный уровень","line":3293},{"id":"content/01_cpp/14_macro_and_build.md:e977c62585350efb:1","question":"<p>Почему LTO размывает границу между компиляцией и линковкой?</p>\n","answer":"<p><strong>Ответ:</strong> Традиционно компилятор не видит соседние единицы трансляции, а линкер оперирует лишь готовыми «черными ящиками» машинных инструкций. При LTO реальная компиляция и кодогенерация (оптимизации, инлайнинг) откладываются и производятся самим линкером в глобальном скоупе.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Функция из utils.cpp может быть заинлайнена внутрь main.cpp линкером при LTO!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://hubicka.blogspot.com/2014/04/linktime-optimization-in-gcc-1-brief.html?utm_source=gemini\">Honza Hubicka's Blog: Link Time Optimization</a></p>\n","group":"Сложный уровень","line":3306},{"id":"content/01_cpp/14_macro_and_build.md:c6c86f7669edf94c:1","question":"<p>Какие плюсы даёт LTO?</p>\n","answer":"<p><strong>Ответ:</strong> Позволяет инлайнить вызовы функций между разными <code>.cpp</code> файлами, выполнять глобальный анализ указателей, девиртуализировать вызовы методов классов и удалять неиспользуемые функции и поля структур во всей программе, увеличивая скорость работы на 5–20% и уменьшая размер бинарника.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cmake\"># Включение LTO в CMake для таргеты:\nset_target_properties(my_target PROPERTIES INTERPROCEDURAL_OPTIMIZATION TRUE)\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://cmake.org/cmake/help/latest/prop_tgt/INTERPROCEDURAL_OPTIMIZATION.html?utm_source=gemini\">CMake Docs: INTERPROCEDURAL_OPTIMIZATION</a></p>\n","group":"Сложный уровень","line":3319},{"id":"content/01_cpp/14_macro_and_build.md:3569bfa17e36eced:1","question":"<p>Какие минусы у LTO?</p>\n","answer":"<p><strong>Ответ:</strong> Огромные затраты оперативной памяти и времени на этапе финальной линковки (может требовать десятков гигабайт RAM), существенное замедление инкрементальной сборки и усложнение сопоставления ассемблера с исходным кодом при отладке.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Линковка Chromium или LLVM с LTO может занимать более получаса на мощных серверах.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/ThinLTO.html?utm_source=gemini\">LLVM ThinLTO: Scalable and Incremental LTO</a></p>\n","group":"Сложный уровень","line":3332},{"id":"content/01_cpp/14_macro_and_build.md:a146969f9e13db2e:1","question":"<p>Что такое PGO — Profile Guided Optimization?</p>\n","answer":"<p><strong>Ответ:</strong> PGO — это методика оптимизации, при которой компилятор собирает специальную инструментированную версию программы, затем она прогоняется на реальных сценариях нагрузки для сбора профиля (частота ветвлений, горячие циклы), после чего бинарник компилируется заново с учетом этих данных.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Шаг 1: Сборка с генерацией профиля\ng++ -fprofile-generate -O3 main.cpp -o app_instrumented\n# Шаг 2: Прогон рабочей нагрузки -&gt; создается файл с профилем (.gcda)\n./app_instrumented\n# Шаг 3: Финальная сборка на основе профиля\ng++ -fprofile-use -O3 main.cpp -o app_optimized\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://gcc.gnu.org/onlinedocs/gcc/Optimize-Options.html?utm_source=gemini#index-fprofile-generate\">GCC Online Docs: Options That Control Optimization (-fprofile-generate)</a></p>\n","group":"Сложный уровень","line":3345},{"id":"content/01_cpp/14_macro_and_build.md:b092e61043d464c2:1","question":"<p>Как PGO использует данные выполнения для оптимизации сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор размещает наиболее вероятные ветки условий <code>if/else</code> линейно подряд в памяти (улучшая работу кэша инструкций и предсказателя переходов процессора), инлайнит строго горячие функции и выносит редкие пути (например, обработку ошибок) в дальние холодные секции памяти.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Если в 99.9% случаев условие false, компилятор при PGO вынесет тело if в холодную секцию .text:\nif (rare_error_condition) {\n    handle_error();\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/cpp/build/profile-guided-optimizations?utm_source=gemini\">Microsoft Docs: Profile-Guided Optimizations</a></p>\n","group":"Сложный уровень","line":3363},{"id":"content/01_cpp/14_macro_and_build.md:600cfb9db3663416:1","question":"<p>Почему PGO относится и к сборке, и к производительности?</p>\n","answer":"<p><strong>Ответ:</strong> К производительности оно относится потому, что дает максимальный выигрыш в скорости исполнения (до 15–30%), а к сборке — потому, что кардинально меняет CI/CD пайплайн, требуя двухэтапного цикла компиляции с обязательным шагом выполнения программы между ними.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>[Исходники] -&gt; Сборка 1 -&gt; [Бинарник с инструментацией] -&gt; Запуск нагрузки -&gt; [Профиль]\n                                                                                   ↓\n[Финальный бинарник] &lt;----------------------- Сборка 2 &lt;---------------------------\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://clang.llvm.org/docs/UsersManual.html?utm_source=gemini#profile-guided-optimization\">Clang Docs: Profile Guided Optimization</a></p>\n","group":"Сложный уровень","line":3379},{"id":"content/01_cpp/14_macro_and_build.md:1889193ac1a57ace:1","question":"<p>Что такое debug symbols stripping?</p>\n","answer":"<p><strong>Ответ:</strong> Debug symbols stripping (удаление отладочных символов) — это процесс очистки итогового бинарного файла от таблиц соответствия адресов строкам исходников (DWARF/PDB) и неэкспортируемых локальных символов с помощью системной утилиты <code>strip</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Удаление отладочной информации:\nstrip --strip-debug my_program\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://sourceware.org/binutils/docs/binutils/strip.html?utm_source=gemini\">GNU Binutils: strip manual</a></p>\n","group":"Сложный уровень","line":3394},{"id":"content/01_cpp/14_macro_and_build.md:d370517ee27ac025:1","question":"<p>Зачем удаляют символы из релизного бинарника?</p>\n","answer":"<p><strong>Ответ:</strong> Это уменьшает физический размер файла на диске в несколько раз (иногда на 80–90%), ускоряет загрузку программы с диска, экономит интернет-трафик при распространении дистрибутива и затрудняет обратную инженерию (реверс-инжиниринг) кода.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># До стриппинга:   app = 45 МБ\n# После стриппинга: app = 4.2 МБ\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://man7.org/linux/man-pages/man1/strip.1.html?utm_source=gemini\">Linux man-pages: strip(1)</a></p>\n","group":"Сложный уровень","line":3408},{"id":"content/01_cpp/14_macro_and_build.md:0c4f8217ef3212de:1","question":"<p>Что такое separate debug info?</p>\n","answer":"<p><strong>Ответ:</strong> Separate debug info (раздельные отладочные символы) — это технология, при которой отладочные секции выносятся из исполняемого файла в отдельный внешний файл (<code>.debug</code>, <code>.dSYM</code> или <code>.pdb</code>), связываемый с бинарником с помощью специального отпечатка сборки (Build ID).</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Извлечение символов в отдельный файл:\nobjcopy --only-keep-debug app app.debug\nstrip --strip-debug app\nobjcopy --add-gnu-debuglink=app.debug app\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://sourceware.org/gdb/current/onlinedocs/gdb.html/Separate-Debug-Files.html?utm_source=gemini\">GDB Documentation: Separate Debug Files</a></p>\n","group":"Сложный уровень","line":3422},{"id":"content/01_cpp/14_macro_and_build.md:d705158185914149:1","question":"<p>Почему бинарник и отладочная информация могут храниться отдельно?</p>\n","answer":"<p><strong>Ответ:</strong> Пользователь получает легковесный бинарник без лишнего веса, а разработчики и CI/CD серверы хранят внешние файлы отладки на Symbol Server. При получении краш-дампа от клиента отладчик автоматически подтягивает файл символов по его Build ID и восстанавливает читаемый стек вызовов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># В Windows: исполняемый файл app.exe отдается пользователю, а app.pdb остается на сервере.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://learn.microsoft.com/en-us/visualstudio/debugger/specify-symbol-dot-pdb-and-source-files-in-the-visual-studio-debugger?utm_source=gemini\">Microsoft Docs: Symbol files (PDBs)</a></p>\n","group":"Сложный уровень","line":3438},{"id":"content/01_cpp/14_macro_and_build.md:1447756fba6d7907:1","question":"<p>Что такое map file линковщика?</p>\n","answer":"<p><strong>Ответ:</strong> Map file (карта компоновки) — это генерируемый линкером текстовый отчет, подробно описывающий внутреннюю организацию собранного бинарника: точные адреса загрузки всех секций, их размеры, а также в каких секциях и из каких объектных файлов расположен каждый конкретный символ.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Заказ map-файла у GNU ld:\ng++ -Wl,-Map=output.map main.o utils.o -o output\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://sourceware.org/binutils/docs/ld/Options.html?utm_source=gemini#index-_002dMap_003d_002a\">GNU ld: -Map option</a></p>\n","group":"Сложный уровень","line":3451},{"id":"content/01_cpp/14_macro_and_build.md:94fe2f4c9a31b340:1","question":"<p>Для чего он бывает полезен?</p>\n","answer":"<p><strong>Ответ:</strong> Для низкоуровневой диагностики: выяснения причин раздувания бинарника (какая функция или массив заняли больше всего места), отладки переполнения памяти во встроенных системах (embedded ROM/RAM), а также поиска нежелательных включений мертвых символов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Фрагмент output.map:\n# .text.calculate  0x0000000000401140   0x3a utils.o\n#                  0x0000000000401140   calculate(int)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://embeddedartistry.com/blog/2019/04/08/demystifying-the-map-file/?utm_source=gemini\">Embedded Artistry: Demystifying the Linker Map File</a></p>\n","group":"Сложный уровень","line":3465},{"id":"content/01_cpp/14_macro_and_build.md:9d7c8880244cdf7b:1","question":"<p>Что такое weak symbol?</p>\n","answer":"<p><strong>Ответ:</strong> Weak symbol (слабый символ) — это символ в таблице объектного файла, помеченный специальной директивой или сгенерированный для <code>inline</code>/шаблонов, определение которого линкер может переопределить другим символом с таким же именем без выдачи ошибки компиляции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Объявление слабого символа в GCC:\n__attribute__((weak)) void default_handler() {\n    // Дефолтная реализация\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://docs.oracle.com/cd/E19683-01/817-1984/chapter2-4/index.html?utm_source=gemini\">Oracle Solaris Linker and Libraries Guide: Weak Symbols</a></p>\n","group":"Сложный уровень","line":3480},{"id":"content/01_cpp/14_macro_and_build.md:05a99e3bf10366e1:1","question":"<p>Что такое strong symbol?</p>\n","answer":"<p><strong>Ответ:</strong> Strong symbol (сильный символ) — это стандартный символ функции или инициализированной глобальной переменной с внешней компоновкой по умолчанию. Наличие двух сильных символов с одинаковым именем в разных объектных файлах гарантированно вызывает ошибку множественного определения (<code>multiple definition</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">int global_flag = 1; // Сильный символ\nvoid standard_function() {} // Сильный символ\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://csapp.cs.cmu.edu/?utm_source=gemini\">CSAPP: Global Symbols and Strong/Weak Rules</a></p>\n","group":"Сложный уровень","line":3495},{"id":"content/01_cpp/14_macro_and_build.md:866b2d6a7f819c4a:1","question":"<p>Как линкер разрешает конфликты символов?</p>\n","answer":"<p><strong>Ответ:</strong> Правила разрешения компоновщика: 1) Несколько сильных символов с одним именем недопустимы (ошибка линковки); 2) При наличии одного сильного и нескольких слабых символов выбирается сильный; 3) При наличии нескольких слабых символов линкер произвольно оставляет первый попавшийся, игнорируя остальные.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Библиотека объявила weak hook(), а вы в main.cpp написали strong hook():\n// Линкер выберет вашу реализацию и подставит ее на место всех вызовов.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eli.thegreenplace.net/2012/12/17/dumping-a-c-objects-memory-layout-with-clang?utm_source=gemini\">Eli Bendersky: Strong and weak symbols</a></p>\n","group":"Сложный уровень","line":3508},{"id":"content/01_cpp/14_macro_and_build.md:4e171ee0a40c9a55:1","question":"<p>Что такое startup code программы?</p>\n","answer":"<p><strong>Ответ:</strong> Startup code (стартовый код, например <code>crt0.o</code> / <code>crt1.o</code> — C Runtime Zero) — это небольшой низкоуровневый ассемблерный блок, управление на который передается загрузчиком операционной системы при создании процесса. Он выполняет первичную инициализацию рантайма перед вызовом функции <code>main()</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>Точка входа ELF-файла: _start (в crt1.o) -&gt; инициализация C-runtime -&gt; вызов main()\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.osdev.org/Creating_a_C_Library?utm_source=gemini\">OSDev Wiki: Creating a C Runtime Library</a></p>\n","group":"Сложный уровень","line":3522},{"id":"content/01_cpp/14_macro_and_build.md:f7376f24400024fb:1","question":"<p>Что происходит до входа в <code>main()</code>?</p>\n","answer":"<p><strong>Ответ:</strong> До вызова <code>main()</code>: 1) ОС настраивает виртуальную память, стек и переменные окружения; 2) Загрузчик разрешает динамические зависимости; 3) Startup-код инициализирует подсистему потоков и кучу (malloc); 4) Вызываются конструкторы глобальных и статических объектов C++ в секциях <code>.init_array</code> / <code>.ctors</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">struct BeforeMain {\n    BeforeMain() { std::cout &lt;&lt; &quot;I run before main!\\n&quot;; }\n};\nBeforeMain g_obj; // Конструктор сработает до входа в main()\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/initialization?utm_source=gemini#Non-local_variables\">cppreference: Initialization of non-local variables</a></p>\n","group":"Сложный уровень","line":3535},{"id":"content/01_cpp/14_macro_and_build.md:3ee59f36bd57d3ea:1","question":"<p>Что происходит после выхода из <code>main()</code>?</p>\n","answer":"<p><strong>Ответ:</strong> После завершения <code>main()</code> (или вызова <code>exit()</code>): 1) В порядке, обратном конструированию, вызываются деструкторы глобальных и статических объектов (зарегистрированные через <code>__cxa_atexit</code>); 2) Выполняются функции, переданные в <code>std::atexit()</code>; 3) Закрываются и сбрасываются стандартные буферы потоков ввода-вывода; 4) Вызывается системный вызов завершения процесса (<code>sys_exit</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">void on_exit_handler() { std::cout &lt;&lt; &quot;Clean up!\\n&quot;; }\n// Регистрация в main:\nstd::atexit(on_exit_handler); // Вызовется после выхода из main\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/utility/program/exit?utm_source=gemini\">cppreference: std::exit</a></p>\n","group":"Сложный уровень","line":3550},{"id":"content/01_cpp/14_macro_and_build.md:33aea2cc8f4be995:1","question":"<p>Почему глобальная инициализация относится к теме сборки и линковки?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор и линкер помещают указатели на функции инициализации глобальных объектов в специальные секции бинарного файла (<code>.init_array</code> в ELF или <code>.CRT$XCU</code> в PE). Именно компоновщик собирает эти указатели из всех объектных файлов в единый непрерывный массив, по которому в рантайме проходит цикл startup-кода.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Просмотр массива глобальных инициализаторов:\nreadelf -S app | grep init_array\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://refspecs.linuxfoundation.org/elf/gabi4+/ch5.init.html?utm_source=gemini\">System V ABI: Initialization and Termination Functions</a></p>\n","group":"Сложный уровень","line":3564},{"id":"content/01_cpp/14_macro_and_build.md:c59882c5e294c84e:1","question":"<p>Что такое static initialization order fiasco?</p>\n","answer":"<p><strong>Ответ:</strong> Это фундаментальная проблема C++, заключающаяся в том, что порядок вызова конструкторов глобальных (статических) объектов, расположенных в <strong>разных</strong> единицах трансляции, стандартом языка не определен. Если глобальный объект <code>A</code> из файла <code>a.cpp</code> в своем конструкторе обращается к глобальному объекту <code>B</code> из файла <code>b.cpp</code>, объект <code>B</code> может оказаться еще не инициализированным.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// a.cpp\nextern int count;\nint val = count + 1; // Если count еще не сконструирован, тут будет мусор или 0!\n\n// b.cpp\nint count = 42;\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.org/wiki/faq/ctors#static-init-order?utm_source=gemini\">ISO C++ FAQ: Static initialization order fiasco</a></p>\n","group":"Сложный уровень","line":3578},{"id":"content/01_cpp/14_macro_and_build.md:ea4a3548d1617ca9:1","question":"<p>Почему глобальные объекты из разных единиц трансляции могут инициализироваться в опасном порядке?</p>\n","answer":"<p><strong>Ответ:</strong> Стандарт C++ гарантирует порядок инициализации только внутри одной единицы трансляции (сверху вниз). Между разными <code>.cpp</code> порядок отдается на откуп линкеру: он обычно зависит от случайных факторов — порядка передачи файлов в строке компоновщика или работы параллельной сборки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Порядок может зависеть от того, что передали первым:\ng++ a.o b.o -o app # Инициализируется сначала A, потом B\ng++ b.o a.o -o app # Инициализируется сначала B, потом A\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/initialization?utm_source=gemini#Non-local_variables\">cppreference: Initialization (Order of initialization)</a></p>\n","group":"Сложный уровень","line":3595},{"id":"content/01_cpp/14_macro_and_build.md:052e7aee6c0c6741:1","question":"<p>Как локальные <code>static</code>-объекты помогают обходить эту проблему?</p>\n","answer":"<p><strong>Ответ:</strong> С помощью идиомы Мейерса (Meyers' Singleton). Локальный статический объект внутри функции создается строго по требованию (lazy) в момент первого прохода управления через его объявление. Стандарт C++11 также гарантирует потокобезопасность такой инициализации (Magic Statics).</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">Database&amp; get_database() {\n    static Database db; // Гарантированно создается в момент ПЕРВОГО вызова функции\n    return db;\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.aristeia.com/books.html?utm_source=gemini\">Effective C++ (Scott Meyers, Item 4)</a></p>\n","group":"Сложный уровень","line":3610},{"id":"content/01_cpp/14_macro_and_build.md:5d992e2382afee4d:1","question":"<p>Что такое modules в C++ и как они меняют тему препроцессора и сборки?</p>\n","answer":"<p><strong>Ответ:</strong> Модули (введенные в C++20) — это компонентная замена механизму включения заголовочных файлов <code>#include</code>. Модули транслируются один раз в бинарный интерфейс (BMI — Built Module Interface), семантически изолированы от макросов, не допускают утечек внутренних определений наружу и не требуют заголовочных файлов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// math.ixx\nexport module math;\nexport int add(int a, int b) { return a + b; }\n\n// main.cpp\nimport math;\nint main() { return add(2, 3); }\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/modules?utm_source=gemini\">cppreference: Modules</a></p>\n","group":"Сложный уровень","line":3625},{"id":"content/01_cpp/14_macro_and_build.md:99ff866d8fba54a4:1","question":"<p>Чем modules отличаются от <code>#include</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Директива <code>#include</code> производит примитивную текстовую вставку содержимого файла со всеми его макросами, требуя повторного парсинга в каждом <code>.cpp</code>. Модуль импортируется как уже разобранное синтаксическое AST-дерево: макросы из модуля не просачиваются в клиентский код, а макросы клиента не могут сломать внутренности модуля.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">#define add(a, b) (0) // Сломает #include &quot;math.h&quot;\nimport math;          // Модулю плевать на макрос 'add', он скомпилирован изолированно\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#mod-modules\">C++ Core Guidelines: Modules</a></p>\n","group":"Сложный уровень","line":3643},{"id":"content/01_cpp/14_macro_and_build.md:76ef85f4e43f3d72:1","question":"<p>Почему modules могут ускорять сборку и уменьшать зависимости?</p>\n","answer":"<p><strong>Ответ:</strong> Компилятор парсит интерфейс модуля один раз и сохраняет его в компактный бинарный вид (BMI). При импорте модуля клиентские файлы загружают готовые структуры данных напрямую в память, минуя сотни тысяч строк препроцессинга и повторный лексический разбор тяжелых заголовочных файлов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// import std; в C++23 компилируется в десятки раз быстрее, чем #include &lt;iostream&gt;, &lt;vector&gt; и т.д.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2019/p1103r3.pdf?utm_source=gemini\">WG21 Paper P1103R3: Merging Modules</a></p>\n","group":"Сложный уровень","line":3656},{"id":"content/01_cpp/14_macro_and_build.md:fce2faead4883af3:1","question":"<p>Почему modules не равны просто “новому способу подключать заголовки”?</p>\n","answer":"<p><strong>Ответ:</strong> Модули фундаментально меняют семантику языка: они вводят понятие «видимости» без «доступности» (reachability vs visibility), предотвращают коллизии макросов, позволяют экспортировать сущности без заголовков, но при этом требуют от сборочных систем строгой топологической сортировки графа сборки до начала компиляции.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>Модули нельзя скомпилировать строго параллельно &quot;вслепую&quot;:\nЕсли B импортирует A, то компилятор обязан скомпилировать модуль A до модуля B.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.kitware.com/cpp-20-modules-in-cmake-with-scheduled-builds/?utm_source=gemini\">Kitware: C++20 Modules in CMake with Scheduled Builds</a></p>\n","group":"Сложный уровень","line":3669},{"id":"content/01_cpp/14_macro_and_build.md:399efc36764ac8e8:1","question":"<p>Что такое interface unit модуля?</p>\n","answer":"<p><strong>Ответ:</strong> Module interface unit (единица интерфейса модуля) — это файл модуля, начинающийся с объявления <code>export module ModuleName;</code>. Он определяет открытый контракт компонента и содержит объявления и определения сущностей, которые помечаются ключевым словом <code>export</code> для предоставления другим модулям.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// logger.ixx\nexport module Logger;\nexport void log_info(const char* msg); // Экспортируемый публичный API\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/modules?utm_source=gemini#Module_interface_unit\">cppreference: Module interface unit</a></p>\n","group":"Сложный уровень","line":3683},{"id":"content/01_cpp/14_macro_and_build.md:1ddce7fff245529c:1","question":"<p>Что такое implementation unit модуля?</p>\n","answer":"<p><strong>Ответ:</strong> Module implementation unit (единица реализации модуля) — это файл модуля с объявлением <code>module ModuleName;</code> (без <code>export</code>). Он содержит тела функций и приватные детали реализации. Изменения внутри implementation unit не меняют бинарный интерфейс модуля (BMI) и не требуют перекомпиляции клиентов, использующих этот модуль.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// logger.cpp\nmodule Logger; // Реализация модуля Logger\nvoid log_info(const char* msg) {\n    // Реализация скрыта от клиентов\n}\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/modules?utm_source=gemini#Module_implementation_unit\">cppreference: Module implementation unit</a></p>\n","group":"Сложный уровень","line":3697},{"id":"content/01_cpp/14_macro_and_build.md:a4b36852a27e175e:1","question":"<p>Как modules меняют ODR и видимость сущностей?</p>\n","answer":"<p><strong>Ответ:</strong> Сущности, объявленные внутри модуля без спецификатора <code>export</code>, получают модуль-компоновку (module linkage): они видны всем единицам одного модуля, но абсолютно невидимы вне его. Это исключает случайные нарушения ODR между неэкспортированными функциями с одинаковыми именами в разных модулях.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// ModuleA.ixx: void helper() {} (без export)\n// ModuleB.ixx: void helper() {} (без export)\n// Обе функции сосуществуют в одном бинарнике без конфликта ODR!\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/basic.link?utm_source=gemini\">ISO C++ Standard: Module linkage (§ 6.6 [basic.link])</a></p>\n","group":"Сложный уровень","line":3713},{"id":"content/01_cpp/14_macro_and_build.md:ac6d23cdf9ef1bcf:1","question":"<p>Почему переход с заголовков на modules в существующем проекте может быть сложным?</p>\n","answer":"<p><strong>Ответ:</strong> Сложности вызваны: 1) необходимостью строгой поддержки со стороны сборочной системы (сканирование динамических зависимостей модулей на лету); 2) плохой совместимостью с легаси-макросами в сторонних C-библиотеках; 3) неполной зрелостью экосистемных инструментов (IDE, индексаторы, линтеры).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Если старый заголовок ожидал макрос конфигурации перед своим вызовом:\n#define USE_SSL\n#include &quot;network.h&quot; // Работает\n// С модулями так сделать нельзя: import network; изолирован от внешних define!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.youtube.com/watch?v=yPfPndY3b_E?utm_source=gemini\">Bryce Adelstein Lelbach: The C++20 Modules Journey</a></p>\n","group":"Сложный уровень","line":3727},{"id":"content/01_cpp/14_macro_and_build.md:3c776b952d1e4031:1","question":"<p>Какие типичные ошибки разработчики делают в теме препроцессора и сборки?</p>\n","answer":"<p><strong>Ответ:</strong> 1) Помещение определений не-<code>inline</code> переменных или функций в заголовки (ошибки <code>multiple definition</code>); 2) Отсутствие include guards; 3) Написание небезопасных макросов без скобок или с побочными эффектами; 4) Циклические включения заголовков; 5) Зависимость от случайных транзитивных инклудов; 6) Нарушение ODR из-за разных флагов компиляции между <code>.cpp</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code class=\"language-cpp\">// Ошибка: в header.h написано\nint global_seed = 42; // Вызовет multiple definition error при линковке\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wiki.sei.cmu.edu/confluence/display/cplusplus/CERT+C%2B%2B+Coding+Standard?utm_source=gemini\">SEI CERT C++ Coding Standard</a></p>\n","group":"Сложный уровень","line":3743},{"id":"content/01_cpp/14_macro_and_build.md:37d334d729dfb22a:1","question":"<p>Какие основные практические принципы по теме <code>Препроцессор_и_сборка</code> ты бы сформулировал для современного C++?</p>\n","answer":"<p><strong>Ответ:</strong> 1) Минимизировать препроцессор: заменять <code>#define</code> на <code>constexpr</code>, <code>inline</code>, <code>const</code> и шаблоны; 2) Делать заголовки самодостаточными; 3) Использовать forward declarations и pImpl для разрыва физических зависимостей; 4) Соблюдать принцип IWYU; 5) Четко разделять интерфейс и реализацию таргетов (<code>PUBLIC</code>/<code>PRIVATE</code> в CMake); 6) Включать санитайзеры в CI/CD; 7) Готовить архитектуру к переходу на C++20 Modules.</p>\n<pre><code> **Пример:**\n\n ```cpp\n // Вместо препроцессора:\n #define BUFFER_SIZE 256\n // Современный C++:\n inline constexpr std::size_t buffer_size = 256;\n ```\n\n **Источник:** [C++ Core Guidelines: Source Files and Architecture](https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#sf-source-files)\n</code></pre>\n","group":"Сложный уровень","line":3756}];
