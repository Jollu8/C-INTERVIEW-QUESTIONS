window.QUESTION_TOPICS = window.QUESTION_TOPICS || {};
window.QUESTION_TOPICS["content/03_concurrency/01_base.md"] = [{"id":"content/03_concurrency/01_base.md:867e845242eda85b:1","question":"<p>Что такое concurrency?</p>\n","answer":"<p><strong>Ответ:</strong> Concurrency (конкурентность) — это архитектурное свойство системы, позволяющее декомпозировать программу на независимые потоки управления (задачи), которые могут продвигаться вперед независимо, перекрываясь во времени. На одноядерном процессоре это достигается за счет вытесняющей многозадачности (timeslicing).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Конкурентное выполнение двух задач:\nvoid task1() { /* обработка сети */ }\nvoid task2() { /* обновление интерфейса */ }\n// Задачи запускаются в отдельных потоках и могут чередоваться на одном ядре\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://go.dev/blog/waza-talk?utm_source=gemini\">Rob Pike: Concurrency is not Parallelism</a></p>\n","group":"1. Основы","line":3},{"id":"content/03_concurrency/01_base.md:1f7b5b8b43ccbfaa:1","question":"<p>Что такое parallelism?</p>\n","answer":"<p><strong>Ответ:</strong> Parallelism (параллелизм) — это одновременное физическое выполнение нескольких вычислительных инструкций или задач в один и тот же момент времени на разных физических ядрах процессора, процессорах или векторных исполнительных блоках (SIMD/GPU).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;algorithm&gt;\n#include &lt;execution&gt;\n#include &lt;vector&gt;\n\n// Параллельная сортировка на доступных физических ядрах CPU:\nstd::vector&lt;int&gt; data(1'000'000);\nstd::sort(std::execution::par, data.begin(), data.end());\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/algorithm/execution_policy_tag_t?utm_source=gemini\">cppreference: Execution policies</a></p>\n","group":"1. Основы","line":19},{"id":"content/03_concurrency/01_base.md:4719e6debeac1a74:1","question":"<p>Чем concurrency отличается от parallelism?</p>\n","answer":"<p><strong>Ответ:</strong> Concurrency — это про структурирование программы как набора независимых процессов, которые потенциально могут выполняться параллельно («concurrency is about structure»). Parallelism — это про фактическое аппаратное одновременное выполнение вычислений («parallelism is about execution»). Конкурентная программа может исполняться на единственном ядре, параллельная всегда требует аппаратной множественности.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Одноядерный CPU: Concurrency есть (потоки переключаются), Parallelism равен 1.\n// 8-ядерный CPU: Concurrency + Parallelism (до 8 потоков на такт).\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.manning.com/books/c-plus-plus-concurrency-in-action-second-edition?utm_source=gemini\">Anthony Williams: C++ Concurrency in Action (Chapter 1)</a></p>\n","group":"1. Основы","line":38},{"id":"content/03_concurrency/01_base.md:7c35a7bc39a7386e:1","question":"<p>Чем поток отличается от процесса?</p>\n","answer":"<p><strong>Ответ:</strong> Процесс — это изолированная сущность операционной системы со своим собственным виртуальным адресным пространством, таблицей файловых дескрипторов и защитой памяти. Поток (thread) — это легковесная единица выполнения внутри процесса: все потоки одного процесса делят общее адресное пространство, кучу и глобальные переменные, имея при этом собственные стеки и регистры процессора.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>int shared_global = 0; // Доступна ВСЕМ потокам внутри одного процесса\n// В разных процессах у каждого была бы своя изолированная копия shared_global\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.os-book.com/?utm_source=gemini\">Silberschatz, Galvin: Operating System Concepts (Processes and Threads)</a></p>\n","group":"1. Основы","line":52},{"id":"content/03_concurrency/01_base.md:5a9591a81638677b:1","question":"<p>Что такое context switch?</p>\n","answer":"<p><strong>Ответ:</strong> Context switch (переключение контекста) — процедура ядра операционной системы или планировщика, сохраняющая состояние регистров процессора, программного счетчика (PC) и указателя стека (SP) текущего активного потока, и загружающая сохраненное состояние другого потока для продолжения его исполнения.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Запуск сотен потоков провоцирует постоянные переключения контекста:\n// ОС: сохранить R1-R15 потока 1 -&gt; переключить CR3/SP -&gt; загрузить регистры потока 2\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://man7.org/linux/man-pages/man7/sched.7.html?utm_source=gemini\">Linux man-pages: sched(7)</a></p>\n","group":"1. Основы","line":66},{"id":"content/03_concurrency/01_base.md:be3e5cbdcb7d2f7a:1","question":"<p>Почему context switch дорогой?</p>\n","answer":"<p><strong>Ответ:</strong> Помимо прямого времени работы ядра ОС на смену таблиц и регистров (~1–3 мкс), основная стоимость вызвана косвенным ущербом для аппаратуры: сброс конвейера CPU, инвалидация буфера ассоциативной трансляции (TLB) при смене процессов и вытеснение горячих данных из процессорных кэшей L1/L2 (ухудшение cache locality).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Высокий показатель cswch в утилите vmstat говорит о деградации:\n// vmstat 1 -&gt; поле 'cs' (context switches per second) &gt; 100 000\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.brendangregg.com/methodology.html?utm_source=gemini\">Brendan Gregg: Systems Performance (Context Switches)</a></p>\n","group":"1. Основы","line":80},{"id":"content/03_concurrency/01_base.md:f6546fac9ee59a26:1","question":"<p>Что такое race condition?</p>\n","answer":"<p><strong>Ответ:</strong> Race condition (состояние гонки) — логическая ошибка синхронизации, при которой корректность поведения или конечный результат программы зависят от случайного порядка или таймингов чередования выполнения инструкций независимых потоков.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Поток A: if (queue.empty() == false)\n// Поток B успел вызвать queue.pop()\n// Поток A: auto item = queue.pop(); // Ошибка: очередь уже пуста!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#cp2-avoid-data-races\">ISO C++ Guidelines: Concurrency (CP.2)</a></p>\n","group":"1. Основы","line":94},{"id":"content/03_concurrency/01_base.md:323cf65972b572ff:1","question":"<p>Что такое data race в C++?</p>\n","answer":"<p><strong>Ответ:</strong> Data race (гонка по данным) — специфическая ситуация в памяти C++, возникающая, когда два или более потока одновременно обращаются к одной и той же ячейке памяти (конфликтный доступ), хотя бы один из них выполняет операцию записи, и между этими обращениями нет отношения синхронизации (happens-before).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>int counter = 0;\n// Поток 1: counter++;\n// Поток 2: counter++; // Data race, если нет std::atomic или std::mutex\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/intro.races?utm_source=gemini\">ISO C++ Standard: Data races (§ 6.9.2.2 [intro.races])</a></p>\n","group":"1. Основы","line":109},{"id":"content/03_concurrency/01_base.md:2922f1c0cb5d5d0d:1","question":"<p>Чем data race опасен в C++?</p>\n","answer":"<p><strong>Ответ:</strong> Согласно стандарту C++, любое наличие data race приводит к Неопределенному Поведению (Undefined Behavior). Компилятор имеет право предполагать отсутствие гонок по данным, что приводит к некорректным оптимизациям (вынос чтений из цикла, разрыв неатомарных операций на части) и разрушению структур в памяти.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Чтение неатомарного double может прочитать 4 байта старого значения\n// и 4 байта нового значения (Tearing / повреждение значения в памяти).\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.hpl.hp.com/techreports/2004/HPL-2004-209.pdf?utm_source=gemini\">Hans Boehm: Threads Cannot be Implemented as a Library (PLDI 2005)</a></p>\n","group":"1. Основы","line":124},{"id":"content/03_concurrency/01_base.md:552639ed2a1ee64c:1","question":"<p>Что такое deadlock?</p>\n","answer":"<p><strong>Ответ:</strong> Deadlock (взаимная блокировка) — критическое состояние многопоточной системы, при котором два или более потока взаимно заблокированы в ожидании освобождения ресурсов (например, мьютексов), удерживаемых друг другом, и ни один из них не может продолжить работу.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Поток 1 удерживает mtxA, ждет mtxB\n// Поток 2 удерживает mtxB, ждет mtxA -&gt; Deadlock\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://dl.acm.org/doi/10.1145/356586.356588?utm_source=gemini\">Coffman, E. G.: System Deadlocks (ACM Computing Surveys)</a></p>\n","group":"1. Основы","line":138},{"id":"content/03_concurrency/01_base.md:e93bcdc9337dc432:1","question":"<p>Что такое livelock?</p>\n","answer":"<p><strong>Ответ:</strong> Livelock (активная блокировка) — состояние системы, при котором два или более потока активно меняют свои внутренние состояния в ответ на действия друг друга, потребляя 100% времени CPU, но не совершают никакого полезного прогресса в алгоритме.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Два потока бесконечно уступают ресурс друг другу в цикле:\nwhile (resource_busy) {\n    yield(); // Оба вежливо отступают и снова сталкиваются\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.manning.com/books/c-plus-plus-concurrency-in-action-second-edition?utm_source=gemini\">Anthony Williams: C++ Concurrency in Action (Deadlock and Livelock)</a></p>\n","group":"1. Основы","line":152},{"id":"content/03_concurrency/01_base.md:890b5261eb0f515c:1","question":"<p>Что такое starvation?</p>\n","answer":"<p><strong>Ответ:</strong> Starvation (голодание) — ситуация, при которой поток с корректной логикой выполнения длительное время или бесконечно не может получить доступ к необходимому разделяемому ресурсу или кванту процессорного времени из-за несправедливого распределения ресурсов в пользу других потоков.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Высокоприоритетные потоки непрерывно захватывают мьютекс,\n// из-за чего низкоприоритетный поток голодает (не может войти в секцию).\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.elsevier.com/books/the-art-of-multiprocessor-programming/herlihy/978-0-12-397337-5?utm_source=gemini\">The Art of Multiprocessor Programming (Herlihy &amp; Shavit)</a></p>\n","group":"1. Основы","line":168},{"id":"content/03_concurrency/01_base.md:a0a28004160bb64b:1","question":"<p>Что такое contention?</p>\n","answer":"<p><strong>Ответ:</strong> Contention (конкуренция / соперничество за ресурс) — состояние, возникающее, когда несколько параллельных потоков одновременно запрашивают доступ к одной и той же критической секции, мьютексу, кэш-линии или аппаратному блоку.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::mutex mtx;\n// 16 потоков на 16 ядрах одновременно делают mtx.lock():\n// Высокий contention приводит к сериализации и задержкам на замке.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.brendangregg.com/methodology.html?utm_source=gemini\">Brendan Gregg: Systems Performance (Contention)</a></p>\n","group":"1. Основы","line":182},{"id":"content/03_concurrency/01_base.md:4219d112e8c2bdaa:1","question":"<p>Почему многопоточность может замедлять код?</p>\n","answer":"<p><strong>Ответ:</strong> Из-за накладных расходов синхронизации: затраты на захват и освобождение блокировок, сериализация потоков в очередях мьютексов (contention), переключения контекста ядра, промахи кэша CPU (cache bouncing) и эффект ложного разделения данных (false sharing).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Параллельный подсчет с записью в общий атомик работает медленнее одного потока:\nstd::atomic&lt;int&gt; sum{0};\n// Потоки перегружают шину когерентности памяти (Cache Invalidation storm)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.packtpub.com/product/the-art-of-writing-efficient-programs/9781800208117?utm_source=gemini\">Fedore Pikus: The Art of Writing Efficient Programs</a></p>\n","group":"1. Основы","line":197},{"id":"content/03_concurrency/01_base.md:d0120006fc72b0e2:1","question":"<p>Что такое thread-safe код?</p>\n","answer":"<p><strong>Ответ:</strong> Thread-safe (потокобезопасный) код — это фрагмент программы (функция, класс, модуль), гарантирующий корректность своего поведения, сохранение инвариантов структур данных и отсутствие data races при одновременном обращении к нему из нескольких потоков без дополнительной внешней синхронизации со стороны пользователя.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>class SafeCounter {\n    std::atomic&lt;int&gt; val_{0};\npublic:\n    void inc() { val_.fetch_add(1, std::memory_order_relaxed); } // Thread-safe\n};\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#cpcon-concurrency\">C++ Core Guidelines: CP.con: Concurrency Rules</a></p>\n","group":"1. Основы","line":212},{"id":"content/03_concurrency/01_base.md:ba472366fafe3b38:1","question":"<p>Что такое memory model в C++?</p>\n","answer":"<p><strong>Ответ:</strong> Модель памяти C++ (формализована в стандарте C++11) — это спецификация, определяющая контракт между программистом, компилятором и аппаратурой: правила взаимодействия потоков через оперативную память, допустимые переупорядочивания операций и гарантии видимости записей между потоками.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Стандарт определяет семантику атомиков, гонок и барьеров:\n#include &lt;atomic&gt;\nstd::atomic&lt;int&gt; a;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/memory_model?utm_source=gemini\">cppreference: Memory model</a></p>\n","group":"2. Модель памяти","line":231},{"id":"content/03_concurrency/01_base.md:7a946c411c120cff:1","question":"<p>Что такое happens-before?</p>\n","answer":"<p><strong>Ответ:</strong> Отношение happens-before («произошло-до») — фундаментальное транзитивное отношение порядка между двумя операциями в C++. Если операция $A$ happens-before операции $B$, то все изменения в памяти, сделанные операцией $A$, гарантированно видимы операции $B$, и $A$ логически предшествует $B$.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Запись данных (A) happens-before чтения этих данных (B)\n// благодаря межпоточной синхронизации acquire-release.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/intro.multithread?utm_source=gemini\">ISO C++ Standard: Happens before order (§ 6.9.2.1 [intro.multithread])</a></p>\n","group":"2. Модель памяти","line":246},{"id":"content/03_concurrency/01_base.md:39a1a1b3ced32689:1","question":"<p>Что такое sequenced-before?</p>\n","answer":"<p><strong>Ответ:</strong> Sequenced-before — отношение строгого частичного порядка между вычислениями, выполняемыми внутри <strong>одного и того же</strong> потока управления, задаваемое грамматическими правилами языка C++ (например, точка с запятой или оператор запятая).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>int a = 1; // Вычисление A sequenced-before вычисления B:\nint b = a + 2; // B\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/language/eval_order?utm_source=gemini\">cppreference: Order of evaluation</a></p>\n","group":"2. Модель памяти","line":260},{"id":"content/03_concurrency/01_base.md:f3fbdbaa79c23e63:1","question":"<p>Что такое synchronization?</p>\n","answer":"<p><strong>Ответ:</strong> Синхронизация — это применение языковых или аппаратных механизмов (мьютексов, атомарных операций, барьеров памяти), устанавливающих отношение межпоточного упорядочивания (synchronizes-with), что исключает состояние гонки и обеспечивает корректную передачу данных между потоками.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::atomic&lt;bool&gt; flag{false};\n// Поток 1: flag.store(true, std::memory_order_release); // Synchronizes-with\n// Поток 2: while(!flag.load(std::memory_order_acquire)); // потоком 2\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://preshing.com/20130823/the-synchronizes-with-relation/?utm_source=gemini\">Jeff Preshing: Synchronizes-With vs Happens-Before</a></p>\n","group":"2. Модель памяти","line":274},{"id":"content/03_concurrency/01_base.md:913233132589f23f:1","question":"<p>Что такое visibility?</p>\n","answer":"<p><strong>Ответ:</strong> Visibility (видимость) — свойство многопоточной памяти, определяющее, когда модификация ячейки памяти, выполненная одним ядром процессора (сохраненная в локальный Store Buffer/L1-кэш), становится фактически доступной для чтения инструкциями, исполняемыми на другом ядре.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Без барьера запись в переменную x может часами оставаться в Store Buffer ядра\n// и быть невидимой для другого ядра CPU.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.morganclaypool.com/doi/abs/10.2200/S00346ED1V01Y201104CAC016?utm_source=gemini\">A Primer on Memory Consistency and Cache Coherence (Sorin et al.)</a></p>\n","group":"2. Модель памяти","line":289},{"id":"content/03_concurrency/01_base.md:0d6d87bea8808233:1","question":"<p>Что такое ordering?</p>\n","answer":"<p><strong>Ответ:</strong> Ordering (упорядочивание) — правила и ограничения, накладываемые на последовательность исполнения операций чтения (<code>load</code>) и записи (<code>store</code>) в память, предотвращающие их опасную перестановку оптимизаторами компилятора или суперскалярным процессором.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::atomic_thread_fence(std::memory_order_seq_cst); // Барьер упорядочивания\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/atomic/memory_order?utm_source=gemini\">cppreference: std::memory_order</a></p>\n","group":"2. Модель памяти","line":303},{"id":"content/03_concurrency/01_base.md:260bdb739f79f623:1","question":"<p>Что такое compiler reordering?</p>\n","answer":"<p><strong>Ответ:</strong> Compiler reordering — перестановка порядка инструкций чтения и записи в память оптимизатором компилятора на этапе генерации ассемблерного кода для более эффективного заполнения конвейера CPU, если такая перестановка не нарушает правил вычисления в рамках текущего единичного потока (As-If Rule).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Исходный код:       // Компилятор может сгенерировать:\nready = true;          val = 42;\nval = 42;              ready = true; // Для другого потока флаг готов раньше значения!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.agner.org/optimize/optimizing_cpp.pdf?utm_source=gemini\">Agner Fog: Optimizing Subroutines in C++</a></p>\n","group":"2. Модель памяти","line":316},{"id":"content/03_concurrency/01_base.md:57bcb5e1549284a2:1","question":"<p>Что такое CPU reordering?</p>\n","answer":"<p><strong>Ответ:</strong> CPU reordering — аппаратное переупорядочивание инструкций суперскалярным процессором во время выполнения программы (Out-of-Order Execution). Процессор отправляет запросы в память через Store Buffer и инвалидационные очереди в произвольном порядке для минимизации простоев шины.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// На уровне кремния запись может задержаться в Store Buffer,\n// а чтение выполниться спекулятивно раньше этой записи.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html?utm_source=gemini\">Intel 64 Architecture Memory Ordering Whitepaper</a></p>\n","group":"2. Модель памяти","line":331},{"id":"content/03_concurrency/01_base.md:e0091a5c77e5b7e9:1","question":"<p>Почему на x86 код иногда “случайно работает”, а на ARM ломается?</p>\n","answer":"<p><strong>Ответ:</strong> Архитектура x86 реализует сильную модель памяти TSO (Total Store Order), где аппаратные перестановки строго ограничены (запрещены перестановки Store-Store, Load-Load, Load-Store; возможен только Store-Load). Архитектура ARM имеет слабую модель памяти (Weakly-Ordered), где процессор имеет право переставлять любые операции памяти местами, обнажая пропущенные барьеры синхронизации.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Без явного std::memory_order_release:\n// На x86 запись флага не обойдет запись данных аппаратно.\n// На ARM процессор свободно переставит записи местами, сломав логику.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://developer.arm.com/documentation/ddi0487/latest/?utm_source=gemini\">Arm Architecture Reference Manual (Memory Model)</a></p>\n","group":"2. Модель памяти","line":345},{"id":"content/03_concurrency/01_base.md:2ccce3e5450f2040:1","question":"<p>Что такое <code>memory_order_relaxed</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>memory_order_relaxed</code> — режим упорядочивания памяти, гарантирующий исключительно атомарность модификации целевой переменной и отсутствие разрыва байтов (non-tearing), но полностью исключающий любые ограничения на перестановку операций относительно других чтений и записей.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::atomic&lt;int&gt; counter{0};\n// Просто счетчик: не синхронизирует состояние других данных\ncounter.fetch_add(1, std::memory_order_relaxed);\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/atomic/memory_order?utm_source=gemini#Relaxed_ordering?utm_source=gemini\">cppreference: memory_order_relaxed</a></p>\n","group":"2. Модель памяти","line":360},{"id":"content/03_concurrency/01_base.md:7886f80019c5a3cf:1","question":"<p>Что такое <code>memory_order_acquire</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>memory_order_acquire</code> — операция чтения из атомика, запрещающая компилятору и процессору переносить любые последующие операции чтения или записи в память <strong>выше</strong> (до) этой операции acquire.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>while (!ready.load(std::memory_order_acquire));\n// Все последующие чтения данных гарантированно увидят актуальные значения:\nint x = payload_data;\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/atomics.order?utm_source=gemini\">ISO C++ Standard: Acquire-Release (§ 31.4 [atomics.order])</a></p>\n","group":"2. Модель памяти","line":375},{"id":"content/03_concurrency/01_base.md:63616b94d4312a49:1","question":"<p>Что такое <code>memory_order_release</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>memory_order_release</code> — операция записи в атомик, запрещающая компилятору и процессору переносить любые предшествующие операции чтения или записи в память <strong>ниже</strong> (после) этой операции release.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>payload_data = 42; // Подготовка данных\n// Публикация: запись данных не опустится ниже флага:\nready.store(true, std::memory_order_release);\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/atomic/memory_order?utm_source=gemini#Release-Acquire_ordering?utm_source=gemini\">cppreference: memory_order_release</a></p>\n","group":"2. Модель памяти","line":390},{"id":"content/03_concurrency/01_base.md:b3da4a7592ace05f:1","question":"<p>Что дает acquire-release пара?</p>\n","answer":"<p><strong>Ответ:</strong> Пара «запись с <code>release</code>» в одном потоке и «чтение с <code>acquire</code>» того же значения в другом потоке формирует межпоточное отношение synchronizes-with. Это гарантирует, что всё, что было записано первым потоком до вызова <code>release</code>, становится полностью видимым второму потоку после вызова <code>acquire</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Поток 1: data = 100; ready.store(true, memory_order_release);\n// Поток 2: if (ready.load(memory_order_acquire)) assert(data == 100); // Всегда верно!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://preshing.com/20120913/acquire-and-release-semantics/?utm_source=gemini\">Jeff Preshing: The Purpose of memory_order_consume/acquire/release</a></p>\n","group":"2. Модель памяти","line":405},{"id":"content/03_concurrency/01_base.md:b817dbd8b25d6ab7:1","question":"<p>Что такое <code>memory_order_seq_cst</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>memory_order_seq_cst</code> (Sequentially Consistent) — самый строгий режим по умолчанию. Он включает в себя acquire-release семантику и дополнительно накладывает гарантию единого глобального порядка (total globally consistent order) всех <code>seq_cst</code> операций во всей программе, одинаково наблюдаемого всеми потоками.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::atomic&lt;int&gt; x{0}, y{0};\n// По умолчанию операции используют seq_cst:\nx.store(1); // Гарантирует абсолютную предсказуемость порядка для всех CPU\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://lamport.azurewebsites.net/pubs/multi.pdf?utm_source=gemini\">Leslie Lamport: How to Make a Multiprocessor Computer That Correctly Executes Multiprocess Programs</a></p>\n","group":"2. Модель памяти","line":419},{"id":"content/03_concurrency/01_base.md:97ffa31ae665e796:1","question":"<p>Когда <code>relaxed</code> оправдан?</p>\n","answer":"<p><strong>Ответ:</strong> Он оправдан тогда, когда атомарная переменная используется изолированно и не служит флагом готовности или защитником других структур данных: например, счетчики посещений сайта, генераторы уникальных числовых ID, сбор метрик или прогресс-бары.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::atomic&lt;uint64_t&gt; metrics_requests_total{0};\nmetrics_requests_total.fetch_add(1, std::memory_order_relaxed); // Идеально\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: CP.atomic: Don't use relaxed unless you have to</a></p>\n","group":"2. Модель памяти","line":434},{"id":"content/03_concurrency/01_base.md:f29e0e1708ce9fad:1","question":"<p>Как создать поток в C++?</p>\n","answer":"<p><strong>Ответ:</strong> Создать поток можно через инстанцирование объекта <code>std::thread</code> (начиная с C++11), передав ему в конструктор вызываемый объект (функцию, лямбду, функтор или метод класса) и его аргументы. Поток запускается на исполнение немедленно в момент создания.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;thread&gt;\nvoid worker(int id) { /*...*/ }\n\nstd::thread t(worker, 1);\nt.join();\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/thread/thread?utm_source=gemini\">cppreference: std::thread::thread</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":450},{"id":"content/03_concurrency/01_base.md:662a38082854f08c:1","question":"<p>Что делает <code>join()</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Метод <code>join()</code> блокирует выполнение вызывающего потока до тех пор, пока целевой поток, ассоциированный с объектом <code>std::thread</code>, полностью не завершит свое выполнение и не вернет управление. После этого поток перестает быть joinable (<code>t.joinable() == false</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::thread t([]() { /* долгая работа */ });\nt.join(); // Ждем завершения t перед тем, как продолжить main\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/thread/join?utm_source=gemini\">cppreference: std::thread::join</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":467},{"id":"content/03_concurrency/01_base.md:893b97a496e9fcc7:1","question":"<p>Что делает <code>detach()</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Метод <code>detach()</code> разрывает связь между объектом <code>std::thread</code> в программе и реальным низкоуровневым потоком операционной системы. Поток становится фоновым (daemon-like), продолжает жить самостоятельно, а его ресурсы автоматически освобождаются ОС при его завершении.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::thread t([]() { background_cleanup(); });\nt.detach(); // t больше не ассоциирован с потоком выполнения\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/thread/detach?utm_source=gemini\">cppreference: std::thread::detach</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":481},{"id":"content/03_concurrency/01_base.md:81ac327f5989851b:1","question":"<p>Что будет, если уничтожить joinable <code>std::thread</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Если деструктор <code>std::thread</code> вызывается для объекта, который всё еще является <code>joinable</code> (для него не были вызваны ни <code>join()</code>, ни <code>detach()</code>), рантайм C++ немедленно аварийно завершает работу всей программы вызовом <code>std::terminate()</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>void bad_func() {\n    std::thread t([](){});\n    // Забыли t.join(); при выходе из функции сработает std::terminate()\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/thread/~thread?utm_source=gemini\">cppreference: std::thread::~thread</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":495},{"id":"content/03_concurrency/01_base.md:e44d26d26dd638aa:1","question":"<p>Почему <code>detach()</code> часто плохая идея?</p>\n","answer":"<p><strong>Ответ:</strong> Поток с <code>detach()</code> теряет предсказуемый жизненный цикл. Если программа завершит <code>main()</code>, фоновый detached-поток будет грубо уничтожен посреди выполнения, не вызвав деструкторы стековых объектов, либо он может попытаться обратиться к уже освобожденной глобальной памяти процесса, вызвав Segmentation Fault.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>void start() {\n    int local_data = 42;\n    std::thread([&amp;local_data](){ /* обращение к умершей local_data! */ }).detach();\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#cp26-dont-detach-a-thread\">C++ Core Guidelines: CP.26: Don't detach a thread</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":511},{"id":"content/03_concurrency/01_base.md:3377a06f5bb667f5:1","question":"<p>Когда <code>detach()</code> допустим?</p>\n","answer":"<p><strong>Ответ:</strong> <code>detach()</code> допустим крайне редко: только для долгоживущих системных фоновых сервисов уровня всего процесса (например, фоновый поток мониторинга оборудования или логирования), которые оперируют строго бесконечным циклом и не используют ссылки на локальные стековые объекты.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Фоновый сборщик статистики, не использующий внешние локальные ресурсы:\nstd::thread([]() { run_os_heartbeat(); }).detach();\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.manning.com/books/c-plus-plus-concurrency-in-action-second-edition?utm_source=gemini\">Anthony Williams: C++ Concurrency in Action (Detaching threads)</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":527},{"id":"content/03_concurrency/01_base.md:f3209657aa41b8d9:1","question":"<p>Чем <code>std::jthread</code> лучше <code>std::thread</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Класс <code>std::jthread</code> (C++20) реализует идиому RAII: он автоматически вызывает <code>request_stop()</code> и <code>join()</code> в своем деструкторе, предотвращая падение программы по <code>std::terminate()</code>. Кроме того, он нативно интегрирован с механизмом кооперативной отмены задач через <code>std::stop_token</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;thread&gt;\nvoid task() {\n    std::jthread jt([](std::stop_token st) {\n        while (!st.stop_requested()) { /* работа */ }\n    });\n} // В конце блока деструктор jt сам запросит остановку и сделает join!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/jthread?utm_source=gemini\">cppreference: std::jthread</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":541},{"id":"content/03_concurrency/01_base.md:ede77fe3935b205e:1","question":"<p>Что такое cooperative cancellation?</p>\n","answer":"<p><strong>Ответ:</strong> Кооперативная отмена — это безопасный подход к остановке потоков, при котором поток нельзя принудительно убить извне (что привело бы к зависшим мьютексам и утечкам памяти). Вместо этого управляющий поток выставляет запрос на остановку, а рабочий поток периодически сам опрашивает этот флаг и корректно освобождает ресурсы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>if (stop_token.stop_requested()) {\n    cleanup();\n    return; // Поток сам решает корректно выйти\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://eel.is/c++draft/thread.stoptoken?utm_source=gemini\">ISO C++ Standard: Stop tokens (§ 32.3 [thread.stoptoken])</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":559},{"id":"content/03_concurrency/01_base.md:fbfdfab480ba6649:1","question":"<p>Что такое <code>std::stop_token</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::stop_token</code> (C++20) — легковесный потокобезопасный объект, передаваемый в исполняемую функцию <code>std::jthread</code>. Он позволяет проверять, поступил ли запрос на остановку потока (<code>stop_requested()</code>), или регистрировать обратные вызовы (<code>std::stop_callback</code>) на случай отмены.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::jthread worker([](std::stop_token st) {\n    while (!st.stop_requested()) {\n        do_step();\n    }\n});\nworker.request_stop(); // Выставляет флаг в st\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/stop_token?utm_source=gemini\">cppreference: std::stop_token</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":575},{"id":"content/03_concurrency/01_base.md:6f948a2d59ea2f9e:1","question":"<p>Как корректно останавливать worker thread?</p>\n","answer":"<p><strong>Ответ:</strong> 1) Послать сигнал остановки (через <code>std::stop_source</code> или атомарный флаг); 2) Разбудить поток, если он находится в состоянии ожидания (через <code>condition_variable.notify_all()</code> или <code>std::condition_variable_any</code>); 3) Внутри потока обработать выход из цикла, завершить транзакции и освободить память; 4) Выполнить <code>join()</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// C++20: jthread делает это автоматически благодаря интеграции stop_token\nstd::jthread th([](std::stop_token st) {\n    while (!st.stop_requested()) {\n        std::this_thread::sleep_for(std::chrono::milliseconds(10));\n    }\n});\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: CP.42: Don't wait without a condition</a></p>\n","group":"3. `std::thread`, `std::jthread`","line":593},{"id":"content/03_concurrency/01_base.md:9724b8fd2d75eb3c:1","question":"<p>Чем опасна передача ссылки в поток?</p>\n","answer":"<p><strong>Ответ:</strong> Опасностью возникновения висячей ссылки (Dangling Reference) и Data Race. Если поток переживет область видимости переменной, на которую ссылается, он обратится к уничтоженной памяти на чужом стеке (Use-After-Free), что приведет к непредсказуемому крашу.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>void bad() {\n    int val = 10;\n    // Ошибка: val будет уничтожен при выходе из bad(), а поток продолжит чтение\n    std::thread t([](int&amp; x) { use(x); }, std::ref(val));\n    t.detach();\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.oreilly.com/library/view/effective-modern-c/9781491903988/?utm_source=gemini\">Scott Meyers: Effective Modern C++ (Item 37)</a></p>\n","group":"4. Передача данных между потоками","line":613},{"id":"content/03_concurrency/01_base.md:e842f69821c2c80b:1","question":"<p>Когда безопасно передавать указатель в поток?</p>\n","answer":"<p><strong>Ответ:</strong> Передача указателя безопасна, когда: 1) объект живет дольше потока (например, статический объект); 2) владение объектом монопольно передается потоку (через <code>std::unique_ptr</code>); 3) доступ к данным за указателем синхронизирован мьютексом.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>auto ptr = std::make_unique&lt;Data&gt;();\nstd::thread t([](std::unique_ptr&lt;Data&gt; p) { p-&gt;work(); }, std::move(ptr));\nt.join();\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: CP.31: Pass small values by value, others by unique_ptr</a></p>\n","group":"4. Передача данных между потоками","line":631},{"id":"content/03_concurrency/01_base.md:d3d3eec383d05f1f:1","question":"<p>Когда лучше передавать данные по значению?</p>\n","answer":"<p><strong>Ответ:</strong> Передача по значению предпочтительна для небольших объектов, примитивных типов и структур данных, когда потоку требуется собственный независимый снимок (snapshot) состояния без накладных расходов на межпоточную синхронизацию и без рисков гонок по памяти.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>int config_id = 42;\nstd::thread t([config_id]() { /* Изолированная копия config_id */ });\nt.join();\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.manning.com/books/c-plus-plus-concurrency-in-action-second-edition?utm_source=gemini\">Anthony Williams: C++ Concurrency in Action (Passing arguments)</a></p>\n","group":"4. Передача данных между потоками","line":646},{"id":"content/03_concurrency/01_base.md:4b9d2336f3f70257:1","question":"<p>Что делать с владением объектом между потоками?</p>\n","answer":"<p><strong>Ответ:</strong> Необходимо явно моделировать семантику владения (ownership): передавать эксклюзивное владение через семантику перемещения <code>std::unique_ptr</code>, разделять владение через <code>std::shared_ptr</code>, либо передавать сообщения через потокобезопасные очереди задач (Actor/Message Passing model).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::queue&lt;std::unique_ptr&lt;Task&gt;&gt; task_queue; // Владение передается через очередь\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: I.11: Never transfer ownership by a raw pointer</a></p>\n","group":"4. Передача данных между потоками","line":661},{"id":"content/03_concurrency/01_base.md:bd7ca736a125896c:1","question":"<p>Чем <code>shared_ptr</code> не решает все проблемы синхронизации?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::shared_ptr</code> защищает только собственный контрольный блок (счетчик ссылок потокобезопасен). Сам объект, на который указывает <code>shared_ptr</code>, никак не защищен: одновременное чтение и запись данных внутри объекта из разных потоков является классическим Data Race.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>auto ptr = std::make_shared&lt;std::vector&lt;int&gt;&gt;();\n// Поток 1: ptr-&gt;push_back(1);\n// Поток 2: ptr-&gt;push_back(2); // DATA RACE! Объекту требуется mutex\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/memory/shared_ptr?utm_source=gemini#Thread_safety?utm_source=gemini\">cppreference: std::shared_ptr thread safety</a></p>\n","group":"4. Передача данных между потоками","line":674},{"id":"content/03_concurrency/01_base.md:559275385b52e57d:1","question":"<p>Атомарен ли сам <code>shared_ptr</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Нет, сам экземпляр <code>std::shared_ptr</code> (пара: указатель на объект + указатель на контрольный блок) не атомарен. Конкурентное переприсваивание одного и того же объекта <code>shared_ptr</code> из двух потоков требует использования <code>std::atomic&lt;std::shared_ptr&lt;T&gt;&gt;</code> (начиная с C++20) или функций <code>std::atomic_load/store</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// C++20 потокобезопасный указатель:\nstd::atomic&lt;std::shared_ptr&lt;Config&gt;&gt; global_config;\nglobal_config.store(std::make_shared&lt;Config&gt;());\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/memory/shared_ptr/atomic2?utm_source=gemini\">cppreference: std::atomic</a><a href=\"std::shared_ptr\">std::shared_ptr</a></p>\n","group":"4. Передача данных между потоками","line":689},{"id":"content/03_concurrency/01_base.md:dc5bc722e5223619:1","question":"<p>Что такое publication объекта между потоками?</p>\n","answer":"<p><strong>Ответ:</strong> Publication (публикация) — это процесс конструирования и последующего предоставления ссылки или указателя на объект другим потокам. Публикация считается безопасной, только если гарантировано, что читающие потоки увидят полностью проинициализированный объект со всеми его инвариантами.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Опасная публикация: указатель опубликован, но поля объекта еще не доехали из Store Buffer!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://jcip.net/?utm_source=gemini\">Java Concurrency in Practice (Goetz et al. - Safe Publication)</a></p>\n","group":"4. Передача данных между потоками","line":704},{"id":"content/03_concurrency/01_base.md:ab3757396723806a:1","question":"<p>Как безопасно публиковать объект?</p>\n","answer":"<p><strong>Ответ:</strong> Безопасная публикация достигается через: 1) захват и освобождение <code>std::mutex</code>; 2) запись указателя через <code>std::atomic</code> с <code>memory_order_release</code> и чтение с <code>memory_order_acquire</code>; 3) статическую инициализацию (Meyers Singleton); 4) передачу через <code>std::promise</code> / <code>std::future</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::atomic&lt;Data*&gt; published_ptr{nullptr};\n// Поток 1:\nauto* d = new Data();\npublished_ptr.store(d, std::memory_order_release); // Безопасная публикация\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: CP.22: Never call a virtual function while holding a lock</a></p>\n","group":"4. Передача данных между потоками","line":717},{"id":"content/03_concurrency/01_base.md:2f5a7ae03210f630:1","question":"<p>Почему обычный флаг <code>bool ready</code> между потоками опасен?</p>\n","answer":"<p><strong>Ответ:</strong> Обычный <code>bool ready</code> не гарантирует атомарности и упорядочивания инструкций. Это прямой Data Race (Undefined Behavior), приводящий к тому, что компилятор может закэшировать чтение флага в регистр и уйти в бесконечный цикл, либо переставить чтение данных до проверки флага.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>bool ready = false;\n// Компилятор может превратить &quot;while (!ready)&quot; в бесконечный цикл:\n// if (!ready) while(true);\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.hpl.hp.com/techreports/2004/HPL-2004-209.pdf?utm_source=gemini\">Hans Boehm: Threads Cannot be Implemented as a Library</a></p>\n","group":"4. Передача данных между потоками","line":733},{"id":"content/03_concurrency/01_base.md:9d70fc547e7b3e59:1","question":"<p>Когда нужен <code>std::atomic&lt;bool&gt;</code> для флага?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::atomic&lt;bool&gt;</code> обязателен всегда, когда флаг читается одним потоком и модифицируется другим без удержания общего мьютекса. Это предотвращает Data Race, запрещает компилятору выкидывать чтение переменной и гарантирует атомарность операции на уровне шины CPU.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::atomic&lt;bool&gt; is_running{true};\nvoid stop() { is_running.store(false); }\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/atomic/atomic?utm_source=gemini\">cppreference: std::atomic</a></p>\n","group":"4. Передача данных между потоками","line":748},{"id":"content/03_concurrency/01_base.md:e6ca7d6927bd7a58:1","question":"<p>Что делает <code>std::mutex</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::mutex</code> (взаимное исключение) — базовый примитив синхронизации, гарантирующий, что в любой момент времени только один поток может владеть блокировкой и исполнять критическую секцию кода, переводя остальные претендующие потоки в состояние блокировки (сна).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;mutex&gt;\nstd::mutex mtx;\nvoid safe_print() {\n    mtx.lock();\n    // Только 1 поток здесь\n    mtx.unlock();\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/mutex?utm_source=gemini\">cppreference: std::mutex</a></p>\n","group":"5. Mutex и RAII","line":764},{"id":"content/03_concurrency/01_base.md:c147c255a97f96ef:1","question":"<p>Почему лучше RAII-обертки, чем ручной <code>lock/unlock</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Ручной <code>lock/unlock</code> приводит к утечкам блокировок (Deadlock) при генерации исключений, неожиданных выходах по <code>return</code> или <code>break</code>. RAII-обертки гарантируют освобождение мьютекса в деструкторе при раскрутке стека в любых обстоятельствах.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::lock_guard&lt;std::mutex&gt; lock(mtx);\nif (error) return; // Мьютекс освободится АВТОМАТИЧЕСКИ\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#cp20-use-raii-never-plain-lockunlock\">C++ Core Guidelines: CP.20: Use RAII, never plain lock()/unlock()</a></p>\n","group":"5. Mutex и RAII","line":783},{"id":"content/03_concurrency/01_base.md:0d15779bd17e6430:1","question":"<p>Что делает <code>std::lock_guard</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::lock_guard</code> — легковесная некопируемая RAII-обертка, которая захватывает переданный мьютекс в своем конструкторе (<code>mtx.lock()</code>) и безусловно освобождает его в деструкторе при выходе из области видимости (<code>mtx.unlock()</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>void add(int x) {\n    std::lock_guard&lt;std::mutex&gt; lock(mtx);\n    shared_list.push_back(x);\n} // mtx освобожден здесь\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/lock_guard?utm_source=gemini\">cppreference: std::lock_guard</a></p>\n","group":"5. Mutex и RAII","line":797},{"id":"content/03_concurrency/01_base.md:44dbd37c67660e54:1","question":"<p>Что делает <code>std::unique_lock</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::unique_lock</code> — расширенная гибкая RAII-обертка над мьютексом. В отличие от <code>lock_guard</code>, она поддерживает отложенный захват (<code>std::defer_lock</code>), явный ручной <code>unlock()</code> и <code>lock()</code>, перемещение владения (MoveConstructible) и необходима для работы с <code>std::condition_variable</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::unique_lock&lt;std::mutex&gt; lock(mtx);\n// временно отпускаем мьютекс:\nlock.unlock();\ndo_heavy_work();\nlock.lock();\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/unique_lock?utm_source=gemini\">cppreference: std::unique_lock</a></p>\n","group":"5. Mutex и RAII","line":813},{"id":"content/03_concurrency/01_base.md:4b7443483a02825c:1","question":"<p>Что делает <code>std::scoped_lock</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::scoped_lock</code> (C++17) — вариативная RAII-обертка, способная атомарно захватывать сразу несколько переданных мьютексов с использованием алгоритма предотвращения deadlock (аналогично <code>std::lock</code>), и безопасно освобождать их при уничтожении.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>void transfer(Account&amp; from, Account&amp; to, double amount) {\n    // Безопасный захват двух мьютексов без риска Deadlock:\n    std::scoped_lock lock(from.mtx, to.mtx);\n    from.balance -= amount;\n    to.balance += amount;\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/scoped_lock?utm_source=gemini\">cppreference: std::scoped_lock</a></p>\n","group":"5. Mutex и RAII","line":830},{"id":"content/03_concurrency/01_base.md:f2be071a58032bd6:1","question":"<p>Когда нужен <code>unique_lock</code>, а не <code>lock_guard</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Он необходим: 1) при интеграции с <code>std::condition_variable::wait()</code>; 2) когда требуется временно отпустить мьютекс внутри критической секции для выполнения долгой работы; 3) при передаче владения локом из функции; 4) при попытках захвата по таймауту (<code>std::timed_mutex</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::unique_lock&lt;std::mutex&gt; lk(mtx);\ncv.wait(lk, []{ return ready; }); // lock_guard здесь передать нельзя!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.manning.com/books/c-plus-plus-concurrency-in-action-second-edition?utm_source=gemini\">Anthony Williams: C++ Concurrency in Action (Flexible locking)</a></p>\n","group":"5. Mutex и RAII","line":848},{"id":"content/03_concurrency/01_base.md:f45f3c6c9452d8c2:1","question":"<p>Что такое критическая секция?</p>\n","answer":"<p><strong>Ответ:</strong> Критическая секция — участок программного кода, осуществляющий доступ к разделяемым разделяемым изменяемым данным (shared mutable state), одновременное нахождение в котором более чем одного потока приводит к состоянию гонки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>{\n    std::lock_guard&lt;std::mutex&gt; lock(mtx);\n    /* НАЧАЛО КРИТИЧЕСКОЙ СЕКЦИИ */\n    shared_data++;\n    /* КОНЕЦ КРИТИЧЕСКОЙ СЕКЦИИ */\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.cs.utexas.edu/users/EWD/transcriptions/EWD01xx/EWD123.html?utm_source=gemini\">Dijkstra, E. W.: Cooperating Sequential Processes</a></p>\n","group":"5. Mutex и RAII","line":862},{"id":"content/03_concurrency/01_base.md:d8f49329e7b239a9:1","question":"<p>Почему критическая секция должна быть короткой?</p>\n","answer":"<p><strong>Ответ:</strong> Пока поток удерживает блокировку, все остальные потоки, претендующие на этот мьютекс, выстраиваются в очередь и усыпляются ядром ОС (contention). Длинные критические секции убивают масштабируемость системы, превращая параллельную программу в строго последовательную.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// ПЛОХО: долгий I/O под замком\n{ std::lock_guard lk(mtx); write_to_disk(data); }\n\n// ХОРОШО: под замком только копирование, I/O снаружи\nData copy;\n{ std::lock_guard lk(mtx); copy = data; }\nwrite_to_disk(copy);\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: CP.21: Move non-shared code outside critical sections</a></p>\n","group":"5. Mutex и RAII","line":880},{"id":"content/03_concurrency/01_base.md:f9188298865a7490:1","question":"<p>Почему плохо вызывать чужой код под mutex?</p>\n","answer":"<p><strong>Ответ:</strong> Вызов внешнего («чужого») неизвестного кода (коллбэков, виртуальных функций, плагинов) под мьютексом чреват: 1) непредсказуемой задержкой; 2) исключением, ломающим инвариант; 3) вызовом повторного входа (reentrancy) в этот же мьютекс; 4) взаимной блокировкой (deadlock), если чужой код попытается взять другой лок.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>void notify_listeners() {\n    std::lock_guard&lt;std::mutex&gt; lk(mtx);\n    user_callback(); // ОПАСНО: callback может внутри захватить другой мьютекс!\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: CP.22: Never call an unknown function while holding a lock</a></p>\n","group":"5. Mutex и RAII","line":899},{"id":"content/03_concurrency/01_base.md:e818cf7e9c9e627b:1","question":"<p>Что такое lock granularity?</p>\n","answer":"<p><strong>Ответ:</strong> Lock granularity (гранулярность блокировок) — степень детализации разделения данных по мьютексам: Coarse-grained (крупнозернистая — один глобальный мьютекс на всю подсистему/таблицу) или Fine-grained (мелкозернистая — отдельный мьютекс на каждую строку, узел дерева или корзину хэш-таблицы).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Coarse: std::mutex global_tree_mtx;\n// Fine: struct Node { std::mutex node_mtx; ... };\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.elsevier.com/books/the-art-of-multiprocessor-programming/herlihy/978-0-12-397337-5?utm_source=gemini\">The Art of Multiprocessor Programming (Fine-Grained Synchronization)</a></p>\n","group":"5. Mutex и RAII","line":915},{"id":"content/03_concurrency/01_base.md:d4a71c438b1527e7:1","question":"<p>Как обычно возникает deadlock?</p>\n","answer":"<p><strong>Ответ:</strong> Deadlock возникает при одновременном выполнении 4 условий Коффмана, чаще всего по сценарию перекрестного захвата (ABBA-deadlock): Поток 1 захватывает мьютекс A и ожидает мьютекс B, в то время как Поток 2 захватывает мьютекс B и ожидает мьютекс A.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Поток 1: lock(A); lock(B);\n// Поток 2: lock(B); lock(A); // Встретились -&gt; Deadlock\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://dl.acm.org/doi/10.1145/356586.356588?utm_source=gemini\">Edward G. Coffman: System Deadlocks (1971)</a></p>\n","group":"6. Deadlock и стратегии захвата","line":931},{"id":"content/03_concurrency/01_base.md:a9dcc34d7f42a5df:1","question":"<p>Как избегать deadlock?</p>\n","answer":"<p><strong>Ответ:</strong> 1) Всегда захватывать мьютексы в строго фиксированном глобальном порядке; 2) Использовать <code>std::scoped_lock</code> или <code>std::lock</code> для одновременного захвата нескольких мьютексов; 3) Не вызывать неизвестный код под блокировкой; 4) Использовать блокировки с таймаутом (<code>std::unique_lock</code> + <code>try_lock_for</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::scoped_lock lock(mtxA, mtxB); // Исключает deadlock\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: CP.25: Prefer std::scoped_lock for multi-mutex locking</a></p>\n","group":"6. Deadlock и стратегии захвата","line":945},{"id":"content/03_concurrency/01_base.md:2eb20cb8a8fac3d9:1","question":"<p>Что такое global lock ordering?</p>\n","answer":"<p><strong>Ответ:</strong> Global lock ordering (глобальный порядок захвата) — архитектурная договоренность или иерархия, предписывающая всем разработчикам проекта захватывать независимые мьютексы строго в определенной последовательности (например, по их уникальным адресам в памяти или уровням иерархии).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Захват мьютексов всегда в порядке возрастания адресов:\nvoid lock_two(std::mutex&amp; a, std::mutex&amp; b) {\n    if (&amp;a &lt; &amp;b) { a.lock(); b.lock(); }\n    else         { b.lock(); a.lock(); }\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.manning.com/books/c-plus-plus-concurrency-in-action-second-edition?utm_source=gemini\">Anthony Williams: C++ Concurrency in Action (Hierarchical mutex)</a></p>\n","group":"6. Deadlock и стратегии захвата","line":958},{"id":"content/03_concurrency/01_base.md:8ab461e96d6c2337:1","question":"<p>Что делает <code>std::lock</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Функция <code>std::lock(m1, m2, ...)</code> атомарно захватывает произвольное количество переданных мьютексов, используя внутренний алгоритм предотвращения взаимных блокировок (deadlock avoidance algorithm, основанный на циклическом переборе с <code>try_lock</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::unique_lock lk1(mtx1, std::defer_lock);\nstd::unique_lock lk2(mtx2, std::defer_lock);\nstd::lock(lk1, lk2); // Deadlock-free захват обоих\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/lock?utm_source=gemini\">cppreference: std::lock</a></p>\n","group":"6. Deadlock и стратегии захвата","line":975},{"id":"content/03_concurrency/01_base.md:cbb58bfabbc718a8:1","question":"<p>Что такое try-lock strategy?</p>\n","answer":"<p><strong>Ответ:</strong> Стратегия неблокирующего захвата: поток пытается захватить ресурс с помощью метода <code>try_lock()</code>. Если мьютекс уже занят другим потоком, текущий поток не засыпает, а немедленно возвращает <code>false</code> и переходит к выполнению альтернативной полезной работы или отступает.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>if (mtx.try_lock()) {\n    // Делаем задачу\n    mtx.unlock();\n} else {\n    // Ресурс занят, делаем что-то другое\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/mutex/try_lock?utm_source=gemini\">cppreference: std::mutex::try_lock</a></p>\n","group":"6. Deadlock и стратегии захвата","line":990},{"id":"content/03_concurrency/01_base.md:53ae3d7b0f64ebf8:1","question":"<p>Минус <code>try_lock</code>-циклов?</p>\n","answer":"<p><strong>Ответ:</strong> Плотный цикл <code>try_lock</code> превращается в активное ожидание (busy-waiting / spinning), которое сжигает 100% ресурсов ядра процессора, перегружает межъядерную шину кэша и может приводить к голоданию (starvation) или livelock.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>while (!mtx.try_lock()) {\n    // Сжигает такты CPU впустую\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.agner.org/optimize/optimizing_cpp.pdf?utm_source=gemini\">Agner Fog: Optimizing Subroutines in C++</a></p>\n","group":"6. Deadlock и стратегии захвата","line":1008},{"id":"content/03_concurrency/01_base.md:8c2c080a33c04f56:1","question":"<p>Что такое recursive mutex?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::recursive_mutex</code> — тип мьютекса, который один и тот же поток может успешно захватывать повторно несколько раз без возникновения самоблокировки (self-deadlock). Мьютекс ведет счетчик захватов и освобождается только тогда, когда поток вызовет <code>unlock()</code> соответствующее число раз.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;mutex&gt;\nstd::recursive_mutex r_mtx;\nvoid recursive_func(int n) {\n    std::lock_guard&lt;std::recursive_mutex&gt; lk(r_mtx);\n    if (n &gt; 0) recursive_func(n - 1); // Безопасно для одного потока\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/recursive_mutex?utm_source=gemini\">cppreference: std::recursive_mutex</a></p>\n","group":"6. Deadlock и стратегии захвата","line":1023},{"id":"content/03_concurrency/01_base.md:3f381492b4fde6ab:1","question":"<p>Почему recursive mutex обычно не любят?</p>\n","answer":"<p><strong>Ответ:</strong> Он маскирует плохую архитектуру и потерю контроля над состоянием: если функция берет лок повторно, значит инварианты структуры данных могут быть разрушены предшествующим незавершенным вызовом. Кроме того, <code>std::recursive_mutex</code> медленнее обычного и не работает с <code>std::condition_variable</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Наличие recursive_mutex часто говорит о том, что код не разбит четко\n// на публичные синхронизирующие методы и приватные методы реализации.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#cp23-prefer-stdmutex-over-stdrecursive_mutex\">C++ Core Guidelines: CP.23: Prefer std::mutex over std::recursive_mutex</a></p>\n","group":"6. Deadlock и стратегии захвата","line":1041},{"id":"content/03_concurrency/01_base.md:94494471145eb6dc:1","question":"<p>Что такое priority inversion?</p>\n","answer":"<p><strong>Ответ:</strong> Инверсия приоритетов — проблема планирования, когда низкоприоритетный поток захватывает мьютекс, после чего его вытесняет среднеприоритетный поток; в итоге высокоприоритетный поток, ожидающий этот мьютекс, простаивает дольше, чем менее приоритетные задачи.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Классический баг марсохода Mars Pathfinder (1997):\n// High-priority поток ждал мьютекс, заблокированный Low-priority потоком,\n// который не мог выполниться из-за потоков со средним приоритетом.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.cs.unc.edu/~anderson/teach/comp737/notes/mars_pathfinder.html?utm_source=gemini\">Mike Jones: What Really Happened on Mars? (Priority Inversion)</a></p>\n","group":"6. Deadlock и стратегии захвата","line":1055},{"id":"content/03_concurrency/01_base.md:04f46dbc82993e6f:1","question":"<p>Как уменьшать риск deadlock в больших системах?</p>\n","answer":"<p><strong>Ответ:</strong> 1) Разрабатывать строгую иерархию блокировок (lock levels); 2) Минимизировать разделяемое изменяемое состояние в пользу очередей сообщений и Actor-моделей; 3) Проводить статический и динамический анализ кода (ThreadSanitizer); 4) Захватывать не более одной блокировки за раз.</p>\n<p><strong>Пример:</strong></p>\n<pre><code># Сборка с детектором гонок и дэдлоков Clang/GCC:\ng++ -fsanitize=thread -g main.cpp\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://github.com/google/sanitizers/wiki/ThreadSanitizerCppManual?utm_source=gemini\">Google Sanitizers: ThreadSanitizer</a></p>\n","group":"6. Deadlock и стратегии захвата","line":1070},{"id":"content/03_concurrency/01_base.md:528dcfab85196ac7:1","question":"<p>Для чего нужна <code>std::condition_variable</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::condition_variable</code> — примитив синхронизации, позволяющий потоку освободить мьютекс и эффективно уснуть (заблокироваться без траты CPU), пока другой поток не изменит разделяемое состояние и не уведомит об этом через <code>notify_one()</code> или <code>notify_all()</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;condition_variable&gt;\nstd::condition_variable cv;\nstd::mutex mtx;\nbool ready = false;\n\n// Ожидание:\nstd::unique_lock lk(mtx);\ncv.wait(lk, []{ return ready; });\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/condition_variable?utm_source=gemini\">cppreference: std::condition_variable</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1086},{"id":"content/03_concurrency/01_base.md:e9d7f4be8123023a:1","question":"<p>Почему <code>wait()</code> делают в цикле?</p>\n","answer":"<p><strong>Ответ:</strong> Проверка в цикле (<code>while(!ready) wait()</code>) обязательна из-за: 1) ложных пробуждений (spurious wakeups); 2) гонки между пробужденными потоками: пока поток просыпался и захватывал мьютекс, другой поток мог успеть изменить условие обратно.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// cv.wait(lk, predicate) внутри разворачивается именно в такой цикл:\nwhile (!ready) {\n    cv.wait(lk);\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.manning.com/books/c-plus-plus-concurrency-in-action-second-edition?utm_source=gemini\">Anthony Williams: C++ Concurrency in Action (Waiting for conditions)</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1106},{"id":"content/03_concurrency/01_base.md:0c9426e84349cb2a:1","question":"<p>Что такое spurious wakeup?</p>\n","answer":"<p><strong>Ответ:</strong> Spurious wakeup (ложное пробуждение) — пробуждение потока из состояния ожидания на условной переменной операционной системой при отсутствии реального вызова <code>notify()</code> со стороны других потоков (допускается реализациями POSIX и Windows ради производительности ядерных примитивов).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Поток проснулся, но данные еще не готовы: без проверки предиката будет ошибка!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://pubs.opengroup.org/onlinepubs/9699919799/functions/pthread_cond_wait.html?utm_source=gemini\">POSIX standard: pthread_cond_timedwait (Spurious wakeups)</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1122},{"id":"content/03_concurrency/01_base.md:0cb4778224c813bc:1","question":"<p>Почему predicate важен?</p>\n","answer":"<p><strong>Ответ:</strong> Предикат — это булева функция, проверяющая реальное состояние разделяемых данных. Он защищает алгоритм от преждевременного выхода при ложных пробуждениях и гарантирует, что поток продолжит работу только тогда, когда требуемые данные действительно готовы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>cv.wait(lk, [&amp;queue]() { return !queue.empty(); }); // Предикат\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#cp42-dont-wait-without-a-condition\">C++ Core Guidelines: CP.42: Don't wait without a condition</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1135},{"id":"content/03_concurrency/01_base.md:66081fe0bec473b6:1","question":"<p>Когда использовать <code>notify_one()</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>notify_one()</code> используется тогда, когда изменение состояния может быть обработано ровно одним потоком (например, в очередь пришла одна новая задача для пула потоков), чтобы избежать паразитных пробуждений остальных воркеров.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>queue.push(task);\ncv.notify_one(); // Просыпается ровно 1 рабочий поток\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/condition_variable/notify_one?utm_source=gemini\">cppreference: std::condition_variable::notify_one</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1148},{"id":"content/03_concurrency/01_base.md:51dd6969d122f099:1","question":"<p>Когда нужен <code>notify_all()</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>notify_all()</code> необходим, когда: 1) изменившееся условие касается всех ожидающих (сигнал завершения программы / shutdown); 2) один вызов удовлетворяет предикаты сразу нескольких потоков (например, пришло сразу несколько пакетов данных).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>is_stopping = true;\ncv.notify_all(); // Будим все потоки для штатного выхода\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/condition_variable/notify_all?utm_source=gemini\">cppreference: std::condition_variable::notify_all</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1162},{"id":"content/03_concurrency/01_base.md:71294bdd2de4d610:1","question":"<p>Что такое thundering herd?</p>\n","answer":"<p><strong>Ответ:</strong> Thundering herd (проблема громогласного стада) — ситуация, когда событие пробуждает одновременно множество спящих потоков (<code>notify_all</code>), но задачу может выполнить только один из них; в результате все потоки начинают бороться за один мьютекс, растрачивая ресурсы CPU на переключения контекста.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// 50 потоков проснулись по notify_all, 1 забрал элемент из очереди,\n// остальные 49 снова заснули, потратив тысячи тактов процессора.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.wikipedia.org/wiki/Thundering_herd_problem?utm_source=gemini\">Wikipedia: Thundering herd problem</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1176},{"id":"content/03_concurrency/01_base.md:6401b40ab5359222:1","question":"<p>Нужно ли держать mutex во время <code>notify_*</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Держать мьютекс во время вызова <code>notify</code> не требуется и часто не рекомендуется. Оптимальный паттерн: изменить данные под мьютексом, освободить мьютекс (<code>lk.unlock()</code>), и только потом вызвать <code>notify()</code>, чтобы разбуженный поток не наткнулся на еще заблокированный мьютекс (anti-pessimization).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>{\n    std::lock_guard lk(mtx);\n    ready = true;\n} // Мьютекс освобожден\ncv.notify_one(); // Разбуженный поток сразу сможет захватить мьютекс!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/condition_variable/notify_one?utm_source=gemini\">cppreference: std::condition_variable::notify_one</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1190},{"id":"content/03_concurrency/01_base.md:17b0bbfd7c1a0f60:1","question":"<p>Что такое semaphore?</p>\n","answer":"<p><strong>Ответ:</strong> Семафор — примитив синхронизации, управляющий доступом к общему ресурсу с помощью внутреннего целочисленного счетчика доступных разрешений (кредитов). Потоки уменьшают счетчик при входе (<code>acquire</code>) и увеличивают при выходе (<code>release</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;semaphore&gt;\n// Семафор, разрешающий одновременный доступ не более чем 3 потокам:\nstd::counting_semaphore&lt;3&gt; sem(3);\nsem.acquire(); // Взять кредит\n// работа\nsem.release(); // Вернуть кредит\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.cs.utexas.edu/users/EWD/transcriptions/EWD00xx/EWD74.html?utm_source=gemini\">Dijkstra, E. W.: Over seinpalen (Semaphores)</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1207},{"id":"content/03_concurrency/01_base.md:2c82b856d8cb6eba:1","question":"<p>Чем semaphore отличается от condition variable?</p>\n","answer":"<p><strong>Ответ:</strong> Семафор обладает внутренней памятью состояния (счетчик сохраняет вызовы <code>release</code>, даже если в данный момент никто не ждет). Condition variable памяти не имеет: вызов <code>notify</code> в момент, когда никто не ждет на <code>wait</code>, полностью теряется. Кроме того, семафор не требует сопутствующего мьютекса для ожидания.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>sem.release(); // Счетчик стал 1.\nsem.acquire(); // Следующий поток сразу пройдет без сна.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://greenteapress.com/wp/semaphores/?utm_source=gemini\">The Little Book of Semaphores (Allen B. Downey)</a></p>\n","group":"7. Condition variable, semaphore, ожидание","line":1225},{"id":"content/03_concurrency/01_base.md:adb44391c625ede5:1","question":"<p>Что такое <code>std::counting_semaphore</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::counting_semaphore</code> (C++20) — шаблонный класс семафора общего вида, поддерживающий произвольное максимальное неотрицательное количество разрешений, задаваемое через шаблонный параметр.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;semaphore&gt;\nstd::counting_semaphore&lt;10&gt; connection_pool_limiter(10);\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/counting_semaphore?utm_source=gemini\">cppreference: std::counting_semaphore</a></p>\n","group":"8. Современные примитивы C++20+","line":1241},{"id":"content/03_concurrency/01_base.md:819d20c185610c89:1","question":"<p>Что такое <code>std::binary_semaphore</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::binary_semaphore</code> (C++20) — псевдоним (alias) для <code>std::counting_semaphore&lt;1&gt;</code>. Он принимает только два значения счетчика: 0 и 1. В отличие от мьютекса, бинарный семафор может быть освобожден (<code>release</code>) из совершенно другого потока, нежели тот, который вызвал <code>acquire</code>.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::binary_semaphore signal(0);\n// Поток 1: signal.acquire(); // Ждет сигнала от потока 2\n// Поток 2: signal.release(); // Посылает сигнал\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/counting_semaphore?utm_source=gemini\">cppreference: std::counting_semaphore</a></p>\n","group":"8. Современные примитивы C++20+","line":1255},{"id":"content/03_concurrency/01_base.md:6663c2a4eb0751d4:1","question":"<p>Что такое <code>std::latch</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::latch</code> (C++20) — одноразовый синхронизационный барьер обратного отсчета. Потоки декрементируют счетчик (<code>count_down</code>), а ждущие потоки блокируются (<code>wait</code>), пока счетчик не достигнет нуля. После обнуления защелка не может быть перезапущена.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;latch&gt;\nstd::latch start_gate(3); // Ждем 3 воркеров\n// В каждом потоке: start_gate.count_down();\n// В главном потоке: start_gate.wait(); // Все 3 готовы!\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/latch?utm_source=gemini\">cppreference: std::latch</a></p>\n","group":"8. Современные примитивы C++20+","line":1270},{"id":"content/03_concurrency/01_base.md:8bb5aa7d3904e50b:1","question":"<p>Что такое <code>std::barrier</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::barrier</code> (C++20) — многоразовый фазовый барьер для группы потоков. Потоки подходят к барьеру и блокируются на вызове <code>arrive_and_wait()</code>. Когда все $N$ потоков фазы пришли, барьер выполняет завершающий коллбэк (completion function) и автоматически открывается для следующей итерации.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;barrier&gt;\nstd::barrier sync_point(4, []{ std::cout &lt;&lt; &quot;Phase complete!\\n&quot;; });\n// Потоки синхронно шагают от фазы к фазе:\nsync_point.arrive_and_wait();\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/thread/barrier?utm_source=gemini\">cppreference: std::barrier</a></p>\n","group":"8. Современные примитивы C++20+","line":1286},{"id":"content/03_concurrency/01_base.md:d64b205e61a2f7a1:1","question":"<p>Когда latch удобнее condition variable?</p>\n","answer":"<p><strong>Ответ:</strong> Когда нужно реализовать разовую синхронизацию инициализации (one-shot rendezvous): дождаться старта пула из $N$ потоков или завершения пачки асинхронных задач. <code>std::latch</code> не требует мьютексов, очередей и предикатов, работая значительно быстрее и компактнее.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::latch done(10);\n// 10 воркеров делают: done.count_down();\ndone.wait(); // Код тривиален и безопасен\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.youtube.com/watch?v=ZQFzMfHIxng?utm_source=gemini&amp;utm_source=gemini\">Bryce Adelstein Lelbach: The C++20 Synchronization Library (CppCon)</a></p>\n","group":"8. Современные примитивы C++20+","line":1302},{"id":"content/03_concurrency/01_base.md:b6c43d5e1219d64e:1","question":"<p>Когда barrier полезен?</p>\n","answer":"<p><strong>Ответ:</strong> В фазовых циклических параллельных алгоритмах: физическое моделирование, клеточные автоматы, параллельный рендеринг, перемножение матриц, где на шаге $K+1$ всем потокам требуются готовые результаты шага $K$ от всех соседей.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>for (int step = 0; step &lt; STEPS; ++step) {\n    compute_local_cells();\n    barrier.arrive_and_wait(); // Ждем всех перед обменом границами\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.manning.com/books/c-plus-plus-concurrency-in-action-second-edition?utm_source=gemini\">Anthony Williams: C++ Concurrency in Action (Synchronizing with barriers)</a></p>\n","group":"8. Современные примитивы C++20+","line":1317},{"id":"content/03_concurrency/01_base.md:4fa02b013dc9fb20:1","question":"<p>Что такое <code>atomic_wait</code> / <code>atomic_notify_*</code>?</p>\n","answer":"<p><strong>Ответ:</strong> Это функции C++20 (<code>std::atomic::wait</code>, <code>notify_one</code>, <code>notify_all</code>), предоставляющие возможность блокирующего ожидания изменения значения атомарной переменной без явного создания <code>std::condition_variable</code> и мьютекса (внутри использует быстрые системные вызовы типа Linux <code>futex</code>).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>std::atomic&lt;int&gt; a{0};\n// Поток 1: ждет, пока значение равно 0\na.wait(0);\n\n// Поток 2: меняет и будит\na.store(1);\na.notify_one();\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/atomic/atomic/wait?utm_source=gemini\">cppreference: std::atomic::wait</a></p>\n","group":"8. Современные примитивы C++20+","line":1333},{"id":"content/03_concurrency/01_base.md:7c20a64db5ab809a:1","question":"<p>Чем <code>atomic_wait</code> хорош?</p>\n","answer":"<p><strong>Ответ:</strong> Он не требует накладных расходов на создание объектов <code>std::condition_variable</code> и <code>std::mutex</code>, использует минимально возможный объем памяти (прямо внутри 4-байтного атомика) и опирается на нативную поддержку системных ядерных вызовов парковки потоков (<code>futex</code> в Linux, <code>WaitOnAddress</code> в Windows).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Реализация сверхлегкого события (Event) на базе 1 атомика:\nstd::atomic&lt;bool&gt; flag{false};\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://wg21.link/p1135r1?utm_source=gemini\">Olivier Giroux: The C++20 Synchronization Library</a></p>\n","group":"8. Современные примитивы C++20+","line":1352},{"id":"content/03_concurrency/01_base.md:7340c1ee067fb475:1","question":"<p>Когда spin-wait лучше block-wait?</p>\n","answer":"<p><strong>Ответ:</strong> Активное ожидание (spin-wait) выгоднее, когда ожидаемая задержка наступления события экстремально мала (меньше времени переключения контекста ОС — порядка единиц микросекунд или сотен наносекунд), что устраняет оверхед на системный вызов усыпления и пробуждения ядра.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Высокочастотный спин-лок с паузой процессора:\nwhile (flag.test_and_set(std::memory_order_acquire)) {\n    #if defined(__x86_64__)\n    _mm_pause(); // Снижает нагрузку на конвейер CPU\n    #endif\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html?utm_source=gemini\">Intel 64 and IA-32 Architectures Optimization Reference Manual (PAUSE instruction)</a></p>\n","group":"8. Современные примитивы C++20+","line":1366},{"id":"content/03_concurrency/01_base.md:811cdc2dbf2e1add:1","question":"<p>Когда spin-wait плох?</p>\n","answer":"<p><strong>Ответ:</strong> При длительном ожидании или высокой конкуренции: поток на 100% сжигает вычислительную мощность ядра CPU, генерирует лишнее тепло (троттлинг), отбирает процессорное время у других потоков и может спровоцировать проблему инверсии приоритетов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Если поток-владелец был вытеснен планировщиком ОС, крутящийся спинлок\n// будет бессмысленно жечь миллионы тактов, ожидая его возвращения.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.kernel.org/doc/Documentation/locking/spinlocks.txt?utm_source=gemini\">Linux Kernel: Locking and Spinlocks</a></p>\n","group":"8. Современные примитивы C++20+","line":1384},{"id":"content/03_concurrency/01_base.md:303266e520b9577f:1","question":"<p>Что такое <code>std::atomic&lt;T&gt;</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>std::atomic&lt;T&gt;</code> — шаблонный класс, инкапсулирующий значение типа <code>T</code> и гарантирующий, что все операции чтения, записи и модификации над ним выполняются атомарно (неделимо на машинном уровне) без возникновения Data Race.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>#include &lt;atomic&gt;\nstd::atomic&lt;int&gt; counter{0};\ncounter++; // Атомарная инструкция (lock inc на x86)\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/atomic/atomic?utm_source=gemini\">cppreference: std::atomic</a></p>\n","group":"9. Atomics и lock-free","line":1400},{"id":"content/03_concurrency/01_base.md:2217f36219e6b1f5:1","question":"<p>Что значит lock-free?</p>\n","answer":"<p><strong>Ответ:</strong> Lock-free (свободный от блокировок) — свойство параллельного алгоритма, гарантирующее, что в любой момент времени хотя бы один поток в системе гарантированно совершает полезный прогресс за конечное число шагов, даже если часть остальных потоков была приостановлена планировщиком ОС.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Lock-free очередь: задержка одного потока не блокирует остальные потоки намертво\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://dl.acm.org/doi/10.1145/114005.102808?utm_source=gemini\">Maurice Herlihy: Wait-Free Synchronization (ACM TOPLAS)</a></p>\n","group":"9. Atomics и lock-free","line":1415},{"id":"content/03_concurrency/01_base.md:f4e30d98225fdea2:1","question":"<p>Что значит wait-free?</p>\n","answer":"<p><strong>Ответ:</strong> Wait-free (свободный от ожидания) — самый сильный класс неблокирующих алгоритмов: каждый поток гарантированно завершает свою операцию за строго ограниченное конечное число шагов, независимо от скорости, задержек и действий всех остальных потоков системы.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Чтение значения из std::atomic&lt;int&gt; является wait-free (одна инструкция mov).\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.elsevier.com/books/the-art-of-multiprocessor-programming/herlihy/978-0-12-397337-5?utm_source=gemini\">Herlihy &amp; Shavit: The Art of Multiprocessor Programming</a></p>\n","group":"9. Atomics и lock-free","line":1428},{"id":"content/03_concurrency/01_base.md:f5bb20ebffb2a269:1","question":"<p>Что значит obstruction-free?</p>\n","answer":"<p><strong>Ответ:</strong> Obstruction-free (свободный от препятствий) — слабейший класс неблокирующей синхронизации: поток гарантированно завершит свою операцию за конечное число шагов, если все остальные конкурирующие потоки будут временно приостановлены.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Алгоритмы на базе Software Transactional Memory (STM) часто obstruction-free.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://ieeexplore.ieee.org/document/1208884?utm_source=gemini\">Herlihy, Luchangco, Moir: Obstruction-Free Synchronization (IEEE)</a></p>\n","group":"9. Atomics и lock-free","line":1441},{"id":"content/03_concurrency/01_base.md:2560f01efa2e88e6:1","question":"<p>Что такое CAS?</p>\n","answer":"<p><strong>Ответ:</strong> CAS (Compare-And-Swap / Compare-Exchange) — фундаментальная атомарная инструкция процессора (<code>CMPXCHG</code> на x86): она сравнивает текущее значение ячейки памяти с ожидаемым, и только в случае равенства заменяет его на новое значение, возвращая признак успеха.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>int expected = 5;\nstd::atomic&lt;int&gt; val{5};\nbool success = val.compare_exchange_strong(expected, 10); // val стал 10\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/atomic/atomic/compare_exchange?utm_source=gemini\">cppreference: std::atomic::compare_exchange</a></p>\n","group":"9. Atomics и lock-free","line":1454},{"id":"content/03_concurrency/01_base.md:67179839c8d0669d:1","question":"<p>Чем <code>compare_exchange_weak</code> отличается от <code>strong</code>?</p>\n","answer":"<p><strong>Ответ:</strong> <code>compare_exchange_weak</code> может завершиться ложной неудачей (spurious failure — вернуть <code>false</code>, даже если значение совпадало с ожидаемым, например из-за прерывания на LL/SC процессорах вроде ARM). <code>weak</code> быстрее и предпочтителен внутри циклов <code>while</code>, а <code>strong</code> гарантирует отсутствие ложных сбоев и используется вне циклов.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>int expected = head.load();\n// В цикле ВСЕГДА используем weak для производительности:\nwhile (!head.compare_exchange_weak(expected, new_node)) {\n    // повтор при неудаче\n}\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://en.cppreference.com/w/cpp/atomic/atomic/compare_exchange?utm_source=gemini\">cppreference: std::atomic::compare_exchange_weak</a></p>\n","group":"9. Atomics и lock-free","line":1469},{"id":"content/03_concurrency/01_base.md:5ade2dcd0f7cdbae:1","question":"<p>Что такое ABA problem?</p>\n","answer":"<p><strong>Ответ:</strong> Проблема ABA — ошибка в lock-free алгоритмах на CAS: ячейка памяти имела значение <code>A</code>, другой поток изменил ее на <code>B</code>, а затем вернул значение обратно на <code>A</code>. Поток, выполняющий CAS, видит <code>A</code>, ошибочно полагает, что состояние системы не менялось, и совершает повреждение данных (например, обращается к удаленному узлу списка).</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Лечение: версионирование указателей (Tagged Pointers), Hazard Pointers или std::shared_ptr\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://dl.acm.org/doi/10.1145/1810931.1810943?utm_source=gemini\">Damian Dechev et al.: Practical Lock-Free Algorithms (ABA Prevention)</a></p>\n","group":"9. Atomics и lock-free","line":1486},{"id":"content/03_concurrency/01_base.md:2d6eaa534d08fde9:1","question":"<p>Почему lock-free не означает faster?</p>\n","answer":"<p><strong>Ответ:</strong> При высокой конкуренции (contention) циклы <code>compare_exchange</code> непрерывно перезапускаются (retries), вызывая шквал инвалидаций кэш-линий по шине процессора (cache line bouncing), что приводит к катастрофическому падению производительности по сравнению с мьютексом, который просто усыпляет лишние потоки.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// 64 потока долбят один lock-free стек: 63 потока промахиваются на CAS на каждой итерации.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://www.packtpub.com/product/the-art-of-writing-efficient-programs/9781800208117?utm_source=gemini\">Fedore Pikus: The Art of Writing Efficient Programs (Lock-free pitfalls)</a></p>\n","group":"9. Atomics и lock-free","line":1499},{"id":"content/03_concurrency/01_base.md:69d5985269ede221:1","question":"<p>Когда mutex лучше atomics?</p>\n","answer":"<p><strong>Ответ:</strong> Мьютекс превосходит атомики, когда критическая секция обновляет сразу несколько связанных переменных (сложный составной инвариант структуры данных), а также когда внутри секции требуется нетривиальная работа (выделение памяти, I/O), которую невозможно выразить единичным CAS.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Защитить сбалансированное красно-черное дерево или составной класс банковского счета\n// через мьютекс несравнимо проще, надежнее и быстрее, чем строить lock-free дерево.\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini\">C++ Core Guidelines: CP.atomic: Don't use atomics for complex invariants</a></p>\n","group":"9. Atomics и lock-free","line":1512},{"id":"content/03_concurrency/01_base.md:fbe311610858b619:1","question":"<p>Главное правило по concurrency в C++?</p>\n","answer":"<p><strong>Ответ:</strong> <strong>«Make it correct first, keep it simple, and measure before optimizing»</strong>. Начинать проектирование следует с минимизации разделяемого состояния, изоляции данных по потокам и использования стандартных высокоуровневых примитивов (<code>std::jthread</code>, <code>std::scoped_lock</code>, параллельные алгоритмы STL). К сложным lock-free структурам и тонкому ручному <code>memory_order</code> следует переходить только при доказанной профилировщиком необходимости.</p>\n<p><strong>Пример:</strong></p>\n<pre><code>// Золотой стандарт надежного параллелизма:\n// 1. Независимые данные\n// 2. RAII блокировки\n// 3. Замер через perf / VTune / ThreadSanitizer\n\n</code></pre>\n<p><strong>Источник:</strong> <a href=\"https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines?utm_source=gemini#cpcon-concurrency\">Bjarne Stroustrup, Herb Sutter: C++ Core Guidelines (Concurrency Philosophy)</a></p>\n<p><a href=\"./02_review_code.md\">Next -&gt;</a></p>\n","group":"9. Atomics и lock-free","line":1526}];
